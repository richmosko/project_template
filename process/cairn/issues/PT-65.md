---
id: PT-65
title: Roster payload: structured work field (replace .split(':') string parsing)
status: done
milestone: PT-0.8
parent: null
blocked_by: []
assignee: null
labels: []
priority: P3
pr: https://github.com/richmosko/project_template/pull/136
created: 2026-08-28
updated: 2026-08-28
---


## Comments

### @implementation-lead — 2026-08-28

Split out of PT-60 (implementer's judgment, per team-lead's explicit
"take it only if it doesn't balloon the PR; splitting to a new issue is
fine and say so").

Original ask (architect, PT-56 review): structured `work` in the roster
payload -- `{id, title, status, kind}` with `kind ∈ current|stale|history`
-- replacing the server-composed English strings the component re-parses
with `.split(':')`. Today "last shipped " is load-bearing framing that
exists only as prose; restructure when the panel next grows.

Touches: `build_roster_payload` (cairn.py, server-side composition),
`RosterAgent`'s `work` type (dashboard-api.ts), and App.svelte's
`agent.work.split(':')[0]` usage in the roster `<details>` summary --
a real payload-shape change with its own test surface (Python payload
shape + a JS/svelte-source guard for the new field access), not a
same-PR fit alongside PT-60's four already-landed items.

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-65-roster-structured-work`.

### @team-lead — 2026-08-28

**Ruled (team-lead, 2026-08-28): the two open kind-mapping cells.** `pending` (backlog/todo assignment) → `kind: current` — it's queued current-cycle work, not history. `cancelled-only` → `kind: history` — extends PT-56's own principle ("a done assignment renders only as history") to the other terminal status; a teammate whose only assignment was cancelled is not currently working, and the client renders the history entry with its cancelled status the same way it composes "Last shipped" for done. (Supersedes implementation-lead's proposed cancelled→current+suffix, which would contradict the PT-56 honesty framing.) qa may tighten the two loose guards to these values.

### @qa-engineer — 2026-08-28

Tightened both guards to the ruled values (`test_dashboard_roster_structured_work.py`:
`test_pending_backlog_or_todo_maps_to_kind_current`,
`test_cancelled_only_maps_to_kind_history`). While tightening I found and
fixed two false-positive bugs of my own: the `RosterAgent` type-block
regex didn't handle the nested `work: { ... } | null;` object (stopped at
its inner closing brace, not the type's own); and the `.split(':')`
literal-substring check would have false-failed on a doc comment that
*mentions* the old pattern by name for explanatory purposes (this file's
own implementation does exactly that) -- replaced with a real
method-call-shape regex. Neither was implementation-lead's issue, both
were my own guard's blind spots.

Independently verified the landed implementation directly (not just
green tests): `build_roster_payload`'s docstring and branches match the
ruling exactly (pending -> `_work(pending, "current")`, cancelled-only ->
`_work(other, "history")`, done -> `"history"`, stale -> `"stale"`,
working -> `"current"`); `RosterAgent['work']` in dashboard-api.ts is the
structured `{id,title,status,kind}|null` type; App.svelte's
`formatWorkLine` correctly re-derives the old prose framing ("Last
shipped ..." for done, `(status)` suffix for cancelled/pending, plain for
active/stale) from the structured fields client-side, and the collapsed
`<summary>` reads `agent.work.id` directly -- no `.split(':')` left as
real code anywhere. Full suite: 851/851 green from a clean shell.
`check_dist_freshness.py` gives the expected pre-commit "uncommitted
changes" message (not a false pass); dist's `index.js` mtime is newer
than both edited source files, confirming a genuine post-edit rebuild.
Merge-ready from QA's side pending commit + PR (will re-confirm against
the sha then, same drill as PT-61/64/67).

### @team-lead — 2026-08-28

PR opened: https://github.com/richmosko/project_template/pull/136. Awaiting Validate.

### @qa-engineer — 2026-08-28

Validate: confirmed PR #136 head (54d522e) matches local HEAD exactly.
Re-ran `check_dist_freshness.py` now that everything's committed — genuine
pass ("dist/ is up to date with source"). Merge approved, pinned to
54d522e.

### @team-lead — 2026-08-28

Validate passed; merging via PR #136. Closing.
