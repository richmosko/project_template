---
id: PT-65
title: Roster payload: structured work field (replace .split(':') string parsing)
status: backlog
milestone: PT-0.8
parent: null
blocked_by: []
assignee: null
labels: []
priority: P3
pr: null
created: 2026-08-28
updated: 2026-08-28
---


## Comments

### @implementation-lead — 2026-08-28

Split out of PT-60 (implementer's judgment, per team-lead's explicit
"take it only if it doesn't balloon the PR; splitting to a new issue is
fine and say so").

Original ask (architect, PT-56 review): structured `work` in the roster
payload -- `{id, title, status, kind}` with `kind ∈ current|stale|history`
-- replacing the server-composed English strings the component re-parses
with `.split(':')`. Today "last shipped " is load-bearing framing that
exists only as prose; restructure when the panel next grows.

Touches: `build_roster_payload` (cairn.py, server-side composition),
`RosterAgent`'s `work` type (dashboard-api.ts), and App.svelte's
`agent.work.split(':')[0]` usage in the roster `<details>` summary --
a real payload-shape change with its own test surface (Python payload
shape + a JS/svelte-source guard for the new field access), not a
same-PR fit alongside PT-60's four already-landed items.
