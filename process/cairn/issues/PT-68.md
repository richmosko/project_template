---
id: PT-68
title: Dashboard: sidebar header shows repo name, not 'Cairn'
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: null
labels: []
priority: null
pr: null
created: 2026-08-28
updated: 2026-08-28
---


## Comments

### @team-lead — 2026-08-28

Scope (Mosko, 2026-08-28): the dashboard sidebar's top-left header currently reads "Cairn" — every clone of the template shows the same label. Uniquify it: show the REPO NAME instead, so each project's dashboard identifies itself. Implementation note: derive the name server-side (the engine already reads git state; repo root basename is the obvious candidate — team decides the exact source) and surface it through the payload rather than hardcoding client-side.

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-68-repo-name-header`. Wraps PT-0.8 — milestone re-flips done at this merge, then v0.8.0 tags.

### @qa-engineer — 2026-08-28

Validate: independently verified. Read the cairn.py diff -- `repo_name`
derives via `_repo_root_for(data_dir).name` (reused, no second git-root
helper) and is included in BOTH the normal and git-unavailable-degrade
return paths, matching the endorsed asymmetry exactly, with a docstring
update explaining why. Read the App.svelte diff -- `Sidebar.Header` now
renders `{data?.git.repo_name ?? '…'}`, an honest loading placeholder
only (never a fabricated name). Ran the full suite myself from a clean
shell: 864/864 green. `check_dist_freshness.py` gives the expected pre-
commit message; dist's `index.js` mtime is newer than both edited source
files, confirming a genuine rebuild. Merge-ready from QA's side; will
re-confirm against the sha once a PR exists.
