---
id: PT-70
title: Embedded board: live theme propagation + disable pull-to-refresh
status: done
milestone: PT-0.9
parent: null
blocked_by: []
assignee: null
labels: []
priority: null
pr: https://github.com/richmosko/project_template/pull/142
created: 2026-08-29
updated: 2026-08-29
---


## Comments

### @team-lead — 2026-08-29

Mosko's post-merge live-test findings #1 and #2 (2026-08-29):
- **Live theme propagation into the embed**: changing theme on the dashboard does NOT restyle the embedded kanban until a page reload. The board's cross-context follow should apply ALL dimensions (attrs + mode) when `cairn.theme` changes from the parent context — investigate whether the storage listener is missing in the embed, only handles mode, or doesn't re-apply attributes.
- **Pull-to-refresh inside the embed is clunky**: disable the PT-33 trackpad-overscroll pull-to-refresh adapter when `isEmbedMode` — the dashboard card context makes it awkward UI; keep it on the standalone board.

### @team-lead — 2026-08-29

Feature started. Branch: `feature/pt-70-embed-theme-ptr`.

### @qa-engineer — 2026-08-29

**QA sha-pinned approval — PR #142 @ b41d871**

Confirmed head via `git rev-parse HEAD` — `b41d871` (fix commit `dc5af9f` + tracker-status `aef0225` + guard-hardening `b41d871`), matching PR #142's moved head exactly, not the earlier `aef0225`. Clean working tree.

Ran the full gate fresh, post-commit, clean shell:
- Python: `python3 -m unittest discover -s tests` — **984 tests, 0 failures, 0 errors, 1 expected skip**.
- JS: `node --test tests/js/*.test.js` — **384/384 passing**.
- `npm run build` — clean.
- `npm run check` (svelte-check + tsc) — **0 errors, 0 warnings, 926 files**.

Covers both findings end to end: live theme propagation into the embedded board (storage + matchMedia listeners now reachable regardless of isEmbedMode — confirmed the fix removed the unconditional early `return` that used to skip them), and pull-to-refresh (touch + wheel/trackpad adapter together) gated off in the embed while staying on standalone (PT-33's pure-logic JS tests unaffected, verified they reference neither `isEmbedMode` nor `wirePullToRefresh` at all). Plus the head-time `:root.embed` flash-window fix (bootstrap class assignment + CSS hide rule), including my own guard's regex being fooled by implementation-lead's explanatory comment on the first pass — fixed at the guard level (comment-stripped search) with a self-test reproducing the exact collision, not just patched around.

**Approved.**

### @team-lead — 2026-08-29

Validate passed; merging via PR #142. Closing.
