---
id: PT-104
title: dashboard: one owner for the wire payload type — dashboard-api.ts redeclares TokensPayload/TokenIssueTotal (5 svelte-check errors)
status: in-progress
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: implementation-lead
labels: [dashboard, hygiene]
priority: P3
pr: null
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Found by implementation-lead on PT-101 (svelte-check, outside the finish-feature gate) and adjudicated by the architect at the PT-101 verdict (PT-101.md @ f3e4ef0): `dashboard-api.ts:292-298` declares a second copy of the `/api/tokens` wire payload type that `token-chart-logic.ts` also declares, and the two have drifted (5 pre-existing svelte-check errors, none introduced by PT-88/PT-101/PT-102 — confirmed against the pre-diff tree). Ask: give the type one owner (export from one module, import in the other), delete the duplicate, and add `svelte-check` to the finish-feature pre-flight so drift can't reappear silently — or record why it stays out of the gate. Not attached to PT-0.12.1 (closing); place at the next planning touch.

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-104-tokens-payload-type-owner`.

**Gate 1 brief (architect):** `dashboard-api.ts` (~L292-298) and `token-chart-logic.ts` each declare the `/api/tokens` wire payload type (`TokensPayload`/`TokenIssueTotal`) and they have drifted — 5 pre-existing svelte-check errors. Rule on: (1) which module owns the type (the fetch layer `dashboard-api.ts` is the natural owner; the chart logic imports it) and the exact shape after reconciling the drift against what `cairn.py`'s `/api/tokens` handler actually emits — the Python side is the source of truth, name the fields; (2) whether `svelte-check` joins the finish-feature pre-flight as a hard gate (cost: node_modules + a few seconds; benefit: type drift can't reappear silently) or stays out with the reason recorded; if in, a scan test pins the SKILL.md line the way PT-83 pinned the JS command; (3) the dist rebuild rule — a type-only change still trips `check_dist_freshness` (git-aware), so dist must be rebuilt and committed; PT-106 (non-deterministic build) is not in scope. qa: a test that svelte-check exits 0 on the dashboard (skipped with a named reason when node_modules is absent) plus the SKILL scan if (2) is in. Writers: implementation-lead → the two TS files, dist, SKILL.md if (2); qa → tests.
