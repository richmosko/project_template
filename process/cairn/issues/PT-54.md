---
id: PT-54
title: Dashboard shell: /dashboard route + tokens.css + status cards from real repo state
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: null
labels: []
priority: P1
pr: null
created: 2026-08-26
updated: 2026-08-26
---


The dashboard's foundation: a **real shadcn-svelte app** (ruled 2026-08-26,
Mosko — see TEMPLATE_DECISIONS.md) using actual Components/Blocks/Charts,
served by the cairn board server, rendering real repo state. The vanilla
zero-build principle stays for the existing board; the dashboard is where
the template dogfoods the stack downstream projects will use.

**Acceptance criteria**
- [x] Svelte + Tailwind + shadcn-svelte app scaffolded (location per
      architect ruling, e.g. `apps/dashboard/`), preset `b6XadDxmQS` tokens
      as the app.css `:root`/`.dark` blocks exactly per
      `design-system-spec.md`; `docs/DESIGN/tokens.css` ships as the same
      blocks (reference deliverable)
- [x] Dashboard page composed from real shadcn-svelte components (Card,
      Badge, Button, Table; a dashboard Block as the layout starting
      point), styled at the spec's Dashboard scale
- [x] Fonts self-hosted (e.g. Fontsource) — no CDN at runtime; architect
      confirms
- [x] Board server serves the built app at `/dashboard`; a
      `/api/dashboard` payload carries real data: current branch +
      clean/dirty (git), latest tag + release row, tracker counts by
      status (engine), `cairn check` result — server-side only
- [x] Build-output policy ruled by architect (committed dist vs on-demand
      build) and documented
- [x] Tests cover the payload endpoint (py harness) and the app's build
      (smoke: built entry exists / renders)

## Comments

### @team-lead — 2026-08-26

Feature started. Branch: `feature/pt-54-dashboard-shell`.

### @architect — 2026-08-26

Four rulings the implementation is gated on. All four are decided — nothing here
is blocked on the team-lead. Verified against `scripts/cairn/cairn.py` @ HEAD
(`_send_static`/`do_GET`/`make_server`/`read_git_tags`), the root `.gitignore`,
`docs/DESIGN/design-system-spec.md`, and the shadcn-svelte + Fontsource registries
(2026-08-26).

## 1. App location — `scripts/cairn/dashboard/` (source), `scripts/cairn/dashboard/dist/` (built)

Sibling of `board/`, under the server that serves it. `DASHBOARD_DIR =
Path(__file__).resolve().parent / "dashboard" / "dist"`, exactly the `BOARD_DIR`
constant's shape — cwd-independent, one line of binding.

Why not `apps/dashboard/`: **this is a template.** `apps/` is the most natural
home for the *downstream project's own product code*; template tooling must not
squat it. A cloner who runs `apps/` for their real app would find our tool
already living there and infer a monorepo convention we are not prescribing.

Second reason, load-bearing: cairn is a spin-off candidate (`/spin-off-component`).
Keeping the dashboard inside `scripts/cairn/` keeps that extraction one directory
move. It survives the extraction functionally too — see §4: the payload reads
only tracker files + git, nothing else in this repo.

Third: PT-57 (board → shadcn) gets an obvious home next door, and can share the
same package.json rather than standing up a second toolchain.

**Trade-off accepted:** a Node project nested under `scripts/` is unusual, and
root-level tooling that auto-discovers `package.json` won't find it. Cost is one
line of README. If the dashboard ever must outlive cairn, the move is mechanical
(one constant + one route prefix).

**Framework flavour: plain Vite + Svelte 5 + TypeScript (SPA), not SvelteKit.**
There is no SSR, no server route, no prerender need — the Python server is the
backend. SvelteKit + adapter-static buys a router and prerender semantics we'd be
configuring around for a single page. shadcn-svelte ships a first-class Vite
install path. Scaffold: `npm create vite@latest` (svelte-ts) → `npx sv add
tailwindcss` → `npx shadcn-svelte@latest init` (npm, not bunx — no bun on this
machine; node 26.7.0 / npm 11.19.0 present). If `sv add` refuses a non-Kit
project, fall back to manual Tailwind v4: `npm i -D tailwindcss @tailwindcss/vite`,
`@import "tailwindcss";` in `app.css`, plugin in `vite.config.ts` — do not let
that block you and do not switch to SvelteKit to dodge it.

**Two Vite settings are not optional** (see §3 and §4):
- `base: '/dashboard/'` — emitted asset URLs must be absolute under the prefix.
- `build.rollupOptions.output.{entryFileNames,chunkFileNames,assetFileNames}` set
  to stable `[name]` forms — no content hashes. Rationale in §3.
- `server.proxy` for `/api` → `http://127.0.0.1:8766` so `npm run dev` runs
  against real data.

`package-lock.json` is committed. `node_modules/` stays ignored.

## 2. Fonts — self-hosted via Fontsource variable packages. No CDN, confirmed.

This resolves the design spec's Pushback #2 in the direction the spec itself
flagged: the board's offline/zero-network contract still holds for `cairn serve`,
and a template that phones Google on every page load is a worse default than one
that ships 380KB once.

Concrete packages (all present on npm at **5.3.0**, verified today):

| Family | Package | Family name in CSS |
|---|---|---|
| Merriweather (body, `--font-sans`) | `@fontsource-variable/merriweather` | `'Merriweather Variable'` |
| Space Grotesk (headings, `--font-heading`) | `@fontsource-variable/space-grotesk` | `'Space Grotesk Variable'` |
| Geist Mono (`--font-mono`) | `@fontsource-variable/geist-mono` | `'Geist Mono Variable'` |

Those family strings match the stacks already written in the spec's Typography
table verbatim — don't re-invent them.

**Import the package default entry only** (`import '@fontsource-variable/
merriweather'` etc. in `src/main.ts`, not a CSS `@import` — keeps Tailwind v4's
`@import "tailwindcss";` at the top of `app.css` where it must be). The default
entry is the `wght` axis, normal style, all unicode subsets, `font-display: swap`.

Measured emitted weight at that import (unpkg file metadata, 5.3.0): Merriweather
5 files / 263KB, Space Grotesk 3 / 47KB, Geist Mono 6 / 69KB — **~14 woff2,
~380KB**, committed once and inert until a version bump. Do **not** import
`opsz.css` / `wdth.css` / the italic entries: Merriweather's full axis set is
4.6MB of woff2 and we use none of it.

Available trim if dist size ever annoys: hand-write `@font-face` blocks against
`…/files/*-latin-wght-normal.woff2` for latin+latin-ext only → ~245KB → ~140KB.
Not worth owning the declarations today.

No manual `<link rel=preload>` in v1. If FOUC is visible on first paint, preload
the *latin* Merriweather file only — one file, not the set.

Reminder from the spec that bites here: display/stat numbers are Merriweather
**bold**, not Space Grotesk. Face is by role, not size.

## 3. Build output — committed, unhashed, at `scripts/cairn/dashboard/dist/`

**Commit the built output.** The template's strongest property is that a fresh
clone runs `python3 scripts/cairn/cairn serve` with nothing but python3 — it is
why the board is vanilla in the first place. Making `/dashboard` require `npm ci`
(hundreds of MB of node_modules) before it renders would break that property for
every cloner, on the one surface most meant to be seen immediately.

The root `.gitignore` ignores `dist/` globally, so this needs an explicit,
commented negation — ordering matters, negation must come after:

```
dist/
!scripts/cairn/dashboard/dist/
```

(`dist/` matches only directories *named* dist, so nested `dist/assets/` is
re-included fine once the parent is un-ignored.)

**Disable content hashing.** `_send_static` already sends `Cache-Control:
no-store` on every asset (PT-49 §7) — hashed filenames buy exactly nothing
against a no-store server, while guaranteeing that every rebuild adds new blobs
and orphans old ones, so the repo grows monotonically with each UI change. Stable
names mean a rebuild overwrites the same paths. Keep `no-store`; that and
unhashed names are one decision, not two.

**What `cairn serve` does when dist is missing: nothing clever.** It does not
build, does not shell out to npm, does not crash, does not 500. `GET /dashboard`
returns **503** with a small python-generated HTML page naming the fix literally
(`cd scripts/cairn/dashboard && npm ci && npm run build`). `GET /api/dashboard`
keeps working — it is pure python and has no build dependency. `cairn check` does
**not** lint on dist presence: dist is not tracker data, and a lint error on a
clean clone of a repo whose dashboard was never built is noise.

Build-at-serve-time was rejected outright: it makes a fast read-only localhost
tool depend on a Node toolchain at runtime and turns a 40ms startup into seconds.

**The accepted cost is staleness** — a source edit shipped without a rebuild
serves an old bundle. Discipline, documented in `scripts/cairn/dashboard/
README.md`: *any PR touching dashboard source rebuilds and commits dist in the
same PR.* Same class of accepted risk as the vendored Mermaid bundle. A
`/finish-feature` freshness check (dist mtime vs newest src mtime) is a
reasonable follow-up, not this issue.

## 4. Serve integration — extend the existing server; one static sender; SSE + slow poll

**Extend `cairn.py`'s server. No second process, no second port.** Roots
resolution, the engine payload builders, the ETag machinery and the SSE
broadcaster all already live there; a second server would duplicate roots
resolution and invent a CORS problem we don't have.

**Static:** do not write a second static handler. Widen the existing one to
`_send_static(rel_path, base=BOARD_DIR)` so the traversal guard (`resolve()` +
`relative_to`) stays in exactly one place, and pass `DASHBOARD_DIR` from the
dashboard branch. Extend the MIME map — it currently knows html/js/css only; add
`.woff2 → font/woff2`, `.svg`, `.png`, `.ico`, `.json`, `.map`. A woff2 served as
`application/octet-stream` mostly works and is still wrong.

**Routing:**
- `/dashboard` and `/dashboard/` → `dist/index.html` (200). No redirect needed —
  `base: '/dashboard/'` makes every asset URL absolute, so there is no
  relative-resolution hazard at either spelling.
- `/dashboard/<rel>` → static from `DASHBOARD_DIR` when the resolved target is a
  file.
- **SPA fallback, narrowly:** target not a file **and** the path has no file
  suffix → `index.html` (200). A path *with* a suffix that doesn't exist → **404**.
  Returning HTML for a missing `.js` is the classic hours-lost debugging trap;
  don't.
- dist absent → 503 per §3.

Take `dashboard_dir: Path = DASHBOARD_DIR` as a `make_server` keyword — the
`source_path: Path = Path(__file__)` parameter is the existing precedent, and it
is what lets the harness test the missing-dist 503 branch without monkeypatching
a module global.

**`GET /api/dashboard`** — new endpoint, sibling of `/api/board`, assembled
server-side only (no git from the browser, per the AC). Payload:

- `git`: `{branch, dirty, head, latest_tag}` — `git -C <primary root path> …`
  (`rev-parse --abbrev-ref HEAD`, `status --porcelain`, `rev-parse --short HEAD`).
  Bounded `timeout=5`, **never raises**: git missing or not-a-worktree degrades
  to `null` + a warning string, exactly the `read_git_tags` / `_git_mv_or_rename`
  contract. Reuse `read_git_tags` for the tag set rather than a fourth subprocess.
- `tracker`: counts by status, **from the same load/parse path `/api/board`
  uses**. No second parser — two counters over the same files is a guaranteed
  future disagreement.
- `check`: `check_repo(primary_root.path)` → `{ok, errors}`.
- `release`: **derived from the tracker + git tags, NOT by parsing `STATE.md`.**
  This is the one place the AC's wording ("latest tag + release row") had to be
  interpreted, so flagging it explicitly: `read_git_tags`'s comment records a
  deliberate engine constraint — cairn reads *no file outside `process/cairn/`*,
  which is what keeps it spin-off-able (a git subprocess's own `.git` reads don't
  count, which is why branch/dirty above is fine). Parsing `STATE.md` would break
  that, and would couple the server to a human-maintained markdown table that has
  already been restructured twice this month. Milestones carry `target_tag` and
  `ga`; git carries the tags. The release row is that join: latest tag → the
  milestone whose `target_tag` matches → its name/status/ga. If a genuinely
  STATE.md-only field is ever needed, the correct shape is a separate reader
  module the *server* composes, never a read inside the engine.
- `generated_at`.

ETag it: hash the serialized body, honour `If-None-Match` → 304, `Cache-Control:
no-store`. Cheap to write, makes the poll below near-free on the wire.

**Freshness — SSE plus a slow poll, and the reason both are needed.** Subscribe
to the existing `/api/events` and refetch `/api/dashboard` on any frame (coarse
contract, same as the board — never a targeted diff). But the watcher scans
`process/cairn/` only, so a branch switch, a new tag, or a dirty working tree
emits **nothing**. Those fields therefore also need a **15s poll** of the same
endpoint. Do **not** extend the watcher to scan `.git` — that widens the fs-scan
that every board client already pays for, to serve one panel.

**Testing shape** (for qa-engineer): the committed gate is the python harness —
`/api/dashboard` 200 + payload keys + the git-unavailable degradation + ETag/304 +
`/dashboard` 200 + the missing-dist 503 (via the `dashboard_dir` kwarg) + the
suffix-vs-no-suffix fallback split. Build smoke: assert `dist/index.html` exists
and that the asset URLs it references resolve to real files under dist — that
catches a half-committed or stale-by-deletion dist. **No vitest/testing-library
in this feature** — `npm run build` succeeding plus `svelte-check` is the app-side
gate; a component-test harness is a follow-up ruling once there is component logic
worth testing.

Nits for implementation-lead: print the Dashboard URL in `cmd_serve`'s startup
banner next to Kanban/List, and put a link to `/dashboard` on the board header —
an unreachable surface is a broken one.

### @implementation-lead — 2026-08-26

Implemented per the ruling above. Both nits done (startup banner, board header
link). `scripts/cairn/tests/test_dashboard.py` (qa-engineer's failing suite,
24 tests) is green; full python suite (689 tests) and the JS harness (331
tests) both still pass with no regressions. Verified end-to-end against a
real `cairn serve` + curl: `/api/dashboard` returns this repo's real branch/
dirty/head/tag/tracker-counts/release/check state, `/dashboard` serves the
real built app.

Two things worth a second look:

1. **Release join widened to search archived milestones, not just live
   ones.** The ruling's join (latest tag → matching `target_tag` →
   milestone) as literally read would search only `/api/board`'s default
   live-only `milestones` list. Tested end-to-end against this repo's own
   `v0.7.1` tag: its milestone (`PT-0.7.1`) is already archived (this
   project's own workflow archives a milestone shortly after its tag
   ships), so a live-only search made `release` null for every real shipped
   tag. Widened `_find_release_milestone` to glob both `milestones/` and
   `archive/milestones/` (a small targeted lookup, not a second
   `build_board_payload` parse — `tracker.counts_by_status` stays live-only
   as ruled). Flagging in case there's a reason releases should stay
   live-only I'm not seeing.
2. **No shadcn "dashboard Block" as the layout starting point.** The AC's
   parenthetical names one as an example; I hand-built the layout directly
   from Card/Badge/Button/Table instead. `npx shadcn-svelte add dashboard-01`
   does exist in the registry, but pulls in a sidebar + chart stack sized for
   a nav-heavy multi-page app, not this four-card shell — judged
   disproportionate for PT-54's actual scope. Table and Button are both
   genuinely used (tracker status breakdown table; a manual refresh button
   and the board-placeholder's link), not just installed-and-unused.

Also found and fixed along the way (not scope creep, both were bugs in my own
first pass, not the ruling): `git.dirty` needs `status --porcelain
--untracked-files=no` — plain `--porcelain` flagged the tree dirty on any
untracked issue/milestone file, which is the routine, expected state of a
`cairn`-managed repo, not an uncommitted-code signal. And the ETag hash
excludes `generated_at` — hashing the full body meant every request's own
timestamp changed the ETag, defeating 304 entirely.

Deferred, not forgotten: agent-roster (PT-56) and board-embed (PT-55)
sections render as honest empty-state placeholders per the brief. dist/
freshness enforcement is PT-58 (team-lead already filed it, saw the state
update after finishing this).

### @architect — 2026-08-26

## Diff review — verdict: **approve with one blocking fix**

Reviewed by construction against the running server, not by reading the diff:
built `make_server` against this repo's own `process/cairn` at HEAD **2cf7c68**
(clean tree), exercised every route with urllib and again over a raw socket
(urllib normalizes `..` client-side, so the traversal cases only mean anything
raw), and unit-called the new helpers directly. Suites re-run here, not taken on
report: **690 python / 331 JS, all green** (the lead's 689 was one behind).

### The ruling asked for: archived-milestone release join — **RATIFIED**

Widening `_find_release_milestone` to search live **and** archived milestones is
correct, and I'd have ruled it this way at design time had I thought it through.
The join answers *"which milestone shipped this tag"* — a historical question,
and this workflow archives a milestone at release close, so history is exactly
where the answer lives. Live-only doesn't return a stale answer, it returns
**no** answer for effectively every real tag. Verified end to end at HEAD:
`latest_tag: v0.7.1` → `{id: PT-0.7.1, name: "board + archive follow-ups",
status: done, ga: false}`.

Ratified specifically because the implementation kept three properties:
- **Targeted, not a second board parse.** Two non-recursive `_dir_glob`s plus a
  frontmatter read. It does not reach for `build_board_payload(archived=True)`,
  which PT-42 measured at ~28× the live read.
- **Counts stayed live-only.** Right call, and the two scopes are not
  inconsistent: counts answer *"what's in flight"* and must agree with what the
  board shows by default; the release join answers *"what shipped"*. Different
  questions, different scopes — that's why only one widened.
- **Engine constraint intact.** Still nothing read outside `process/cairn/`.

**Does the archive-layout coupling need a guard? No new guard — but extract a
helper.** This is *not* the PT-50/52 hazard class: that was archived **issues**
moving from flat `archive/*.md` to `archive/issues/`; archived milestones have
always lived at `archive/milestones/`, and `_dir_glob` is non-recursive so a
relocation degrades to `release: null`, never to reading the wrong records. A
lint rule for a layout that has never moved would be inventing a guard for a
hypothetical. What *is* worth doing: `data_dir / "archive" / "milestones"` is now
spelled literally in **three** places (`build_board_payload`'s archived branch,
`_RECORD_SEARCH_SUBDIRS`, and `_find_release_milestone`). PT-52 §1's own
principle — one definition point, so a layout change has one place to fix —
applies verbatim. Extract `milestone_paths(data_dir, include_archived=False)` and
route `build_board_payload` and `_find_release_milestone` through it. Cheap
enough to ride in this PR; a follow-up issue is also fine. **Non-blocking.**

One thing the widening surfaces that predates it: **`target_tag` uniqueness is a
decision-recorded invariant with no enforcement.** The 2026-08-23 entry makes
milestone ↔ `target_tag` 1:1, but `check_repo` never checks it, and PT-54 is the
first consumer that *depends* on it. Today the glob order makes live win over
archived, which is the right tiebreak — but it's a tiebreak for a state the spec
says can't exist. Either lint it in `check_repo` (a few lines, matches the
recorded invariant) or document live-first as the deliberate precedence. My
preference is the lint. **Non-blocking, but file it.**

### BLOCKING — `_latest_semver_tag` sorts pre-release tags *above* their release

```
_latest_semver_tag({"v1.0.0", "v1.0.0-rc1"})  ->  "v1.0.0-rc1"
```

Verified by direct call at HEAD. The key is `(major, minor, patch, tag)`, so a
pre-release ties on the numeric triple and then wins the string tiebreak
(`"v1.0.0-rc1" > "v1.0.0"`).

This is not a hypothetical input. `WORKFLOW.md` prescribes pre-release tags in
three places — Model A cuts in-milestone checkpoints as `N.0.0-alpha.1` /
`-beta.1` / `-rc.1` and states in as many words that they **sort before**
`N.0.0`, and the `V2`+ pre-GA run-up is specified to use them. So the first time
this repo follows its own documented release procedure, the dashboard's headline
Release card reports the rc tag as latest and the release join then very likely
returns `null` (the milestone's `target_tag` is the *final* tag). Silent wrong
data on the one field the card exists to show.

Fix is three lines — rank "has a pre-release suffix" below "doesn't", per semver
§11:

```python
def _key(tag: str) -> Tuple[int, int, int, int, str]:
    m = _SEMVER_TAG_RE.match(tag)
    if not m:
        return (-1, -1, -1, -1, tag)
    rest = tag[m.end():]
    is_release = 0 if rest.startswith("-") else 1   # 1.0.0-rc.1 < 1.0.0
    return (int(m.group(1)), int(m.group(2)), int(m.group(3)), is_release, tag)
```

Plus one test (`{"v1.0.0", "v1.0.0-rc.1"} -> "v1.0.0"`). I'm calling this blocking
rather than a follow-up because it's ~5 minutes of work against a *documented*
convention, and the failure mode is wrong-but-plausible data rather than a
visible break. If the lead would rather ship and file it, say so and I'll
withdraw the block — but the cost asymmetry is stark.

### Ruling conformance — all four met

| Ruling | Verdict | How verified |
|---|---|---|
| §1 location / stack | ✅ | `scripts/cairn/dashboard/` + `dist/`, `DASHBOARD_DIR` mirrors `BOARD_DIR`; plain Vite + Svelte 5 + TS, `base: '/dashboard/'` present in `vite.config.ts` |
| §2 fonts | ✅ | 14 woff2 emitted, ~380KB — matches the pre-ruling estimate exactly (Merriweather 5 / Space Grotesk 3 / Geist Mono 6); default entries only, no opsz/wdth/italic; imported from `main.ts`, not a CSS `@import`, so `@import 'tailwindcss'` stays first; zero CDN references |
| §3 committed unhashed dist | ✅ | `.gitignore` negation placed after `dist/` with the precedence noted; `assetFileNames`/`entryFileNames` on stable `[name]`; a test asserts filenames carry no hash; missing dist → **503** naming the literal command, `/api/dashboard` still 200s, `cairn check` unaffected — all three probed against a `dashboard_dir` pointed at a nonexistent path |
| §4 serve integration | ✅ | one widened `_send_static(rel, base=…)`, guard not duplicated; MIME map extended (woff2 served as `font/woff2`, confirmed on the wire); `/dashboard` + `/dashboard/` → index.html; `/dashboard/some/client/route` → 200 index; `/dashboard/assets/missing.js` → **404**, not HTML; `/dashboard/../../cairn.py` and `/dashboard/../../board/board.css` → **403** over a raw socket; ETag honours `If-None-Match` → 304 with `generated_at` correctly excluded from the hash |

Two implementation choices I want to record as **ratified, not overlooked**:

- **`status --porcelain --untracked-files=no`.** I specified plain `--porcelain`.
  The narrowing is right — an untracked file mid-`cairn new` is the routine state
  of this repo and shouldn't read as a dirty tree. Consequence to keep honest:
  the "Clean" badge means *no uncommitted changes to tracked files*, not *nothing
  new on disk*. Fine as long as nobody later reads it as "safe to switch branch".
- **Whole-group degradation in `read_git_state`** (all fields null + one warning,
  rather than a partially-populated dict) is better than what I specified.

### Layout (skipped dashboard Block) — structurally sound

Product acceptability is Mosko's call and already with them; on structure alone I
have no objection. Header + 4-up status grid (`grid-cols-1 / sm:2 / lg:4`) +
table card + two honest empty-state sections in a `max-w-6xl` column is a
conventional dashboard skeleton — it's dashboard-01 minus the sidebar and the
chart, not an ad-hoc invention. Canvas convention is correctly implemented at the
`body` layer (`bg-muted` canvas, `bg-card` panels), so the muted plane covers the
viewport rather than just the centered column — I checked this specifically
because applying it to the column instead is the easy way to get white gutters.
Type roles follow the spec's role-not-size rule: the 28px stat value is
`font-sans` **bold** (Merriweather), the eyebrow and section headings are
`font-heading`. The two project-extension badge variants (`chart`, `inverted`)
are actually implemented in `badge.svelte` against preset tokens — no invented
hues. Empty states name their tracking issues (PT-55 / PT-56) instead of
fabricating data.

Three forward-looking notes, none blocking:
1. **No `Skeleton`.** The spec calls this out as the gap shadcn closes and asks
   every fetch boundary to render a skeleton shaped like its real content;
   loading here is `—` placeholders and a "Loading…" badge, and the component
   isn't installed. Worth closing when PT-55/56 add real fetch boundaries.
2. **No navigation.** Fine for a one-page shell; the moment the board embed and
   the roster land, this page needs a real nav surface (the spec maps that to
   `Sidebar`), not a button.
3. `aria-live` on the loading/error badge would make the state change audible to
   a screen reader; sections are otherwise labelled correctly.

Cost note for the record, since it's a consequence of my own §4 ruling: every
`/api/dashboard` hit — including the ones that end in 304 — runs a full board
parse plus `check_repo`, because the ETag is computed from the payload. At a 15s
poll per open tab that's fine for a single-user localhost tool; it is not a
pattern to carry to anything multi-client.
