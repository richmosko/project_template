---
id: PT-118
title: Settings → Chart Color no longer changes any chart — both charts moved to chart-local tokens that ignore the selected ramp
status: todo
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: ux-designer
labels: [dashboard, ux]
priority: P2
pr: null
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Mosko (2026-09-09): "chart-color in the top right settings no longer seems to take effect."

**Measured on the live board @main:** the preference mechanism itself works — switching Chart Color from `yellow` to `lime` sets `data-cairn-chart="lime"` on the root and `--chart-1` goes from `oklch(90.5% .182 98.111)` to `oklch(89.7% .196 126.665)`. Nothing consumes those tokens any more: the throughput chart draws from fixed `--chart-flow-opened/closed/wip` (PT-85 retired the ramp for that chart; PT-92 ratified the three hues), and the token chart draws from `--chart-counter-input/cache-write/cache-read/output` and `--chart-role-1..7` (PT-88/PT-101/PT-104), all defined once in app.css, not per chart variant — 56 bars rendered, every fill `oklch(0.65 0.0777 205)` regardless of the setting. So the setting is a no-op; the 24 chart variants in variants.css style tokens no chart reads.

**Ask (ux-designer + architect):** decide what "Chart Color" means now that charts are semantic (opened/closed/wip; input/cache/output; roles), and make the setting true to it. Candidates: (a) derive the chart-local tokens from the selected ramp per variant in `design/variants.json` → `gen_variants.py` (hue family follows the choice, the semantic distinctness guards and contrast floors from PT-92/PT-83 must still pass in every variant × mode); (b) retire the Chart Color control and its 24 variants as dead, with the removal recorded in the design spec; (c) keep the control but scope it to the token chart's role palette only, relabelled. Acceptance: whichever lands is browser-verified — change the setting, reload, and the affected chart's rendered fills/strokes change (elementFromPoint-sampled), or the control is gone; the palette guards stay green; both variants × light/dark. Not a PT-110 regression — measured against the PT-85 date.

### @team-lead — 2026-09-08

**Decision (Mosko, 2026-09-09): "Make it drive both charts."** ux-designer recommended retiring the control (nothing consumes `--chart-1..5`; the charts' fixed hue families pass the CVD/contrast guards); Mosko chose the larger option. Scope for the ruling: derive the chart-local tokens (`--chart-flow-opened/closed/wip`, `--chart-counter-*`, `--chart-role-1..7`) from the selected chart ramp per variant, defined in `design/variants.json` → `gen_variants.py` → the three variants.css copies, for all 24 chart variants × light/dark; PT-92's distinctness guards (role-collision ≥25°, pairwise ≥60°, lightness/chroma bands) and PT-83's contrast floors must pass in every combination, mechanically (the generator or a test iterates all variants); the default (`yellow`) must reproduce today's ratified values byte-for-byte so nothing regresses for anyone who never touched the setting. ux-designer owns the derivation rule and the spec entry; implementation-lead regenerates and rebuilds; qa iterates the guards across variants. Browser acceptance: switch the setting, reload, rendered fills/strokes change on both charts; two variants × light/dark sampled by the lead.

### @ux-designer — 2026-09-08

**Derivation rule (ux-designer).** Scope correction first: the role family is `--chart-role-1..8` (8, not 7 — team-lead's shorthand undercounted) plus 3 neutral guards (`-guard-aux`, `-guard-unattributed`, `-other`). The guards stay fixed across all variants — they're near-achromatic (C≈0.01–0.02), deliberately built to blend with `--muted-foreground`, not carry a role hue; rotating them serves nothing and risks pulling them into a visible hue.

**Mechanism: one global hue rotation per Chart Color variant, applied to the fixed families, L/C untouched.** `Δh(variant) = variant["chart-1"].h − yellow.chart-1.h` (98.111°), read from already-vendored `variants.json` — no new hue authoring. Apply `Δh` to every hue in `--chart-flow-{opened,closed,wip}`, `--chart-counter-{input,cache-write,cache-read,output}`, `--chart-role-1..8`, both modes (ramp hue is mode-invariant, confirmed in current data), holding each token's own L and C exactly at today's ratified value. Same code shape as `derive_muted_foreground()` — computed in `gen_variants.py` at render time, not hand-baked into `variants.json`. Yellow is `Δh=0` by construction → byte-identical, trivially, no regression risk.

**Why this clears PT-92 structurally:** pairwise (≥60°) and role-collision (≥25°) are hue-*difference* tests; a uniform rotation preserves every pairwise delta exactly, so those two guards pass in all 24 variants by construction — no per-variant hand-tuning.

**What does NOT come free — mechanical re-check per variant×mode, not hand-verified:**
1. PT-83 contrast floors (≥2:1 vs both `--card`s) — sRGB luminance isn't hue-invariant at fixed OKLCH L/C; some rotations may fail.
2. In-gamut check — `-role-3`/`-role-4` sit at C=.24/.237, near sRGB's edge; `_oklch_to_linear_rgb` silently clamps out-of-gamut today. Clamping must be a hard fail, never silent, at any rotated hue.
3. CVD (deutan) resim — original hues were placed partly for CVD safety; that's not rotation-invariant (confusion depends on absolute hue region, not just relative spacing). Re-simulate per variant; rescue via the existing L/C-nudge-hue-fixed pattern (PT-92 precedent), never re-placing hue.
4. Cross-dimension: role hues now vary with Chart Color while `--primary` varies with Theme — the role-vs-`--primary` ΔE_OK check was a single fixed pair before, now needs Theme(7)×Chart(24)×mode(2) coverage. Flagging as a real test-cost/architecture point, not absorbing it silently — loop in architect on budget.

**Fallback for any variant×mode that fails #1–3 after rotation:** nudge that token's L/C only (hue fixed) to clear the floor, recorded per-variant like the existing `--chart-flow-closed` light-mode nudge — never re-derive hue placement.

**Next:** I'll land the spec section (`design-system-spec.md` → Chart-local ramp) once the guard sweep's shape is settled; implementation-lead adds `derive_chart_family_hue_rotation()` to `gen_variants.py`; qa extends the variant-sweep test to all 24×2 for guards 1–4 above.
