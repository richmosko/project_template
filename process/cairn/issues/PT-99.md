---
id: PT-99
title: Gate guard: the verdict's serial-discover leg can't be issued by a teammate — stock unittest has no --gate
status: done
milestone: PT-0.12.1
parent: null
blocked_by: [PT-97]
assignee: architect
labels: [cairn, tests, hooks]
priority: P3
pr: https://github.com/richmosko/project_template/pull/182
created: 2026-09-07
updated: 2026-09-07
---


## Acceptance criteria

1. No backtick code span in ratified text — `process/WORKFLOW.md`, `process/TRACKER.md`,
   `.claude/roles/*.md`, `.claude/agents/*.md`, `.claude/skills/*/SKILL.md` — contains the
   token `unittest`. A test guards it.
2. WORKFLOW's tiered-gating bullet names the gate run as
   `run_tests.py --gate <red|green|verdict|finish>`, and names the verdict's
   counts-agreement leg as `run_tests.py --serial --gate verdict`.
3. `.claude/skills/finish-feature/SKILL.md` no longer cites serial discover as the
   counts reference, and its gate run declares `--gate finish`.
4. `.claude/hooks/test_run_guard.py`, its refusal message, and
   `scripts/cairn/tests/test_test_run_hooks.py` are unchanged: a teammate's bare discover
   is still refused with exit 2.
5. Full suite green at build-green (`run_tests.py --gate green`).

## Comments

### @team-lead — 2026-09-07

Found by the architect during the PT-90 gate-4 verdict (PT-90.md @ 390c07e): `.claude/hooks/test_run_guard.py` refuses a bare `python3 -m unittest discover -s tests` from any teammate, and stock unittest has no `--gate` flag, so the verdict's third leg — PT-93 AC1's "serial `discover` still works and agrees" — cannot be issued as a command by the architect at all. Today only the human/lead lane can run it. Options for the ruling: accept an env marker (e.g. `CAIRN_GATE=verdict`) on a bare discover, recorded by the PostToolUse hook as runner=unittest; or drop the discover leg from verdicts in favour of `run_tests.py --serial --gate verdict` and amend WORKFLOW. Measure which; nothing else changes.

### @team-lead — 2026-09-07

Feature started. Branch: `feature/pt-99-gate-guard-discover-leg`.

### @architect — 2026-09-07

**Gate 1 ruling — option 2 (Mosko, 2026-09-07): the bare-discover leg is retired. The hooks do not change.** ACs added to the body above.

**Measured** @1ae3a94, every command run this turn:
- Ratified-text scan — 33 files (WORKFLOW, TRACKER, `.claude/roles/*.md`, `.claude/agents/*.md`, `.claude/skills/*/SKILL.md`), 3056 backtick code spans, every file yielding ≥1 span. Spans containing the token `unittest`: **exactly 2** — `process/WORKFLOW.md:100` (``serial `unittest discover` ``) and `.claude/skills/finish-feature/SKILL.md:21` (same phrase, inside the pre-flight ```bash fence). **No agent definition and no role file names it**, so `.claude/agents/` is untouched by this feature.
- Guard, real hook + real payload, `agent_type: architect`: bare `python3 -m unittest discover -s tests` → **2**; the same `--gate verdict` → **0**; `python3 run_tests.py --serial --gate verdict` → **0**; `python3 run_tests.py --serial` → **2**.
- Stock unittest with the flag: `python3 -m unittest discover -s tests --gate verdict` → exit 2, `error: unrecognized arguments: --gate`. The discover+`--gate` form clears the guard and then dies in argparse before one test runs — loud, never a false green — so no new refusal is needed for it.
- `run_tests.parse_args(["--serial","--gate","verdict"])` → `jobs=1, gate='verdict', pattern=['test_*.py']`; `--gate` conflicts only with an explicit `-p`.
- **Second defect in the same text:** WORKFLOW prescribes the gate run as a bare `python3 scripts/cairn/run_tests.py`, which the guard **refuses** for a teammate (row 4). Fixed below.

**Refusal message: unchanged.** It already names the only working form (`run_tests.py --gate <…>`); adding `--serial` would read as the default gate run, which it is not. `.claude/hooks/*` and `scripts/cairn/tests/test_test_run_hooks.py` stay as they are.

**Replacement text — implementation-lead, exact.** `process/WORKFLOW.md`, replacing lines 99–102 (`` `python3 scripts/cairn/run_tests.py` — a file-level `` … `doubt-the-parallel-path control; a doc-only edit needs no run at all.`):

>     `python3 scripts/cairn/run_tests.py --gate <red|green|verdict|finish>` — a file-level
>     parallel runner, default 8 workers, ≤ 45 s on the reference machine
>     (`process/reviews/PT-93/timings.md`); `--gate` is what the PreToolUse guard requires of
>     a teammate and what records the run. The verdict's counts-agreement leg is
>     `python3 scripts/cairn/run_tests.py --serial --gate verdict` — the same files one at a
>     time, a doubt-the-parallel-path control. A bare unittest discover run is no longer a
>     gate leg: stock unittest has no `--gate` flag, so a teammate cannot issue one (PT-99).
>     A doc-only edit needs no run at all.

Leaving "unittest discover" out of backticks there is deliberate: AC1 scans code spans, i.e. commands the doc hands you, and prose naming the retired form is in scope for nobody. Add no sentence about the human/lead lane — it is unchanged, and a code span for it would fail AC1.

`.claude/skills/finish-feature/SKILL.md`: L21 ``as serial `unittest discover` — process/…`` → ``as `--serial` — process/…``; L26 `( cd scripts/cairn && python3 run_tests.py )` → `( cd scripts/cairn && python3 run_tests.py --gate finish )`.

**Judgment call, strike it if you disagree:** `--gate finish` is beyond "drop the discover leg". It costs one flag, changes no behaviour but the recorded gate, and makes the skill's own "PT-94 C9: gate owner only, once" comment true in `test-runs.jsonl`.

**Tests — qa, one new file `scripts/cairn/tests/test_gate_leg_naming.py`, 2 tests**, extraction in the `test_skill_id_literals.py` style: read as text, pull backtick spans, raise a named `ExtractionError` on a file with zero spans rather than returning an empty list. A new file rather than an append: `test_test_run_hooks.py`'s subject is the hooks' runtime behaviour; this is a source-text scan over docs.
1. `test_no_ratified_code_span_names_unittest` — the 33-file set above; fail listing file + span for any span matching `\bunittest\b`. Red mutation: none needed to start — it is **red at HEAD**, 2 hits, measured above; after the fix, restoring either `` `unittest discover` `` turns it red again.
2. `test_extraction_raises_when_a_file_has_no_code_spans` — a spanless temp file; assert `ExtractionError`. Red mutation: make the extractor `return []` instead of raising.

**One writer per file.** qa: the new test file. implementation-lead: `process/WORKFLOW.md`, `.claude/skills/finish-feature/SKILL.md`. architect: this issue file. Nobody: `.claude/hooks/`, `.claude/agents/`, `process/cairn/metrics/`. Full runs go through `run_tests.py --gate <g>`; commit by pathspec.

### @architect — 2026-09-07

**Gate 4 verdict @487496d — PASS, no deltas.** Ruling @eab939a; tests red @28f597c; build @487496d.

| Axis | Result | Evidence |
|---|---|---|
| AC1 — no ratified code span names `unittest` | **PASS** | `test_gate_leg_naming.py` 2/2 OK @487496d. Its `RATIFIED_FILES` resolves to **33** files, none missing, none yielding zero spans — the scan is the set the ruling measured, not a subset |
| AC1 red mutation fires | **PASS** | isolated worktree at 487496d: baseline **2/2 OK**; restoring ``counts identical to serial `unittest discover` `` into WORKFLOW L100 → **FAILED (failures=1)**. Both branches decisive; worktree removed, main tree carries only the hooks' own metrics writes |
| Extraction non-vacuity | **PASS** | `extract_code_spans("No backticks anywhere.")` raises `ExtractionError`; the named mutation (`return []`) returns without raising, so the `assertRaises` fails |
| AC2 — the amended WORKFLOW gate-run text is a command the guard accepts from a teammate | **PASS** | real hook, real payload, `agent_type: architect`, `CLAUDE_PROJECT_DIR` = repo root: `--gate red` / `green` / `verdict` / `finish` → **0**, `--serial --gate verdict` → **0**, mid-loop `-p "test_<area>*.py"` → **0**. **Control:** the pre-fix bare `python3 scripts/cairn/run_tests.py` → **2** |
| AC3 — the skill's pre-flight declares its gate | **PASS** | `git show 487496d:.claude/skills/finish-feature/SKILL.md` — L26 is `( cd scripts/cairn && python3 run_tests.py --gate finish )`, L21 cites `--serial` in place of the retired form; that exact L26 line → guard **0** |
| AC4 — hooks and their tests unchanged | **PASS** | `git show --stat 487496d` touches exactly 2 files; `.claude/hooks/` and `scripts/cairn/tests/test_test_run_hooks.py` appear in neither that commit nor `git diff --stat main...HEAD` |
| AC5 — full suite green, counts agree | **PASS** | `run_tests.py --gate verdict` → **1454 / skipped=1 / OK**, 19.84 s (86 files, 8 workers); `run_tests.py --serial --gate verdict` → **1454 / skipped=1 / OK**, 106.21 s (1 worker). One run per leg; no bare discover leg exists any more. WORKFLOW's "≤ 45 s" claim holds at 19.84 s |
| Nothing outside the two files changed | **PASS** | branch diff is 5 files: the 2 gate-3 files, qa's new test, this issue file, and `process/STATE.md` — STATE from 1ae3a94, the lead's feature-start commit, not gate 3 |
| `cairn check` | **PASS** | `ok`; the amended WORKFLOW paragraph trips neither the D13 phrase lint nor the 8-sentence cap |

**Deltas: none.**

**Construction note, recorded because the same table without a control would have read as a total-refusal finding against gate 3.** My first guard sweep returned `2` on every row *including* the `-p` control that must return 0. The session's working directory had moved to `scripts/cairn` between turns, so the subprocess was Python failing to open a now-relative hook path — the guard never ran. The AC2 row above is the re-run from an absolute repo root, control included.

### @team-lead — 2026-09-07

PR opened: https://github.com/richmosko/project_template/pull/182. Awaiting Validate. Lead re-verified @487496d: scan + hooks modules 27/27; `run_tests.py --gate finish` 1454 / skipped=1 in 19.8 s; JS 458; cairn check ok; every scorecard cap held.

### @qa-engineer — 2026-09-07

Validate: PR #182 diff matches verified sha 487496d byte-for-byte for WORKFLOW.md, finish-feature/SKILL.md, test_gate_leg_naming.py. Re-ran `run_tests.py -p test_gate_leg_naming.py` once: 2/2 green. Approve-merge.

### @team-lead — 2026-09-07

Validate passed; merging via PR #182. Closing.
