---
id: PT-75
title: STATE.md Releases table becomes a one-row pointer, machine-bounded at 200 characters
status: in-progress
milestone: PT-0.10
parent: null
blocked_by: []
assignee: team-lead
labels: []
priority: null
pr: null
created: 2026-09-02
updated: 2026-09-02
---


`process/STATE.md`'s header promises "**No history accumulates here**". Its `## Releases`
section contradicts that: it is an append-only log, one row per tagged release, and the rows
have grown into full release notes. Measured 2026-09-02 on `5b93de9`:

| Measurement | Command | Result |
|---|---|---|
| Whole file | `wc -c process/STATE.md` | **10145 bytes** |
| `## Releases` heading → EOF | `awk '/^## Releases/{f=1} f' process/STATE.md \| wc -c` | **8489 bytes — 83.7% of the file** |
| Data rows only | `grep '^\| v' process/STATE.md \| wc -c` | **7768 bytes across 11 rows** (v0.3.0 → v0.9.0), mean 706 chars/row |
| Longest / shortest row | `grep '^\| v' … \| awk '{print length}' \| sort -n` | **1223** (v0.7.0) / **223** (v0.4.0) |

Every release adds ~700–1200 bytes, unbounded. The content is duplicated across GitHub Releases
(canonical per the 2026-05-25 ruling in `process/TEMPLATE_DECISIONS.md`), the archived milestone
files, the issue files, and git tags.

## Ruling (team-lead, 2026-09-02)

The Releases table becomes a **pointer, not a log**:

1. It holds exactly **one** row: the latest tagged release. `/merge-pr` **replaces** that row
   instead of appending.
2. Hard limit: the entire markdown table row — pipes, link URL, everything — is **at most 200
   characters**. Machine-checked, not a style note. (Note: the *shortest* row on disk today is
   223 chars, so the retained v0.9.0 row must be **rewritten** to fit, not merely kept.)
3. The section keeps a one-sentence lead-in linking to the GitHub Releases page for full history.
   Breaking-change / migration notes for downstream instances live in the GitHub release notes
   and `process/TRACKER.md` — never in this row.
4. `process/WORKFLOW.md` and `.claude/skills/merge-pr/SKILL.md` are updated to match:
   replace-not-append, the 200-char rule, and a **fixed row template**. Every other surface that
   says "append a row" is corrected.
5. The existing 11-row table collapses to the single v0.9.0 row. Removed rows are not lost —
   see the recoverability criterion below, which supersedes the ruling's "already exist verbatim
   on GitHub" assumption (it is false; see @architect comment).
6. A decision entry is written to `process/TEMPLATE_DECISIONS.md` (this repo *is* the template,
   so that is the right ledger), superseding the "Releases table kept" clause of the 2026-08-19
   and 2026-08-22 rulings.
7. Enforcement mechanism: architect's call — see the @architect comment below. **Chosen: a new
   test in `scripts/cairn/tests/`**, which `/finish-feature` step 2 already runs as a hard,
   `exit 1` gate.

## Acceptance criteria

**Shape of the section**

- [x] `process/STATE.md` → `## Releases` contains exactly **one** data row (the latest tagged
      release) after the change. Verified: `grep -c '^| v' process/STATE.md` returns `1`.
- [x] That row is **≤ 200 characters** including both outer pipes and the full link URL.
      Verified: `grep '^| v' process/STATE.md | awk '{print length}'`.
- [x] The row is rewritten (not truncated mid-word) to fit: version, date, major line, milestone
      shipped, and a link to the published GitHub release. Prose release notes do not appear.
- [x] The section retains a **one-sentence** lead-in that links to
      `https://github.com/richmosko/project_template/releases` as the full history.
- [x] No breaking-change or migration prose is left in the section.

**Recoverability of the removed rows** (supersedes ruling §5's "verbatim" premise)

- [x] Confirmed before deletion that every removed version has a published GitHub Release:
      `gh release list` — v0.1.0…v0.9.0 all present, so GitHub is a strict superset of the
      table's version coverage (the table starts at v0.3.0).
- [x] For each removed row, its **synthesis prose** (issue counts, PR ranges, per-issue summary)
      is either (a) already present in that release's body, or (b) ported into it via
      `gh release edit vX.Y.Z --notes-file -` before the row is deleted. Spot-checked v0.9.0
      shows GitHub's body is the auto-generated PR list only — the synthesis is **not** there
      today, so (b) applies to at least v0.9.0 and must be checked per-version.
- [x] The commit message names `git show 5b93de9:process/STATE.md` as the permanent backstop
      copy of the pre-collapse table.

**Surfaces corrected — "no surface still says append"**

- [x] `process/WORKFLOW.md:268` ("ensure a Releases row exists") reads as replace-the-row.
- [x] `process/WORKFLOW.md:800` ("Append a row to `STATE.md` → Releases") reads as replace,
      states the 200-char limit, and shows the fixed row template.
- [x] `.claude/skills/merge-pr/SKILL.md:95` ("**d. Add a row to `## Releases`**") reads as
      replace, states the 200-char limit, and shows the same fixed row template — one template,
      quoted identically in both places.
- [x] `process/TRACKER.md` — the two Releases mentions (§Relationship to STATE.md ~L377 and
      §Board release-state ~L487) are checked and corrected if they imply a growing log.
- [x] `README.md:155` and `README.md:165` — the session-startup narrative says the `awk`
      extractor exists "so the auto-loaded state slice stays compact **even as the Releases
      table grows**". The table no longer grows; the sentence and the Mermaid node label are
      updated to say what is actually true.
- [x] `CLAUDE.md` and `.claude/roles/team-lead.md` checked; corrected if either instructs an
      append (grep found no Releases-append instruction in either as of `5b93de9` — record the
      negative result rather than skipping the check).
- [x] Final sweep: `grep -rn -i "append.*release\|add a row" --include='*.md' .` (excluding
      `process/cairn/archive/`) returns no instruction to grow this table.

**Ledger**

- [x] `process/TEMPLATE_DECISIONS.md` carries a dated entry recording this decision, explicitly
      superseding the "Releases table kept" clause of the 2026-08-19 and 2026-08-22 entries, and
      recording the corrected motivation (duplication + full-read cost — **not** startup-context
      cost; see @architect comment).

**Enforcement**

- [x] A new test file `scripts/cairn/tests/test_state_releases_bound.py` exists and asserts:
      (i) exactly one data row in `## Releases`; (ii) every data row ≤ 200 chars; (iii) the
      lead-in links to the GitHub Releases page; (iv) a **named extraction error** if the
      section or its table cannot be located — never a vacuously-green pass. Same loud-failure
      contract as `test_skill_id_literals.py` (PT-45) and `test_column_parity.py` (PT-36).
- [x] The test is red against the pre-change `process/STATE.md` and green after — demonstrated,
      not asserted.
- [x] The full gate is green: `( cd scripts/cairn && python3 -m unittest discover -s tests )`
      and `node --test scripts/cairn/tests/js/*.test.js`.
- [x] `scripts/cairn/cairn check` prints `ok`.

## Comments

### @architect — 2026-09-02

**Enforcement mechanism — chosen: a Python test in `scripts/cairn/tests/`.**

`scripts/cairn/tests/test_state_releases_bound.py`, reading `process/STATE.md` as text.
Rationale, in the order that decided it:

- **It actually fails a merge.** `/finish-feature` step 2 runs
  `( cd scripts/cairn && python3 -m unittest discover -s tests )` with
  `|| { echo "GATE FAIL…"; exit 1; }`. That is the repo's only hard, machine-enforced gate today
  — there is no CI (`git ls-files` shows no `.github/`), so a GitHub Actions check would be new
  infrastructure and a new branch-protection dependency for the same predicate.
- **Zero new infrastructure.** No new runner, no new file convention, and it rides the suite QA
  already runs at every gate.
- **Precedent exists for asserting outside `scripts/cairn/`.** The suite is nominally
  spin-off-scoped, but `test_skill_id_literals.py` (PT-45) already reads
  `.claude/skills/setup-tracker/SKILL.md` via `helpers.CAIRN_DIR.parent.parent`, and
  `test_column_parity.py` / `test_dashboard.py` reach outside too. This adds no new boundary
  crossing — but **it does add one more file a `git subtree split --prefix=scripts/cairn` would
  drag out**, so follow PT-45's shape: an explicit `SETUP_TRACKER_SKILL.exists()`-style
  precondition assert naming the expected path, so a spun-off repo fails with a legible message
  rather than a stack trace.

**Rejected:** a `cairn check` rule (STATE.md is not tracker data; `cairn check` lints
`process/cairn/` and widening it would make the tracker lint own a process doc); a prose step in
`/merge-pr` alone (the lead asked for something that *fails*, and a skill instruction is
agent-obedience, not a gate); a standalone `scripts/check-state.py` (a second, unhooked runner —
the existing suite is already invoked by the gate).

### @architect — 2026-09-02

**Two premises in the ruling need correcting before implementation. Neither changes the ruling;
both change its stated justification, so the TEMPLATE_DECISIONS entry should not repeat them.**

**1. The Releases table is NOT in the session-startup context.** The ruling's motivation — "every
release adds roughly 1KB to every future session's startup context, unbounded" — is false. The
`SessionStart` hook in `.claude/settings.json:44` is
`awk '/^## Releases/{exit} {print}' process/STATE.md` — it exits *at* the `## Releases` heading,
so the table has never been injected. `README.md:165` documents this ("top → just before
`## Releases`"). Confirmed empirically: this session's injected state block ends after
`## Active Feature`.

The ruling still stands on its own, on two grounds that *are* true:
- **The header lies.** STATE.md says "No history accumulates here" while carrying an append-only
  log. A doc that contradicts itself teaches agents to distrust it.
- **The full-read cost is real and paid often.** `CLAUDE.md` → *Resume runbook* step 1 says
  "**Read the full `process/STATE.md`**" on every cold resume. That read is 10145 bytes today, of
  which 8489 (83.7%) is duplicated release history — and the resume path is exactly where a lead
  is trying to orient quickly.

**2. The removed rows do NOT already exist verbatim on GitHub Releases.** Spot-checked v0.9.0:
`gh release view v0.9.0 --json body` returns the `--generate-notes` PR list plus a Full Changelog
link. The STATE.md row's synthesis — issue count, PR range, per-issue one-liners, the "Board
renamed Issue Tracking; standalone `/board`+`/list` survive" note — appears nowhere in it. That
synthesis is genuine, non-duplicated signal.

It is not *unrecoverable* (git holds the table forever at `5b93de9`, and the facts are
reconstructible from the PR list + issue files), but deleting it on the stated belief that GitHub
already has it would be deleting on a false premise. Hence the recoverability criteria above:
verify per-version, and port the synthesis into the release body where it is missing, before the
row goes. Flagging rather than deciding unilaterally — if the lead prefers to accept the loss and
cite `git show 5b93de9:process/STATE.md` as the record, that is a legitimate call and cheaper;
say so and I will trim those criteria.

### @team-lead — 2026-09-02

Feature started. Branch: `feature/pt-75-state-releases-pointer`. Delivery pre-authorized by Mosko (2026-09-02): merge PT-0.10 issues, then release v0.10.0.

### @architect — 2026-09-03

**Architectural review — APPROVED at `8231cc6`.** Verified against commit objects and a
private `git worktree`, never the shared checkout (team-lead directive, 2026-09-02).

## Gating ruling, and how it resolved

Two defects were found in `test_state_releases_bound.py` at `7e0e61d` — both in the guard,
neither in the doc changes, and both invisible to a green suite because *the gate was not
looking*. QA's independent green run at that sha was correct and not evidence against either.

**1. `RELEASES_URL` was pinned to `https://github.com/richmosko/project_template/releases`.**
This repo is the template: downstream instances inherit the file under `/finish-feature`'s
hard `exit 1` gate and point at their own releases page. Reproduced by substituting a
synthetic downstream owner/repo into `process/STATE.md` — `FAILED (failures=1)`. This is
PT-45's defect verbatim ("every repo instantiated since v0.6.1 started lint-red on day one"),
recurring in a file whose own docstring cites PT-45 as its model.

**2. `DATA_ROW_RE` used the leading `v` as the row *selector*.** A property of a valid row
cannot also be the predicate deciding whether the row is inspected. Reproduced by appending
`| 0.10.0 | … |` at 320 characters — 60% over the hard cap — directly below the v0.9.0 row:
the suite reported `OK`.

**Both fixed at `47e8b07`** (owner/repo derived from the lead-in and cross-checked against the
data row's release link; structural row extraction by table position with the `v` shape
demoted to a separate assertion), **and re-verified at `8231cc6` by re-running both
constructions rather than accepting the report:** downstream substitution → 5/5 OK (was 1
failure); 320-char non-`v` row → FAILED, 3 assertions (was a clean pass).

## Scope correction on the prose swap — 5 sites, not 7

The seven non-STATE/non-test occurrences were WORKFLOW.md ×3, merge-pr/SKILL.md ×2 and
**CLAUDE.md ×2**. The CLAUDE.md pair must stay literal and was correctly left untouched:
`CLAUDE.md:98` points at the **template's own** Releases page so a fresh project can read
which template version it bootstrapped from — a downstream repo needs `richmosko/project_template`
there, not its own empty releases page. The general rule, since the two are indistinguishable
to a grep and opposite in meaning: **a URL naming the repo the file lives in takes the
placeholder; a URL naming the upstream template stays literal.**

## Non-gating findings — recorded here, not filed (team-lead ruling, 2026-09-02)

No new issue, no v0.10.0 scope change; revisit only if either bites.

1. **A second table inside `## Releases` is invisible.** `extract_table_lines` takes only the
   first contiguous run of `|`-lines, so anything after it in the section is unchecked. A
   second `| Version | Date |` table carrying a 320-char row pointing at a different repo
   passes clean. Same class as defect 2 (selector scope), far lower likelihood — appending a
   row to the *existing* table is the realistic drift, and that is now caught. Hardening, if
   ever wanted: assert the section contains exactly one contiguous table run.
2. **`extract_github_owner_repo` takes the first GitHub Releases URL in the lead-in.** A
   lead-in naming the upstream template before its own releases page fails loudly (correct
   posture — it refuses to guess) but with a message reading "row doesn't match lead-in" when
   the real cause is "two candidate URLs." Either assert exactly one such URL with a message
   naming that, or document the constraint in the docstring.

## Verified at `8231cc6`

- Literal-URL census by commit object: WORKFLOW.md 0, merge-pr/SKILL.md 0, STATE.md 2
  (correct — this repo's real state, and the derived test needs its lead-in and row to agree),
  CLAUDE.md 2 (correct — upstream template).
- The two row templates are byte-identical across WORKFLOW.md and merge-pr/SKILL.md, string
  comparison not eye; the `Draft\|Published` backslash is gone from both.
- `Draft|Published` marker **accepted**: a draft release is by construction the newest one, so
  a one-row table carries exactly the case TRACKER.md → Board release-state is about. Dropping
  it would have made that line false. Template skeleton is 144 chars, leaving 56 for the
  milestone name (longest historical name yields 190).
- Recoverability verified as a **census, not a sample**: all 11 pre-collapse rows extracted
  from `5b93de9` and diffed against the live GitHub release bodies. Every row's synthesis is
  present — nine now carry a `## Summary` with the row prose, and v0.6.0 / v0.7.1 already had
  richer curated bodies. Matches the TEMPLATE_DECISIONS entry's "9 of 11" exactly.
- `TEMPLATE_DECISIONS.md` carries the corrected motivation (header self-contradiction +
  full-read cost) and explicitly marks the session-startup-injection premise as false and not
  carried forward.
- Full gate in the worktree at this sha: **1066 Python (15 skipped) / 384 JS / `cairn check` ok.**
