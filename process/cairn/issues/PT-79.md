---
id: PT-79
title: Dashboard block: token usage and estimated cost per issue, stacked by role, input vs output
status: done
milestone: PT-0.11
parent: null
blocked_by: [PT-77, PT-78]
assignee: architect
labels: [dashboard, cairn]
priority: null
pr: https://github.com/richmosko/project_template/pull/158
created: 2026-09-03
updated: 2026-09-03
---


## Problem

Once PT-77 and PT-78 populate the shared data file, nothing shows it.

## Acceptance criteria

1. The board serves the data file on an API path. The dashboard gets a new block beside the
   PT-61 chart: one bar per issue (plus `main`), stacked by role.
2. Toggle between **tokens** and **estimated cost**. In tokens view, input and output are
   separate series; cache read and cache write are distinguishable from fresh input.
3. Cost uses a dated, checked-in price table per model (input, cache write, cache read, output,
   $/MTok). The block is labelled "estimated"; tokens are not.
4. The issue drawer shows the issue's totals with the role split.
5. Existing dashboard tests extend to the block; a browser-visibility check per the PT-32/PT-57
   protocol, not property reads.
6. `process/TRACKER.md` documents the price table and where to update it.

## Comments

### @team-lead — 2026-09-03

Feature started. Branch: `feature/pt-79-token-dashboard`. Blockers PT-77 and PT-78 are merged (7cf42b9, 44f8b0c). Architect's gating ruling (drafted in temp/ during PT-78) lands as the next comment; lead's bar-order call: sort by the displayed metric in each toggle state.

### @architect — 2026-09-03

**Gating ruling — PT-79 token/cost dashboard block.** Every number below was computed against the committed `process/cairn/metrics/token-usage.jsonl` (262 lines, 69 buckets, 5.75 B tokens) at the time of writing, and the rates were read off the published pricing page, not recalled.

---

## 1. The price table

**File: `scripts/cairn/prices.json`.** Beside the engine, **not** under `process/cairn/`. — the rates are identical for every project that ever uses cairn; they are engine data, not this project's state. `git subtree split --prefix=scripts/cairn` must carry the table with the engine, and a spun-off cairn must not drag this repo's usage history along with it.

**Format** — flat, dated, explicit per-model rates in $/MTok:

```json
{
  "source": "https://platform.claude.com/docs/en/about-claude/pricing",
  "retrieved": "2026-09-04",
  "currency": "USD",
  "unit": "per_mtok",
  "models": {
    "claude-fable-5-1":          {"input": 10,  "cache_write_5m": 12.50, "cache_write_1h": 20, "cache_read": 0.25, "output": 50},
    "claude-fable-5":            {"input": 10,  "cache_write_5m": 12.50, "cache_write_1h": 20, "cache_read": 1.00, "output": 50},
    "claude-opus-5":             {"input": 5,   "cache_write_5m": 6.25,  "cache_write_1h": 10, "cache_read": 0.50, "output": 25},
    "claude-opus-4-8":           {"input": 5,   "cache_write_5m": 6.25,  "cache_write_1h": 10, "cache_read": 0.50, "output": 25},
    "claude-sonnet-5":           {"input": 2,   "cache_write_5m": 2.50,  "cache_write_1h": 4,  "cache_read": 0.20, "output": 10},
    "claude-haiku-4-5-20251001": {"input": 1,   "cache_write_5m": 1.25,  "cache_write_1h": 2,  "cache_read": 0.10, "output": 5}
  }
}
```

Those six are exactly the models present in the committed data — verified, zero unpriced buckets.

**Store explicit rates per model, never multipliers.** The docs express caching as multipliers (5m = 1.25×, 1h = 2×, read = 0.1× base input) and that is *wrong for one of our models*: **Claude Fable 5.1's cache read is 0.025×, not 0.1×** — $0.25/MTok against Fable 5's $1.00/MTok on the same $10 base. A multiplier-based table overcharges `claude-fable-5-1` cache reads by 4×. Rates are data; derived formulas silently encode an exception you did not know about.

**Dating: one flat snapshot with `retrieved`, not a list of effective periods.** — proportionate to a three-week window in which no listed rate changed (Sonnet 5's $2/$10 introductory rate became permanent on 2026-09-01 at the same numbers, so nothing in range moves). **Cost:** if a rate ever changes, updating the table silently re-prices *history*. **Reversal:** promote each model's value to a list of `{effective_from, rates}` and pick by the line's `window_end`; the consumer changes in one place. Say so in TRACKER so the next person updating rates knows what they are doing to the past.

**An unknown model is never priced at zero.** Its cost is `null`, the bucket still renders its **tokens**, and the block names the unpriced models in its caption. — a missing rate must look like missing information, not like free work. This is the guard that catches the next model rename, which is certain: the data already contains one dated id (`claude-haiku-4-5-20251001`) and five undated ones.

**Documented assumption:** rates are standard-speed, global-inference list prices. Fast mode (Opus 5/4.8 at $10/$50) and the `inference_geo: "us"` 1.1× multiplier are not modelled — the backfill did not persist `speed` or `inference_geo`, and every transcript record observed carried `speed: standard` / `inference_geo: not_available`. Batch and volume discounts are not modelled either. This is why the chart says *estimated*.

## 2. The API

**`GET /api/tokens`, a separate endpoint.** — PT-56's roster and PT-61's flow both set this precedent: a different cost profile and a different refresh cadence do not belong bolted onto `/api/dashboard`. Memoize in-process keyed by the data file's `(mtime, size)`; the file changes rarely and a full re-read per request is waste.

**Payload:**

```json
{
  "issues": [
    {"issue": "PT-69", "total": {"input": 0, "cache_write": 0, "cache_read": 0, "output": 0, "cost_usd": 226.57},
     "roles": [{"role": "implementation-lead", "input": 0, "cache_write": 0, "cache_read": 0, "output": 0, "cost_usd": 0.0}]}
  ],
  "window_start": "2026-08-18", "window_end": "2026-09-04",
  "generated": "2026-09-04T02:18:03Z",
  "sources": ["transcript-backfill"],
  "prices": {"retrieved": "2026-09-04", "source": "<url>", "unpriced_models": []},
  "warning": null
}
```

- Aggregated to **issue × role**; model detail is folded into `cost_usd` server-side and does not cross the wire. — AC2 needs the four counters and AC4 needs the role split; nothing on the page needs a per-model breakdown, and computing cost server-side keeps the price table on one side of the boundary.
- `cost_usd` is `null` — not `0` — when any contributing model is unpriced.
- `window_start`, `window_end`, `sources` and `prices` come from the data, never hardcoded, so the caption in §5 cannot go stale.
- A missing or unreadable data file is `warning: "no token data"` with `issues: []`, HTTP 200. — the dashboard degrades to an empty block; it does not 500 because a metrics file has not been written yet. (This is the one place the "missing means error" rule does *not* apply: `token-usage.jsonl` is optional data, unlike `config.yml` which defines the tracker.)

## 3. Block placement

`scripts/cairn/dashboard/src/lib/components/TokenCostChart.svelte`, placed **directly below** the PT-61 `IssueFlowChart` in `App.svelte`, in a full-width `Card.Root` matching the flow chart's own `sm:col-span-2 lg:col-span-4` span. **Lazy-loaded by the same dynamic-import pattern `IssueFlowChart` already uses** — the chart library is already kept out of the main bundle deliberately; a second eager chart import would undo that.

## 4. What the block shows

- **Bars: top 12 issues by the *currently displayed* metric, plus `main`, ordered descending by that same metric.** A "Show all" control expands to all 69. The caption names the basis: `showing the top 12 of 68 issues by estimated cost` (or `by tokens`).
  - **Selection follows the metric, not just the order** (team-lead ruled order; selection is the consequence). Measured on the committed data: the top 12 by tokens and the top 12 by cost **share only 8 members — 4 swap out each way** (`PT-3`, `PT-22`, `PT-26`, `PT-58` leave; `PT-32`, `PT-54`, `PT-59`, `PT-73` enter). If selection were pinned to tokens, the **cost** view would silently omit four of the twelve most expensive issues while calling itself a cost chart. A changing bar set is surprising; a cost chart that hides top costs is wrong, and wrong beats surprising here. The caption naming the basis is what makes the change legible rather than glitchy.
  - **Departure from AC1's literal "one bar per issue".** **Cost:** the default view hides 56 buckets. **Reversal:** change one constant. **Why:** there are 68 issue buckets plus `main` today and the number only grows; 69 bars in a dashboard card is not a chart, it is a texture. The top 12 carry the story and the toggle keeps AC1 satisfiable on demand.
- **`main` gets its own bar** — kept, not spread, not hidden. It is **8.4% of all tokens ($528.72, the single largest bucket)**, so dropping it would make the bars visibly fail to sum to the stated total, which is exactly the kind of quiet inconsistency that makes people stop trusting a chart. Render it last, visually muted, with **no drawer link** — it is not an issue and must not look clickable.
- **Stacked by role** within each bar.
- **Toggle: tokens ↔ estimated cost.** In **tokens** view the series are `input`, `cache_write`, `cache_read`, `output` — four distinguishable series, satisfying AC2's "input and output separate" and "cache read and cache write distinguishable from fresh input" in one encoding rather than two. In **cost** view a single `cost_usd` per role. — the role stack and the token-type stack cannot both occupy the same axis; role is the stack (AC1) and token type is the toggle's tokens mode.

## 5. Caption — both caveats, derived from data

One caption line under the block, values interpolated from the payload:

> Tokens are exact. Dollars are estimated from published list prices retrieved {prices.retrieved} — standard speed, global inference, no discounts. History begins {window_start}; earlier work predates the local transcript retention window and was never recorded.

**Only the dollar view is labelled "estimated"; the token view is not** (AC3). If `prices.unpriced_models` is non-empty, append: `{n} model(s) have no published rate and are excluded from cost: {names}`.

## 6. Drawer totals (AC4) — a different codebase, flag it

**The issue drawer is `scripts/cairn/board/board.js`, not the Svelte app.** AC4 therefore lands in the board bundle with `board-logic.js`'s pure-function split and the PT-22 harness, while AC1–3 land in `dashboard/src/`. Two files, two test paths, one PR. — worth stating because "add it to the drawer" reads like a dashboard task and is not one.

Drawer shows the issue's four token counters, its `cost_usd`, and the per-role split. Fetched from the same `/api/tokens` payload, filtered client-side by issue id — no second endpoint, following PT-51's "no `GET /api/milestone/<id>`" precedent.

## 7. Build and the freshness gate

- `cd scripts/cairn/dashboard && npm run build`, then **commit `dist/`**. `check_dist_freshness.py` treats the whole `scripts/cairn/dashboard/` subtree except `dist/` as source, so a `.svelte` change with a stale `dist/` fails `/finish-feature`.
- **`scripts/cairn/board/` is outside that subtree** — the AC4 drawer change needs no build and is not covered by the gate. Do not expect the gate to catch a board-only regression.

## 8. Tests

1. `/api/tokens` payload shape, aggregation correctness, and `cost_usd: null` for an unpriced model.
2. A price-table test asserting **Fable 5.1's cache read is 0.25 and Fable 5's is 1.00** — the specific asymmetry a multiplier refactor would destroy.
3. A regression pinning the **1h/5m split**: pricing all `cache_write` at the 5m rate understates the current corpus by **$264.08 (9%)**. That number will drift; assert the *inequality* and the direction, not the value.
4. Missing data file → empty block plus warning, never a 500.
5. `main` renders as a bar and is **not** drawer-clickable.
6. Browser-visibility check per the PT-32/PT-57 protocol — `elementFromPoint`/screenshot, **not** property reads — for the block, the toggle in both states, and the caption. PT-76's `hidden=true`-while-rendered defect is exactly why an attribute read is not evidence here.
7. Drawer totals via the PT-22 board harness.

---

### Open questions for the team-lead

- **Cost view and `main`:** `main` is 16% of estimated dollars ($528.72 of $3,230.40) versus 8.4% of tokens, because lead-session work skews to expensive models. The bar reorders between the two toggle states. I think that is honest and worth seeing; if it reads as a bug to you, the alternative is fixing bar order to the token view in both states.
- **PT-78 dependency:** every number here is backfill-only. When the receiver lands, `sources` becomes `["transcript-backfill", "otel"]` and the caption should say so. Nothing in this ruling needs to change for that — but if PT-78's §5 teammate check fails, the role stack has only historical data behind it and this block will slowly become a chart of one source. Worth knowing before the block ships.

### @architect — 2026-09-03

**Addendum — the concrete implementation surface.** The ruling above is the *what and why*; this is the exact set of paths, names, constants and strings, so implementation-lead and qa do not have to infer them. Nothing here changes a decision above.

### Files

| Path | Role |
|---|---|
| `scripts/cairn/prices.json` | the dated price table (schema in §1 above) |
| `scripts/cairn/cairn.py` | `load_prices()`, `build_tokens_payload()`, the `/api/tokens` route |
| `scripts/cairn/dashboard/src/lib/token-chart-logic.ts` | **pure** selection/sort/caption functions — the test seam |
| `scripts/cairn/dashboard/src/lib/components/TokenCostChart.svelte` | the block |
| `scripts/cairn/dashboard/src/App.svelte` | mounts it |
| `scripts/cairn/board/board-logic.js` | `issueTokenSummary()` — pure, for the drawer |
| `scripts/cairn/board/board.js` | renders the drawer section |

### Server

- `load_prices(engine_dir: Path) -> dict` — reads `scripts/cairn/prices.json`, resolved from the **engine directory** (`Path(__file__).parent`), never from cwd. A missing or malformed table is **not** fatal: it yields `cost_usd: null` everywhere plus a `warning`, because the token half of the block must still render.
- `build_tokens_payload(data_dir: Path) -> dict` — returns §2's shape. Memoized in `_TOKENS_CACHE` keyed by `(str(path), st_mtime_ns, st_size)`; a vanished file invalidates rather than serving a stale hit. Follows `_FLOW_CACHE`'s existing pattern.
- Route `/api/tokens` in `do_GET`, beside `/api/flow`.
- **Cost is computed per `(issue, role, model)` before aggregation, then summed.** Aggregating tokens across models first and pricing the total is wrong by construction — the models have different rates.

### `token-chart-logic.ts` — the pure seam

```ts
export const DEFAULT_BAR_LIMIT = 12;
export type Metric = 'tokens' | 'cost';
export function barValue(issue, metric): number
export function selectBars(issues, metric, limit = DEFAULT_BAR_LIMIT, showAll = false)  // top-N by metric, `main` always appended last
export function formatCaption(payload, metric, shown, total): string
```

Every selection, ordering and caption assertion belongs here — `node --test` against `scripts/cairn/tests/js/`, no browser, mirroring how `board-logic.js` is already split from `board.js`. `main` is **never** subject to the top-N cut; it is appended after the cut regardless of rank.

### Caption — exact strings

Tokens view:

> Tokens are exact. Showing the top {n} of {total} issues by tokens, plus `main`. History begins {window_start}; earlier work predates the local transcript retention window and was never recorded.

Cost view:

> Dollars are **estimated** from published list prices retrieved {prices.retrieved} — standard speed, global inference, no discounts. Showing the top {n} of {total} issues by estimated cost, plus `main`. History begins {window_start}; earlier work predates the local transcript retention window and was never recorded.

Appended to either when `prices.unpriced_models` is non-empty:

> {k} model(s) have no published rate and are excluded from cost: {names}.

With Show-all active, `Showing the top {n} of {total} issues by …` becomes `Showing all {total} issues, ordered by …`. Every `{}` is interpolated from the payload — no literal dates in the component.

### Drawer (AC4)

`issueTokenSummary(tokensPayload, issueId)` in `board-logic.js` returns `{input, cache_write, cache_read, output, cost_usd, roles: [{role, input, cache_write, cache_read, output, cost_usd}]}`, or `null` when the issue has no bucket. `board.js` renders it in a section with **`id="drawer-token-usage"`** — a stable target the browser-visibility leg can aim at. An issue with no token data renders **nothing**, not a zero row: zero tokens and no record are different facts.

### Build and the gate

```bash
cd scripts/cairn/dashboard && npm run build   # then commit dist/
```

`check_dist_freshness.py` treats the whole `scripts/cairn/dashboard/` subtree except `dist/` as source, so a `.svelte` or `.ts` change with a stale `dist/` fails `/finish-feature`. **`scripts/cairn/board/` is outside that subtree** — the drawer change needs no build and the gate will not catch a board-only regression, so the drawer needs its own test rather than relying on the gate.

### Tests

| File | Asserts |
|---|---|
| `tests/test_prices_table.py` | every model in the committed data has rates; **Fable 5.1 cache read is `0.25` and Fable 5's is `1.00`** — the asymmetry a multiplier refactor destroys |
| `tests/test_tokens_endpoint.py` | payload shape; per-model-then-sum cost; `cost_usd: null` (never `0`) for an unknown model; missing data file → `issues: []` + warning + **HTTP 200, never 500**; missing `prices.json` → tokens still render; memo invalidates on mtime change |
| `tests/js/token-chart-logic.test.js` | top-N by each metric; **`main` always present regardless of rank**; ordering follows the displayed metric; Show-all returns all; caption strings for both metrics, Show-all, and the unpriced suffix |
| `tests/test_dashboard_token_block.py` | the built `dist/` serves the block; toggle present in both states |
| `tests/js/…` or the PT-22 harness | `issueTokenSummary` totals and role split; no section for an issue with no data |

**Browser-visibility leg — PT-32/PT-57 protocol, and it is load-bearing, not ceremony.** `elementFromPoint` or a screenshot, **never** property reads, for: the block itself, the toggle in **both** states, the caption, and `#drawer-token-usage`. PT-76 shipped `hidden=true` while rendered for four releases; an attribute read cannot tell you a user can see something. Sample **every placement** — the block in the dashboard shell and the drawer in the embedded board are different containers with different inherited styles (PT-64).

**A regression worth pinning explicitly:** pricing all cache writes at the 5m rate understates the current corpus by **$264.08 on $3,230.40 (9%)**. That figure drifts as data grows — assert the **direction and inequality**, never the value.

### @architect — 2026-09-03

**Amendment — block structure correction, and the role palette is a token proposal, not my call alone.**

## 1. Correction: §3's mount description was wrong

I wrote that the block goes in a `Card.Root` "matching the flow chart's own `sm:col-span-2 lg:col-span-4` span". **`IssueFlowChart` carries no such class.** Verified in `App.svelte`: the 4-column `<section class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4">` closes at line 506, and `{#if IssueFlowChart}<IssueFlowChart />{/if}` sits **after** it at 513–515 as a bare component that owns its own `<section>` and `Card.Root` internally. The span class I quoted belongs to the error/skeleton cards *inside* the grid.

**Ruling: `TokenCostChart.svelte` follows `IssueFlowChart`'s literal pattern** — its own `<section>` and `Card.Root` inside the component, mounted as a bare `{#if TokenCostChart}<TokenCostChart />{/if}` immediately after the `IssueFlowChart` block, **no grid classes**, dynamically imported the same way. — full-width chart panels are siblings of the status grid, not members of it; putting this one in the grid would make it the only chart that reflows with the status cards.

## 2. The role palette — mechanism ruled, hues escalated

**This is the moment `design-system-spec.md` named.** That document already says, verbatim, that `--chart-1`…`--chart-5` is a single-hue sequential ramp that "reads badly for **categorical data**", that "there is genuinely **no multi-hue categorical palette anywhere in this system**", and that the need should be flagged "at chart-spec time… that is the point to bring a token proposal to the team, decided against a concrete use case rather than pre-emptively invented here." Roles are exactly that case: unordered categories, ~12 of them today.

**What I rule (engineering):**

- **Do not reuse `--chart-flow-*`.** Those six are keyed one-per-`STATUS_ORDER` status and validated as an **ordinal** ramp (monotone lightness). Roles have no order; borrowing an ordinal ramp would render `architect` and `qa-engineer` as if one ranked above the other, and would couple this block to a status taxonomy it has nothing to do with.
- **Do not reuse `--chart-1`…`--chart-5`.** Same single-hue problem, and two of the five already fail the 2:1 light-end gate against the white `--card` (`--chart-1` at 1.33:1, `--chart-2` at 1.91:1) — the finding that produced `--chart-flow-*` in the first place.
- **The validator to run is the dataviz skill's *categorical* check, not `validate_ordinal`.** Applying the ordinal validator here would enforce monotone lightness across roles, which is meaningless for unordered categories and actively misleading. Whatever palette lands must be validated in **both** modes against each mode's own real `--card`, and guarded mechanically the way `test_dashboard_chart_ramp.py` guards the flow ramp — re-running the validator against whatever values actually sit in `app.css`, never a snapshot of today's numbers.
- **Token count: 8.** — beyond roughly eight hues, categorical series stop being reliably tellable apart, and 8 covers every role that has ever appeared in real data except the ad-hoc probes. This is a distinguishability limit, not a roster count; the roster will outgrow it.
- **Assignment is a pure function of the role name, via an explicit ordered list checked into `token-chart-logic.ts`** (`ROLE_TOKEN_ORDER`), **not** derived from roster directory order and **not** from rank in the current payload. — a role must keep its colour across reloads *and* across data changes; deriving from `sorted(.claude/agents/*.md)` means adding one agent file silently reshuffles every colour after it alphabetically, and deriving from payload rank means a role changes colour when its usage changes. Appending to an explicit list can only ever add.
- **Fold rule:** roles not in `ROLE_TOKEN_ORDER` aggregate into a single **`other`** series, rendered last in a muted neutral, never one of the 8 hues. The caption states it: `{k} other role(s) grouped: {names}`. — the same posture as `cost_usd: null` for unpriced models; a grouped bucket must announce itself rather than silently absorb.

**What I am not ruling, and why it goes to ux-designer + you:** the eight hue values, and which roles occupy the list. Both are design-system decisions the spec explicitly reserves for a team token proposal, and the membership question has a real trade-off I should not settle alone:

- **Option A — roster-only:** the 8 slots go to `team-lead` plus the seven most-used `.claude/agents/` roles. `auxiliary` and `subagent-unattributed` fold into `other`. Cleanest reading of "who did the work", but it hides the two diagnostic buckets in a grey lump.
- **Option B — two reserved slots:** 6 hues for roles, plus fixed distinct treatments for `auxiliary` (cheap-model overhead) and `subagent-unattributed` (the role-resolution failure guard). Costs two hues but keeps the failure signal visible on the chart, which is where someone would actually notice it.
- **Option C — 8 role hues, and `auxiliary`/`subagent-unattributed` rendered as neutral hatched/muted segments outside the hue palette.** Keeps all 8 for real roles and still distinguishes the two synthetic buckets.

I lean toward the shape of B or C on the grounds that `subagent-unattributed` is a guard and guards should be visible — but the hue budget is ux-designer's to spend, so I am stopping here rather than picking.

**Blocked until that lands:** implementation-lead should not touch `app.css`. The block can be built against the existing tokens with a `TODO` marker and the palette swapped in afterwards; the structure, selection, caption and drawer work in §§1–8 are all independent of which hues win.

### @ux-designer — 2026-09-03

**Categorical role palette — the token proposal architect reserved for the team (ux-designer, 2026-09-03).**

Full writeup with hue-derivation rationale and validator evidence is now in `docs/DESIGN/design-system-spec.md` → **Categorical role palette (PT-79)** (new section, right after **Chart-local ramp**). Summary:

**Validator note:** the dataviz skill's `validate_palette.py` categorical check was not reachable in this session (bundled-skill path present but unpopulated, no `Skill`/`ToolSearch` grant). Computed manually instead — OKLCH↔sRGB math (Ottosson's matrices), WCAG contrast, and Machado/Oliveira/Fairchild 2009 CVD simulation (deuteranopia + protanopia). Flagging the method, not asserting precision — re-run through the real skill script before lock.

**Mechanism:** followed architect's ruling — 8 role-hue tokens, `ROLE_TOKEN_ORDER`-shaped, base construction from the published Okabe & Ito (2008) CVD-safe categorical palette (7 hues + 1 added violet to fill the budget), lightness compressed into a band that clears 2:1 against **both** this app's real `--card` surfaces (light white, dark `#1c1917`) — a single value serves both `:root` and `.dark`, no per-mode split needed.

**Membership (picked Option C, per your lean):** 8 of the 10 named roles get dedicated hues — `team-lead`, `product-manager`, `architect`, `ux-designer`, `seceng`, `devops-engineer`, `implementation-lead`, `qa-engineer`. `frontend-lead` (3.06M tokens in the real committed data) and `backend-lead` (0, never appears) fold into `--chart-role-other` — both are Implement-phase specialist variants of work `implementation-lead` already covers as the generalist case to date; promoting either later is a pure append, never a reshuffle. `mcp-broker` was already outside your 10-role list and stays out.

**The two guards get separate neutral values, not one** — deviating from the `--chart-role-guard` single-token naming in the kickoff: `--chart-role-guard-aux` (lighter, for `auxiliary`) and `--chart-role-guard-unattributed` (darkest/heaviest of the three, for the failure-signal series — your "guards should be visible" note), plus `--chart-role-other` for the generic fold. Reasoning: collapsing `auxiliary` and `subagent-unattributed` into one identical grey would make it impossible to tell overhead from an attribution failure at a glance — same "a grouped bucket must announce itself" logic you applied to `cost_usd: null`.

**One brand-adjacent nudge:** Okabe-Ito's raw "blue" hue sits within ΔE_OK 0.033 of this system's `--primary` — close enough to look like the same blue, which would make the `team-lead` bar segment read as an active/primary UI affordance. Re-hued (H 244°→252°, same L/C character) to ΔE_OK 0.078 from `--primary` — related "family" blue, not a duplicate. Also checked `implementation-lead`'s vermillion against `--destructive` (ΔE_OK 0.119, comfortably distinct — no error-state confusion from a large role segment).

**Values (identical `:root`/`.dark`):**

| Token | Role/use | OKLCH | Hex |
|---|---|---|---|
| `--chart-role-1` | team-lead | `oklch(0.570 0.1587 252.00)` | `#1a79d1` |
| `--chart-role-2` | product-manager | `oklch(0.6375 0.1242 76.77)` | `#b5801e` |
| `--chart-role-3` | architect | `oklch(0.600 0.2403 300.00)` | `#9a4cf6` |
| `--chart-role-4` | ux-designer | `oklch(0.6018 0.2368 346.32)` | `#da229d` |
| `--chart-role-5` | seceng | `oklch(0.6286 0.1262 236.18)` | `#2193ca` |
| `--chart-role-6` | devops-engineer | `oklch(0.710 0.1402 105.04)` | `#afa625` |
| `--chart-role-7` | implementation-lead | `oklch(0.5735 0.1465 47.51)` | `#bb5718` |
| `--chart-role-8` | qa-engineer | `oklch(0.5728 0.1114 165.46)` | `#1d8d68` |
| `--chart-role-guard-aux` | auxiliary series | `oklch(0.700 0.015 56.0)` | `#a69c96` |
| `--chart-role-guard-unattributed` | subagent-unattributed series | `oklch(0.440 0.020 56.0)` | `#5c5048` |
| `--chart-role-other` | grouped roles outside `ROLE_TOKEN_ORDER` | `oklch(0.600 0.010 56.0)` | `#857f7b` |

**Evidence:** all 11 values clear 2:1 contrast against both real `--card` surfaces (worst: `devops-engineer` 2.54:1 white, `--chart-role-guard-unattributed` 2.24:1 dark). Closest pairwise separation (ΔE_OK, OKLab) among the 8 role hues: `team-lead`/`seceng` at 0.078, `product-manager`/`implementation-lead` at 0.096 — both well above the ~0.02 JND, everything else clears 0.11+. Under simulated deuteranopia/protanopia the closest pair (still `team-lead`/`seceng`) sits at 0.131/0.147 — no collapse. Full per-pair tables and the rejected "flat lightness" attempt (which did collapse two pairs) are in the spec section.

Ready for implementation-lead to paste into `app.css` once you unblock — the exact `:root`/`.dark` block is in the spec section's closing subsection.

### @architect — 2026-09-03

**Amendment — the role-palette guard uses an in-repo validator and never skips.**

**Ruling:** the categorical role-palette check runs against `scripts/cairn/tests/palette_check.py` — an **in-repo, stdlib-only** module — not the external dataviz skill script. The external script stays optional and keeps backing the **ordinal** `--chart-flow-*` ramp test exactly as today. — a skipping guard is indistinguishable from a passing one in a green run, and fail-on-missing would leave this harness permanently red; vendoring the two checks we actually need removes the choice between an invisible skip and a useless failure.

**The module implements exactly two checks, both of which I ran independently against ux-designer's hand-computed table (11/11 hex values and 22/22 contrast figures reproduced exactly):**

1. **WCAG contrast ≥ 2:1** for all 8 role tokens and all 3 neutral fold/guard tokens, against **each mode's own `--card` — both parsed from the same `app.css` under test**, never hardcoded. — the surface is half of a contrast ratio; a guard that pins it cannot catch someone re-tuning `--card`.
2. **Minimum pairwise OKLab ΔE ≥ 0.06** among the eight role hues. — this is the categorical criterion. **Do not substitute the ordinal validator's monotone-lightness check**: it is meaningless for unordered categories and would render roles as though ranked. Measured minimum today is **0.078** (`team-lead` / `seceng`, the two blues), 3.9× the ~0.02 JND, so 0.06 has headroom without being toothless.

**Values are parsed from the CSS under test, never snapshotted** — same posture as `test_dashboard_chart_ramp.py`, which is what caught the original `--chart-1`/`--chart-2` failure.

**Verified before handing over — the guard is decisive in both directions**, which is the property that matters:

- **Passes** the 11 proposed values, reporting the same figures ux-designer published.
- **Fails** a token pushed under the contrast floor (`--chart-role-6` at the old `--chart-1` yellow → `1.32:1`).
- **Fails** two role hues collapsed together (ΔE 0.005).
- **Fails** a deleted token, by name.
- **Fails** a change to the *surface* side — lightening the dark `--card` produces 10 failures. A snapshot-based guard would miss this entirely, and it is the realistic future edit.

**Not covered, deliberately: CVD/deuteranopia simulation.** It stays a design-time check documented in `design-system-spec.md`. — it needs simulation matrices, and reimplementing them here would be a second unverified implementation checking a first one; that is the shape of the self-consistency trap this whole amendment exists to avoid.

**qa-engineer owns the drop-in.** The implementation is at `temp/2026-09-04-architect-palette_check.py`, written to be moved to `scripts/cairn/tests/palette_check.py` unchanged.

### @team-lead — 2026-09-03

PR opened: https://github.com/richmosko/project_template/pull/158. Awaiting architect review and QA validate + browser leg from 3aa09e8.

### @architect — 2026-09-03

**Review of the block @ `3aa09e8` — PASS, one minor delta against my own ruling and three observations.** Reviewed by construction from a detached worktree of my own (`/tmp/arch-wt`), never the shared tree, against `3836ce6` + `a375ff7` + `ad940d3` + `962f3e9` + `fd6df5c`.

### Delta (minor) — a missing `prices.json` produces no `warning`

My addendum said a missing price table yields "`cost_usd: null` everywhere plus a `warning`". Constructed: with `prices.json` absent the payload returns `cost_usd: null`, tokens intact, `prices: {"retrieved": null, "source": null, "unpriced_models": [<every model>]}` — and **`warning: null`**.

**I recommend amending the ruling, not the code.** The implemented behaviour is *more* informative than what I specified: every model lands in `unpriced_models`, so the caption names them explicitly and renders "retrieved unknown" via its `?? 'unknown'` fallback, rather than a generic "prices unavailable". The only thing lost is a programmatic flag for an API consumer, and there is no such consumer. Lead's call; if you want the ruling honoured literally it is a one-line addition.

### Observation 1 — a receiver test fails in any detached checkout

`test_otel_receiver.py::test_prefix_and_branch_resolution_do_not_depend_on_cwd` fails in my worktree: it asserts the resolved issue set is not `{"main"}`, but resolution reads the **checkout's own branch**, and a detached HEAD yields `main`. It passes in the shared checkout, which is on a real branch. **Not a defect at `3aa09e8`** — but the test is coupled to the checkout's branch state and will fail for the wrong reason in any worktree, CI checkout of a sha, or `git bisect`. Worth pinning it to a fixture branch name rather than "whatever this checkout is on". It also documents real behaviour worth knowing: **the receiver attributes everything to `main` whenever HEAD is detached.**

### Observation 2 — the drawer function was renamed, correctly

My addendum named it `issueTokenSummary`; the implementation is `CairnLogic.tokenTotalsForIssue`, a deliberate rename flagged in qa's own test header under `INTERFACE.md`'s rename-if-awkward instruction. Behaviour is exactly as ruled and I have no objection — noting it so my ruling's name does not confuse a later reader. Its five tests pass: returns the counters, cost and role split; **`null` for an absent issue rather than fabricated zeros**; `null` for `main`; and it does not mutate the payload.

### Observation 3 — `node --test` on the js *directory* reports a failure

Every individual file under `scripts/cairn/tests/js/` passes when run alone, including all PT-79 ones, and `helpers.js` passes too — but the directory-level invocation reports a failure. `helpers.js` predates PT-79 (present at `44f8b0c`), so this is not introduced here. Worth someone establishing the canonical js runner so "the js tests pass" has one meaning.

### Verified correct — no action

**Palette.** All **11 token values in `app.css` match the spec paste block exactly** (parsed both sides and compared; zero mismatches), and the vendored `palette_check.py` **passes against the real committed `app.css`** — contrast ≥2:1 for all 11 against both real `--card` surfaces, minimum pairwise ΔE among the 8 role hues above the 0.06 floor.

**Cost ordering — the decisive construction.** One `(issue, role)` bucket, two models with different rates: 1 M input on `claude-fable-5-1` ($10/MTok) plus 1 M input on `claude-sonnet-5` ($2/MTok) → **$12.00**. Aggregating tokens first and pricing the total would have produced $20.00 or $4.00. Cost is computed per `(issue, role, model)` and then summed, as ruled.

**Degradation, all HTTP-200-with-data:** unknown model → `cost_usd: null` (never `0`), tokens still rendered, model named in `unpriced_models`; missing data file → `issues: []` + `warning: "no token data"`; malformed line → the good rows survive and the warning names the line number.

**Memo.** Keyed `(path, mtime_ns, size)` and genuinely invalidating: appending a line moved 1 000 000 → 1 500 000, and a **same-length** edit moved it to 1 400 000 — proving `mtime_ns` is in the key and not just `size`.

**Selection and ordering.** With 14 issues ranked deliberately differently by the two metrics: the two bar sets **differ**, each is ordered descending by its own displayed metric, the non-`main` count is exactly `DEFAULT_BAR_LIMIT = 12`, `showAll` returns everything, and **`main` is present and last in both views despite having the smallest token total (5)** — proving it is appended after the top-N cut rather than competing in it. Two independent guards keep it unclickable: the component returns early on `issue === 'main'`, and `tokenTotalsForIssue` returns `null` for it.

**Captions** reproduce my addendum's strings verbatim in all four states — tokens, cost, Show-all ("Showing all 68 issues, ordered by…"), and the unpriced suffix.

**`ROLE_TOKEN_ORDER`** is the 8 spec roles in spec order; `roleTokenSeries` maps listed roles to themselves, `auxiliary` and `subagent-unattributed` to their **own** neutral series rather than being folded, and everything else (`frontend-lead`, `impl2`, an invented role) to `other`.

**Structure.** `TokenCostChart` is dynamically imported exactly as `IssueFlowChart` is, mounted as a bare `<TokenCostChart />` after it, and owns its own `<section aria-label=…>` + `Card.Root`. Its single `grid` occurrence is an internal `<div class="grid gap-1">` header layout — **no `col-span` anywhere**, so it is not a member of the 4-column status grid. `dist/` is fresh.

### @architect — 2026-09-03

**Re-check @ `b934262` — PASS, no deltas.** From my detached worktree, spanning `b9a9447` → `5697e0c` → `be317c1` → `53fd27a` → `b934262`. My `3aa09e8` review's one delta is closed and nothing regressed.

### The four items asked for

**1. `warning` field — closed, and hardened beyond the ruling.** Missing `prices.json` → `warning: "no price table found"` with `cost_usd: null` and tokens intact. A **present-but-empty `models` dict** warns identically — a case my addendum never named, and the better boundary: "the table exists but prices nothing" is the same user-visible failure as no table. An unknown model against a *valid* table correctly does **not** warn; that stays `unpriced_models`' job. Normal path: no warning, cost computed.

**2. `main` muted per datum.** `fillOpacity={(d) => (d.issue === 'main' ? 0.55 : 1)}` — per-datum, not a separate series, so it cannot drift out of sync with `selectBars`' own `main` handling.

**3. Click handler moved to the tooltip layer.** `onTooltipClick` receives the hovered datum and forwards to `onBarClick`, with the reasoning recorded in-file: the tooltip's overlay `<rect>`s sit above the real bars and swallow clicks aimed at them. The `issue === 'main'` early return survives the move, so `main` remains unclickable, and `tokenTotalsForIssue` returning `null` for `main` is still the second, independent guard.

**4. Counter series bound to `--chart-counter-*`.** The four counters map to the counter tokens and the role stack to `--chart-role-*`; the two families no longer share tokens.

**Values check on what actually shipped, not what I was told.** I re-derived the counter palette from the **oklch in `app.css`**, not the hex I was handed — the two could have diverged in rounding. They do not: the shipped oklch render to exactly `#356d73`, `#42858c`, `#4f9da6`, `#5cb4be`. Ordinal criteria on the shipped values: monotone ascending, adjacent ΔL **0.075 / 0.075 / 0.070** (floor 0.06), hue spread **0.0°**, minimum contrast **2.40:1** on white and **3.00:1** on the dark card. Both mode blocks carry byte-identical values, so "one set, both modes" is true as written. The vendored `palette_check.py` still passes on the real `app.css`.

### Regression sweep — every `3aa09e8` construction re-run

Cost per-model-then-sum still **$12.00** on the two-rate bucket (aggregate-then-price would be $20 or $4) · unknown model → `cost_usd: null` with tokens intact · missing data file → `[]` + `"no token data"` · malformed line tolerated with a warning naming it · memo still invalidates on a **same-length** edit · `main` present and last in both toggle states with the non-`main` count at exactly 12 and `showAll` returning everything · selection still differs between metrics · all four caption states verbatim · `roleTokenSeries` still keeps `auxiliary` and `subagent-unattributed` as their own series and folds unlisted roles to `other` · drawer 5/5 · **the full Python suite is green**, and `dist/` is fresh.

**No `col-span` on the component.** A raw grep count reports one occurrence, but it is inside the explanatory comment; the `<section aria-label="Token usage and estimated cost">` and `Card.Root` carry no grid classes, and the only `grid` class is an internal `<div class="grid gap-1">` header layout.

### Observation from my last review, now closed

`53fd27a` fixed the detached-HEAD coupling in `test_otel_receiver.py`. That test now passes in a worktree, a CI sha checkout and under `git bisect`, and it records the real underlying behaviour (**detached HEAD resolves to `main`**) rather than depending on the shared checkout happening to sit on a feature branch. The whole suite being green from a detached worktree is the evidence.

### @architect — 2026-09-03

**Re-check @ `078ad9e` — PASS, no deltas. Merge-ready from my side.** From my detached worktree; every claim checked against the thing that would break, not against the commit message.

### The four items

**1. Padding — diagnosis and fix both confirmed against layerchart's own source.** `states/chart.svelte.js`'s padding getter reads `if (paddingProp == null && !radial && effectiveAxis)` before calling `defaultChartPadding`, then returns `{...defaultPadding, ...paddingProp}`. So the earlier `padding={{left:64}}` did bypass the default computation entirely and merged against a zeroed base — bottom 0, hence the clipped legend. Exactly as described.

The replacement reproduces the default's structure: `defaultChartPadding({axis:true, legend:true})` yields `top 4 / right 4 / left 20 / bottom 20+32`; `CHART_PADDING` is `top 4 / right 4 / left 64 / bottom 20 + 24×3`. Top and right match the default exactly, `left` is widened deliberately for the formatted value labels, and the legend allowance is tripled to three rows.

**2. `fillOpacity` moved under `props.bars`.** It now sits in the nested `bars` object rather than as a top-level `BarChart` prop, which is what reaches `Bar`'s per-datum accessor; the in-file comment cites the exact forwarding path. `main` → 0.55, everything else → 1, still per-datum rather than a separate series.

**3. Deep link — verified against the reader, not the claim.** The chart emits `/dashboard/issues?issue=<id>`. I traced all three hops: `App.svelte` reads `?issue=` and derives `&open=<id>` for the embed; `board.js` reads `open` via `parseOpenIssueId`; `openDrawer` fires. The chain closes. `?open=` emitted directly would indeed have been a key the shell never reads.

**4. `parseOpenIssueId` + `openDrawer(openId)` inside `init()`'s `apiGetBoard().then` callback** — confirmed at the call site, after `state.board` is populated, with the reason recorded in-file.

### One thing I chased down and cleared

`parseOpenIssueId` performs **no id-shape validation** — it returns `<script>X`, `../../etc`, `PT-79 OR 1=1` and a 300-character string verbatim. Given this repo's PT-20 XSS history I followed both consumers rather than assume: `apiGetIssue` wraps the id in `encodeURIComponent`, and `showToast` assigns via `el.textContent`, never `innerHTML`. A bogus id is therefore inert — a failed fetch and a text-only toast. **Not a defect and not a merge blocker.** Adding a shape check would be defence in depth, nothing more.

### Regression sweep vs `b934262` — clean

Cost still **$12.00** on the two-rate bucket · memo still invalidates on a same-length edit · `main` present and last in both toggle states, limit 12, `showAll` returns everything, selection still differs by metric · all four caption states verbatim · `roleTokenSeries` guards intact · **430 JS tests passing across every file** · full Python suite green · `dist/` fresh.

### One thing for the manual browser leg

**The legend reservation is a fixed three rows (72 px).** That is a constant, not a measurement, so a fourth wrapped row would overlap the axis again — the same class of defect just fixed. The realistic trigger is **Show-all at a narrow viewport**, where 69 bars and up to 11 series give the legend the most to wrap. Worth making that an explicit case in the visibility leg rather than checking the default view only.

### @team-lead — 2026-09-03

Browser-visibility leg (team-lead, real Chrome, elementFromPoint + screenshots, scratch board; qa had no browser tools this session). On 3aa09e8 seven visual defects were found that tests and the by-construction review could not see (y-axis clipping, show-all label smear, main not muted, bar click inert, counter swatches indistinguishable, legend clipping at narrow width, empty drawer container). Fixed across b9a9447, 5697e0c, b934262, 078ad9e, 7f96d47; two regressions along the way (legend moved into the plot; fixed-row legend reservation) were caught by re-checks. Final state on 7f96d47: at 1500px and at ~600px viewports, tokens/cost/show-all states all render with no x-label overlaps, legend below the axis with every entry hit-tested, main muted (fill-opacity 0.55), bar click on archived PT-69 opens its drawer with the usage block visible ($226.57), cold-load /dashboard/issues?issue=PT-77 opens the drawer, light theme reads on the white card. Residual, cosmetic: at ~600px in Show-all one label pair (main's forced label and its neighbour) overlaps. Recorded, not blocking.

### @team-lead — 2026-09-03

Validate passed on 7f96d47 (qa green; architect PASS; lead browser leg at both widths); merging via PR #158. Closing. Follow-ups filed after merge: PT-81 (receiver hardening, ships in v0.11.0), PT-82 (worktree isolation), PT-83 (ordinal ramp guard, JS runner), PT-84 (main → milestone overhead attribution).
