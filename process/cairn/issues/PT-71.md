---
id: PT-71
title: Icon consistency: sidebar nav icons for collapse rail + Lucide trigger on board
status: done
milestone: PT-0.9
parent: null
blocked_by: []
assignee: null
labels: []
priority: null
pr: https://github.com/richmosko/project_template/pull/143
created: 2026-08-29
updated: 2026-08-30
---


## Comments

### @team-lead — 2026-08-29

Mosko's post-merge live-test findings #3 and #6 (2026-08-29):
- **Collapse rail has no icons**: the sidebar collapse toggle collapses to an icon rail, but Dashboard/Board nav items have no icons — add Lucide icons (the project's ruled icon library) so the collapsed rail is usable.
- **Dropdown trigger icon isn't Lucide**: the theme-settings trigger icon looks off-reference — ensure BOTH surfaces use the actual Lucide glyph (dashboard via lucide-svelte; board needs the inline Lucide SVG, not a unicode character).

### @team-lead — 2026-08-30

Feature started. Branch: `feature/pt-71-lucide-icons`. ux's icon spec (from prep, 2026-08-29): Dashboard nav = LayoutDashboard, Board nav = Kanban (better rail-size silhouette than ListTodo, distinct from LayoutDashboard's grid), theme trigger = Settings2 on BOTH surfaces — the board vendors the real Lucide settings-2 SVG inline (actual Lucide path data, not a unicode stand-in; exact visual parity is the point). Lucide default 24px/2px-stroke scales to rail size — do not thin the stroke. The "Issue Tracking" rename is PT-72's scope; PT-71 ices the CURRENT nav labels.

### @qa-engineer — 2026-08-30

**QA sha-pinned approval — PR #143 @ 8ee3a98**

Confirmed head via `git rev-parse HEAD` — `8ee3a98` (fix commit `b30d44e` + tracker-status `8ee3a98`), matching PR #143 exactly. Clean working tree.

Ran the full gate fresh, post-commit, clean shell:
- Python: `python3 -m unittest discover -s tests` — **995 tests, 0 failures, 0 errors, 1 expected skip**.
- JS: `node --test tests/js/*.test.js` — **384/384 passing**.
- `npm run build` — clean.
- `npm run check` (svelte-check + tsc) — **0 errors, 0 warnings, 928 files**.

Covers both findings: sidebar collapse rail now renders `LayoutDashboard`/`Kanban` icons on the Dashboard/Board nav entries (confirmed real, not just green tests), and the board's theme-settings trigger vendors the actual Lucide settings-2 SVG (two paths, two circles, viewBox/stroke-width matching the installed `@lucide/svelte` v1.37.0 package exactly — verified against the real installed source, not approximated). Dashboard's trigger already had the real `Settings2` component from PT-69, now pinned as a regression guard.

Also caught and fixed a real side effect: the new icon markup (nested elements + a `<span>` label wrapper) broke a pre-existing PT-61 sidebar-nav test whose regex assumed no nested tags between a link's opening tag and its label. Retargeted both assertions to check the link's stripped text content instead of exact tag adjacency — in line with that test's own stated intent of being loose about icon composition, not a scope change.

**Approved.**

### @team-lead — 2026-08-30

Validate passed; merging via PR #143. Closing.
