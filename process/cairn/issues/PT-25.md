---
id: PT-25
title: Board + CLI sub-issue delivery: parent-side child badge, drawer nesting, cairn show children
status: todo
milestone: "0.6"
parent: null
assignee: null
labels: [board, cli]
priority: P2
pr: null
created: 2026-08-21
updated: 2026-08-21
---

TRACKER.md § Sub-issues already promises more than the tree delivers. The engine
has the `parent` field, `cairn new --parent`, a dangling-parent lint, and a
server-side child count — but the board only renders a `↳ <parent>` chip on the
*child*. Close the spec gap on the delivery end.

## Acceptance criteria

- [ ] Board: parent cards show a `n/m` child badge (done-children / total-children), per the spec's `2/3` example.
- [ ] Board drawer: a parent's children are listed (id + title + status), each linking/opening to the child; a child's drawer links back to its parent.
- [ ] `cairn show <id>` on a parent lists its children (id, status, title); on a child, names its parent.
- [ ] Children sort by id (`_id_sort_key` semantics — numeric, not lexicographic).
- [ ] Pure DOM-logic goes through board-logic.js with node:test coverage (PT-22 harness); Python-side changes covered by the pytest suite.
- [ ] TRACKER.md § Sub-issues updated only if delivered behavior deviates from the spec text.

