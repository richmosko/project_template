---
id: PT-77
title: One-time backfill — scrape existing transcripts for per-issue token usage, nothing else
status: in-progress
milestone: PT-0.11
parent: null
blocked_by: []
assignee: architect
labels: [dashboard, cairn]
priority: null
pr: null
created: 2026-09-03
updated: 2026-09-03
---


## Problem

Nothing records what past issues cost to deliver. The only record is the local Claude Code
transcripts under `~/.claude/projects/-Users-mosko-Projects-project-template/` (57 files on
2026-09-03; nested subagents in `<session>/` subdirectories). They are outside the repo and
Claude Code prunes them on its retention window, so the history is decaying now.

Each `assistant` record carries `message.usage` (`input_tokens`, `cache_creation_input_tokens`,
`cache_read_input_tokens`, `output_tokens`), `message.model`, `timestamp`, `gitBranch`, and
`agentName` (absent for the lead). Verified 2026-09-03: bucketing by `gitBranch` → PT id and
`agentName` → role gives a clean per-issue, per-role table.

## Scope ruling (team-lead, 2026-09-03, per user)

This is a **one-time scrape**, not an ongoing process. It reads the transcripts once and records
**token usage only**: counts per issue, role, and model, split input vs output, with cache read
and cache write kept separate from fresh input. **No transcript content, no prompts, no tool
output, no message text, no session ids are recorded.** The transcripts themselves are never
copied into the repo. Ongoing collection is PT-78; the chart is PT-79.

## Acceptance criteria

1. A script under `scripts/cairn/` (name is the architect's call; not a permanent `cairn`
   subcommand) scans the transcript directory for this project, dedupes by `requestId`
   (fallback `uuid`), and buckets by issue (`feature/pt-NN-*`, `chore/pt-NN-*` → PT-NN; anything
   else → `main`) and role (`agentName`, default `team-lead`).
2. Output is a single committed data file under `process/cairn/` in the **same schema PT-78 will
   write**, so the two sources merge without translation. Per issue × role × model: fresh input,
   cache write, cache read, output. Plus a `source: transcript-backfill`, a `generated`
   timestamp, and the date window covered. Nothing else.
3. The `main` bucket is kept as its own bucket, not spread across issues.
4. The script fails loudly on a missing field; it never silently under-counts.
5. Tests in `scripts/cairn/tests/` run it against a **synthetic** fixture transcript (duplicate
   `requestId`, a lead record with no `agentName`, a subagent record in a subdirectory, a record
   on `main`). No real transcript is checked in as a fixture.
6. `process/TRACKER.md` gets a short note: what the file is, that it was produced once, and that
   PT-78 owns the ongoing path.
7. `cairn check` passes. No network call.

## Comments

### @team-lead — 2026-09-03

Feature started. Branch: `feature/pt-77-token-backfill`. Lead is green to merge on QA sign-off; the user stops in at the PT-0.11 release only.

### @architect — 2026-09-03

**Gating ruling — data file, merge semantics, script contract.** Every count below was measured today against the real transcript dir (30,281 assistant records with usage, window 2026-08-18 → 2026-09-04); no record content was read into context.

### 1. The data file

**Path:** `process/cairn/metrics/token-usage.jsonl` (new `metrics/` subdir). — `_dir_glob` globs `*.md` only, inside `issues/` `milestones/` `majors/` `archive/`, and nothing scans the data-dir top level, so this file is invisible to every loader and to `cairn check`.

**Format:** JSON Lines — one object per line, UTF-8, LF, trailing newline. — PT-78 AC4 requires an appender that loses nothing when it dies; appending a line is crash-safe, a single JSON object forces read-modify-write on every flush.

**Granularity:** bucket totals only — one line per (source, issue, role, model) per contribution run. No per-session rows, no session ids, no sub-window timestamps. — the privacy scope ruling, and per-datapoint rows would put transcript-shaped volume into git.

**Schema** — exact keys, all required unless marked optional:

```json
{"source":"transcript-backfill","generated":"2026-09-03T21:04:11Z","window_start":"2026-08-18","window_end":"2026-09-04","issue":"PT-28","role":"implementation-lead","model":"claude-sonnet-5","input":1234,"cache_write":56789,"cache_read":901234,"output":5678,"cache_write_5m":0,"cache_write_1h":56789,"records":412}
```

- `source` — `transcript-backfill` | `otel`.
- `generated` — RFC3339 UTC, `Z` suffix.
- `window_start` / `window_end` — `YYYY-MM-DD` UTC, inclusive, derived from the contribution's own data (not "now").
- `issue` — `<PREFIX>-<n>` or `main`.
- `role` — normalised agent name (§4).
- `model` — raw model string, unmapped. — PT-79's price table keys on it; a normalisation here would need undoing there.
- `input` / `cache_write` / `cache_read` / `output` — ints, the four canonical counters, 1:1 with OTel `type` = `input` / `cacheCreation` / `cacheRead` / `output`. **Authoritative for all totals.**
- `cache_write_5m` / `cache_write_1h` — optional ints, backfill only. OTel has no such attribute, so omitting them is not a translation. **Never sum these in place of `cache_write`:** across the corpus they total 216,247,562 against `cache_write`'s 216,245,870 — they disagree by 1,692 tokens. Keep them because this repo's cache writes are **100% 1-hour** (5m total: 0), and the 1h write rate differs from the 5m rate; discarding the split hands PT-79 a materially wrong cost.
- `records` — optional int, deduped API records folded into the bucket. Sanity/QA only.
- **No other keys.** The script reads `type`, `requestId`, `uuid`, `timestamp`, `gitBranch`, `agentName`, `message.model`, `message.usage.*` and nothing else; `message.content` is never touched.

**Line order:** issues by numeric id ascending, `main` last, then role, then model, then source. — git diffs and fixture assertions need determinism.

### 2. Merge semantics

- **Totals = plain sum over every line.** No last-wins, no read-time keying.
- **A regenerating source rewrites only its own lines:** read all lines → drop every line whose `source` equals its own → append its fresh lines → write via temp + `os.replace`. — re-running is idempotent and otel lines survive byte-for-byte.
- **An appending source only appends, never rewrites.** — a rewriting appender can lose a concurrent append.
- Both take an exclusive lock at `process/cairn/metrics/.lock` (`O_CREAT|O_EXCL`, treat as stale after 60s) before writing.
- **Non-overlap invariant:** no two lines from different sources may cover overlapping windows for the same bucket. The backfill sets `window_end` to its `generated` date; PT-78 must never emit data predating its own start. There is no cross-source dedupe — overlap is a silent double count.

### 3. The script

- **`scripts/cairn/backfill_tokens.py`**, run directly (`python3 scripts/cairn/backfill_tokens.py`). Not a `cairn` subcommand — it is one-time, and `check_dist_freshness.py` is the standing precedent for a standalone script in that dir.
- **Transcript dir default:** `~/.claude/projects/<slug>`, slug = absolute repo root with `/`, `_`, and `.` each replaced by `-`. Verified today: `/Users/mosko/Projects/project_template` → `-Users-mosko-Projects-project-template`, the only matching dir on this machine. (The `.` rule is untested — no dot in this path — but costs nothing and the script fails loudly if the dir is absent.)
- **Missing dir → error + non-zero exit, never an empty result.** — TRACKER's standing rule: "no tracker here" and "no data here" must not render identically.
- **Flags:** `--transcript-dir PATH` (override), `--out PATH` (tests), `--dry-run` (summary to stdout, write nothing).
- **Scan:** recursive `**/*.jsonl`. Nested subagent transcripts live at `<session>/subagents/agent-*.jsonl` (4 today); `memory/` and `<session>/tool-results/` hold no `.jsonl`, so the recursive glob needs no exclude list.
- **Record filter:** keep `type == "assistant"` carrying `message.usage`. **Skip `message.model == "<synthetic>"`** — 8 today, all-zero usage, no `requestId`, no API call behind them.
- **Fail loudly** (error naming file + line number, non-zero exit, nothing written): absent `gitBranch`, absent `message.model`, or any of the four counters missing. All four are present on all 30,281 records today, so an absence is news, not noise.
- **One tolerated skip:** a malformed **final** line of a file — Claude Code appends live (the corpus grew 38 records between two reads minutes apart). Warn, skip, continue. A malformed line anywhere else is a hard fail.
- **Prefix from `process/cairn/config.yml`** (`prefix: PT`), never hardcoded — the template ships to other projects.

### 4. Branch → issue, `agentName` → role

**Issue:** `^(?:feature|chore)/<prefix>-(\d+)(?![\d.])`, case-insensitive, anchored. Everything else → `main`.

- Case-insensitive and slug-optional because `feature/PT-47`, `feature/PT-48`, `feature/PT-49`, `feature/PT-51`, `feature/PT-53` exist with no trailing description.
- **The `(?![\d.])` guard is load-bearing.** `chore/pt-0.11-token-accounting`, `chore/pt-0.10-ledger-hygiene`, `chore/pt-0.8-dashboard-setup`, `chore/pt-0.7.1-setup` are **milestone** branches — they must land in `main`, not a fabricated `PT-0`. PT-79 joins these keys against issue files; a milestone id in an issue-keyed bucket breaks that join.
- **Multi-issue branches attribute wholly to the first id** (`feature/pt-7-8-9-13-cli-hardening` → PT-7, `feature/pt-10-11-drawer-fixes` → PT-10, `feature/pt-21-23-close-0.5.1` → PT-21). — the transcript carries no finer signal; any split would be invented. Note it in the TRACKER blurb.
- `phase/*` and `feature/cairn-engine` → `main`, as intended.

**Role:** `agentName`; absent → `team-lead` (8,220 records today).

- **Normalise by stripping a trailing `-<digits>` only when the stem is a roster name** (a file in `.claude/agents/*.md`): `qa-engineer-76` → `qa-engineer`, `qa-engineer-2` → `qa-engineer`, `implementation-lead-2` → `implementation-lead`.
- **Everything else verbatim, no alias guessing:** `impl` (537), `impl2` (470), `guide-pt18` (19), `qa-pt18` (20), `roundtrip-probe` (9). — `impl`/`impl2` were *probably* implementation-lead sessions; "probably" does not belong in committed data. The script prints unknown role names to stderr; folding them later is a deliberate one-line alias dict.

### 5. Dedupe key

**`requestId`, falling back to `uuid`, deduped globally across all files — not per file.**

- Measured: 30,281 records → **16,508 unique keys**; **11,027 requestIds appear more than once** and **zero duplicates disagree** on (four counters, model, `gitBranch`, `agentName`). Skipping dedupe inflates totals ~1.83×.
- `uuid` is unique across all records; the fallback fires only on `<synthetic>` records, which are filtered first, so it is defensive today. Keep it anyway — one line, and the AC asks for it.

### Notes carried forward

- **Retention has already bitten.** The earliest surviving record is 2026-08-18. Everything before that is gone. `window_start` states this in the data; PT-79 must not present the chart as complete history.
- **QA:** assert only against synthetic fixtures. The real corpus is a moving target — it grew while I measured it. Fixture cases to cover, all seen in the real data: duplicate `requestId` across two files, a lead record with no `agentName`, a `<session>/subagents/agent-*.jsonl` record, a `main` record, `agentName: qa-engineer-76`, `model: "<synthetic>"`, an uppercase `feature/PT-47` branch, a `chore/pt-0.11-*` milestone branch, and a truncated final line.

### @architect — 2026-09-03

**Addendum to the ruling above — test seams and dedupe fallback, both named exactly.** These supersede the flag names in §3; everything else in the ruling stands.

### Flags (exact spellings — qa's fixtures bind to these)

- **`--transcripts-dir PATH`** — overrides the derived `~/.claude/projects/<slug>` location. Adopting qa's proposed spelling verbatim: the `<noun>-dir` shape matches `cairn.py --data-dir`, plural or not is arbitrary, and re-litigating it would cost a coordination round for nothing.
- **`--out-file PATH`** — overrides the output path (default `process/cairn/metrics/token-usage.jsonl`). Paired with `--transcripts-dir` so each flag names the *kind* of path it takes; `--out` alone reads like it could accept a directory.
- **`--dry-run`** — computes and prints the summary, writes nothing, exits 0. Matches `cairn.py`'s existing `--dry-run`.
- Both path flags accept any location, inside the repo or not — the tests point them at tmpdirs, so no containment check.
- Exit 0 on success; non-zero on any hard fail, with nothing written.

### Dedupe fallback — exact order of operations

Per record, in this order:

1. Parse the line. Malformed → hard fail, **except** a malformed final line of a file (live append), which warns and skips.
2. Keep only `type == "assistant"` carrying `message.usage`.
3. **Drop `message.model == "<synthetic>"`.** This happens *before* dedupe, so synthetic records never reach the key logic.
4. **Validate required fields on every kept record** — `gitBranch`, `message.model`, and all four counters — *before* dedupe, not after. A malformed duplicate must fail the run even though its twin is valid; validating post-dedupe would let a bad record hide behind a good one.
5. **Dedupe key = `requestId` when present and non-empty, else `uuid`.** An empty-string `requestId` counts as absent.
6. **`uuid` also missing or empty → hard fail.** With no key at all the record cannot be deduped, and counting it would risk exactly the double-count dedupe exists to prevent.

So qa's no-`requestId` fixture case: a **non-synthetic** record with no `requestId` is *kept* and keyed on `uuid`. In today's real corpus the only keyless records are the 8 synthetic ones, dropped at step 3 — the fallback is unexercised there and lives for the fixture and for future shape drift.

### On implementation-lead's branch-case finding

Confirmed and already ruled: §4's regex is case-insensitive and names those same five branches (`feature/PT-47`, `-48`, `-49`, `-51`, `-53`). Their read of the dotted milestone branches and of `phase/*` / `feature/cairn-engine` / bare `main` falling to `main` matches §4 exactly. No change.

### @architect — 2026-09-03

**Amendment — `window_end` is data-derived. §2's sentence is struck.** My ruling contradicted itself; this is the tie-break. Only `window_end` changes.

### The ruling

**`window_end` = the latest record date the contribution actually folded in (UTC `YYYY-MM-DD`). `window_start` = the earliest, unchanged.** §1 line 82 stands; the §2 sentence "The backfill sets `window_end` to its `generated` date" is **struck**.

**Rationale — `generated` already records the scrape moment.** If `window_end` were always `generated`'s date, the field would carry no information the line doesn't already have. The only reading under which `window_end` earns its place in the schema is the data-derived one: *this is the span the counts cover*, which is checkable against the data and is the claim PT-79's chart needs.

**Scope: contribution-wide, not per-bucket.** One `window_start` / `window_end` pair computed across the whole run, written identically on every line of that run. — these fields describe the *contribution*, not the bucket; per-bucket min/max would be a different claim (an issue's first/last activity date) that nothing in PT-77/78/79 asked for, and a reader could not tell the two apart by looking.

### Consequence for the non-overlap invariant (§2)

**The invariant keys on `generated`, not on `window_end`.** Restated: *PT-78's receiver must never emit data timestamped before the `generated` of the latest `transcript-backfill` line in the file.*

- `generated` ≥ `window_end` always, so this guard is strictly the more conservative of the two and is the one that is actually sound. The backfill scanned up to `generated`; it merely *found* nothing after `window_end`. The scanned boundary is what makes a later source safe, and that boundary is `generated`.
- Nothing is lost by the change: any interval between `window_end` and `generated` contains no records by construction, so honouring `generated` cannot double-count and cannot under-count.
- The lead's read is right that the invariant holds under either reading; it holds *for a reason* only under `generated`, so that is what PT-78's brief should cite.

### For qa and implementation-lead

- implementation-lead's draft (max observed record date) is correct as written, provided the min/max are computed **once per run** and repeated on every line.
- qa's assertion should change from `generated`'s date to the fixture's latest record date. On a fixture whose newest record predates the run — which any static fixture is — those two values differ, so the test is a real discriminator rather than an incidental pass.
- Worth one fixture: a run whose newest record is well before `generated`, asserting `window_end` tracks the record and not the clock. That is exactly the case the struck sentence would have got wrong.

### @architect — 2026-09-03

**Review of `backfill_tokens.py` @ `99725d0` — one blocking delta, three minor.** Reviewed by construction against synthetic fixtures in `/tmp`, not by reading the diff. The worktree blob is byte-identical to the blob at `99725d0` (`b8bec80`), so the tree file *is* the commit object; no branch was switched and nothing in the repo was written.

### BLOCKING — the tracker prefix is resolved from the current working directory

`main` calls `cairn.load_config(cairn.find_data_dir())`. `find_data_dir()` walks up from **`Path.cwd()`**, not from the `repo_root` the script has already computed four lines earlier. Run the script from anywhere outside the repo and it finds no `config.yml` — and `load_config` **returns the built-in default `prefix: "ISS"` instead of raising**. Every `feature/pt-NN-*` branch then fails to match, and **all 69 issue buckets collapse silently into `main`**, exit 0, with a cheerful `wrote ...` line.

Constructed and observed:

- fixture run with cwd = repo root → `['PT-7', 'PT-47', 'PT-77', 'main']`
- same script, same fixtures, cwd = `/tmp` → `['main']`, exit 0

This is exactly AC4's "fails loudly … never silently under-counts", and worse than an under-count: it is a total mis-attribution that looks like a successful run. **The committed data file at `a4f702c` is unaffected** — it has 69 distinct issues and no `PT-0`, so it was run from the repo root — so this is a latent trap for any re-run, not corrupt data.

**Required fix** (prototyped in an isolated fake repo, all four legs verified):

```python
env = os.environ.get("CAIRN_DATA_DIR")
data_dir = Path(env).resolve() if env else (repo_root / "process" / "cairn")
if not (data_dir / "config.yml").exists():
    raise cairn.CairnError(f"no config.yml at {data_dir}")
prefix = cairn.load_config(data_dir)["prefix"]
```

Anchoring to `repo_root` removes the CWD dependence; the explicit `config.yml` existence check is the load-bearing half, because `load_config`'s silent default is the actual trap. Verified after patching: cwd `/tmp` → `['PT-7','PT-47','PT-77','main']`; `CAIRN_DATA_DIR` seam still overrides (prefix `ZZ` → `['ZZ-5','main']`); missing `config.yml` → exit 1 with a named path instead of `ISS`.

**Required test:** run the script with `cwd` set outside the repo and assert issue buckets still appear. A test that only ever runs from the repo root cannot catch this — qa's 35 tests didn't.

### Minor 1 — my ruling was wrong, not the code: "byte-for-byte" is unachievable and unnecessary

§2 promised foreign (`otel`) lines "survive byte-for-byte". The implementation parses and re-emits them, so a foreign line written with compact separators comes back with `json.dumps` defaults. **Amending the ruling rather than the code:** foreign lines are preserved **semantically** — every key, its order, and its value are unchanged, and no field is added or dropped (verified, including an unknown `cost_usd` key surviving a regeneration). The file's canonical serialisation is `json.dumps` defaults, one object per line; **PT-78 should write that same form** so regeneration produces no diff churn. Idempotency verified independently: two consecutive runs over a file holding an otel line produce identical output ignoring `generated`, with the otel line present exactly once.

### Minor 2 — a malformed foreign line crashes with a traceback instead of a clean error

An `otel` line missing `role` (or `model`) raises `KeyError` out of `_sort_key`, printing a Python traceback. Exit is 1, the lock **is** released, and the atomic write means nothing is corrupted — so this is cosmetics, not correctness. Worth folding into `_read_existing_lines`: validate that each existing line has `source`, `issue`, `role`, `model` and raise `BackfillError` naming the file and line number, consistent with every other failure path.

### Minor 3 — TRACKER note is accurate but omits the approximation

The note correctly states what the file is, that PT-77 wrote it once, and that PT-78 owns the ongoing path; I verified its claim that no loader or `cairn check` scans it. Two things §4 and the "Notes carried forward" asked to appear there are missing: **multi-issue branches attribute wholly to the first id** (`feature/pt-7-8-9-13-*` → PT-7), and **the data starts at 2026-08-18 because Claude Code's retention had already pruned everything earlier** — PT-79 must not present the chart as complete history. Both are one sentence each.

### Verified correct — no action

Validation precedes dedupe (a record missing `gitBranch` fails the run even when its valid same-`requestId` twin was already accepted, and no output file is created) · `<synthetic>` dropped before dedupe · a non-synthetic record with no `requestId` is kept and keyed on `uuid` · duplicate `requestId` across two files counted once · nested `<session>/subagents/*.jsonl` scanned · `feature/PT-47` → PT-47 · `qa-engineer-76` → `qa-engineer`, `implementation-lead-2` → `implementation-lead`, `impl2`/`guide-pt18` verbatim with a stderr note · `chore/pt-0.11-token-accounting` → `main`, no `PT-0` bucket · truncated **final** line warns and continues, malformed **non-final** line exits 1 · missing transcripts dir exits 1 · `--dry-run` writes nothing, exits 0 · lock held by another writer exits 1 and writes nothing, a >60s lock is cleared, and the lock is released even on an uncaught crash · sort order and the 14-key schema are exactly as ruled · `window_end` tracks the latest record date, not the clock (fixture newest record 2026-08-20 vs `generated` 2026-09-04) · **no transcript content reaches the output** — a fixture whose every record carried a marker string in `message.content` produced zero occurrences in the data file.
