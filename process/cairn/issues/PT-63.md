---
id: PT-63
title: Board: self-hosted fonts (board/vendor/ + NOTICE.md, latin+latin-ext, ~245KB) per PT-57 ruling
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: null
created: 2026-08-27
updated: 2026-08-27
---


Pre-decided in the PT-57 token-delivery ruling (4785853), location
superseded by the addendum (781e000): the board gets the three spec faces
self-hosted under the existing `scripts/cairn/board/vendor/` precedent
with `NOTICE.md` provenance rows (SIL OFL) — latin + latin-ext subsets,
six woff2, ~245KB. Cross-mounting the
dashboard's `/dashboard/assets/*.woff2` is PERMANENTLY REJECTED (couples
the board's rendering to the dashboard's build artifact). Out of PT-57's
scope by ruling: that AC is tokens-only; fonts are a payload change.

## Comments

### @team-lead — 2026-08-27

Plan refined by the PT-57 addendum (781e000): fonts land under the existing `scripts/cairn/board/vendor/` precedent — checked-in third-party binaries with `NOTICE.md` provenance (library/version/license/upstream). The three faces are SIL OFL; provenance treatment required. Supersedes this issue's original `board/fonts/` location.

### @team-lead — 2026-08-27

Feature started. Branch: `feature/pt-63-board-fonts`. Pre-decided mechanical per the PT-57 ruling + addendum (vendor/ + NOTICE.md, latin+latin-ext, ~245KB, cross-mounting dashboard dist PERMANENTLY REJECTED) — no ruling gate; architect reviews the diff. Standing merge authorization applies.

### @implementation-lead — 2026-08-27

Implemented per the pre-decided plan, no deviations. Single commit: `0b377dd`.

Six woff2 files (256KB, within the ~245KB target) copied from the dashboard's
own installed `@fontsource-variable` packages at 5.3.0 (the exact version
the PT-57 ruling measured) into `scripts/cairn/board/vendor/fonts/` —
sourced by copy, not re-download, since those packages already passed
npm's own registry-integrity check at install time. `NOTICE.md` extended
with provenance rows + a new "Fonts" section (subsetting rationale,
font-specific re-vendor recipe) + updated checksums, without disturbing
the existing marked.js/purify.min.js rows.

`@font-face` declarations (verbatim from each package's own generated CSS,
unicode-range/weight-range intact, only the `src` repointed at
`/board/vendor/fonts/`) added to `board/tokens.css`. `board.css`'s
font-family declarations switched to the three tokens by role: body/UI
stays `var(--font-sans)`, the wordmark + eyebrow labels + card/dialog
titles + section headers get `var(--font-heading)`, every id/code
monospace site switches from a hardcoded stack to `var(--font-mono)`.
Cross-mounting the dashboard's build output was never on the table.

Same commit: retired the dashboard Board-panel's now-false seam note
from both `App.svelte` and the rebuilt `dist/` bundle.

Turns qa's `test_board_fonts.py` green (14/14). Also updated two of qa's
*pre-existing* tests to reflect this issue's own anticipated evolution —
`test_board_tokens_parity.py`'s font-var assertion (PT-57's negative pin
flips to PT-63's positive one, exactly as that test's own comment
predicted) and `board-css-badge-variants.test.js`'s `.chip.repo`
monospace check (now expects `var(--font-mono)`, since PT-63
deliberately replaced every hardcoded mono stack in board.css, this one
included — the monospace *distinction* PT-57's "must NOT migrate" list
protected is intact, just delivered through the token now).

Full suite 773 py / 384 JS, `cairn check` clean, `svelte-check` clean.
Verified both `board.css` and `board/tokens.css` parse cleanly with
`postcss.parse()` *before* calling this done — applying the PT-57 lesson
proactively this time rather than after a Validate FAIL. Verified against
a live `cairn serve`: every vendored woff2 serves 200 with the correct
`font/woff2` Content-Type, `/board/tokens.css` and `/dashboard` both 200.

No browser regression pass performed — browser automation unavailable
this session, same caveat as PT-57. Recommend a visual check (do the
three faces actually render, does the wordmark/headings switch look
right) before this closes out, not just the automated coverage above.

### @team-lead — 2026-08-27

**Validate browser pass (2026-08-28) — PASS.**

Verified at code HEAD b196c74, live server: all three faces genuinely LOADED (document.fonts.check true for Merriweather/Space Grotesk/Geist Mono Variable — loaded status, not just requested families) and rendered by role exactly per spec — body: Merriweather; wordmark/column headers/card titles: Space Grotesk; card IDs: Geist Mono. Screenshot on record: the board's typography now visibly matches the dashboard. The latin-ext siblings show "unloaded" pending glyph demand — normal. Suites 773/773 py + 384/384 JS on my own run. Awaiting architect approve + qa pin, then merge under standing authorization.
