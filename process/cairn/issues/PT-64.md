---
id: PT-64
title: Board: smallest text (11-12px) renders in serif — scope to Space Grotesk or bump size (a11y)
status: done
milestone: PT-0.8
parent: null
blocked_by: []
assignee: null
labels: []
priority: P3
pr: https://github.com/richmosko/project_template/pull/134
created: 2026-08-27
updated: 2026-08-28
---


From the architect's PT-63 review (e7299f5): with fonts landed, ~11 board
selectors at 11-12px (chips, drawer/filter labels) render in Merriweather —
the serif — which is the exact "badge/chip labels and small form labels"
risk the design spec's Accessibility section names, at sizes below what
the spec described.

**Options (Mosko's call — design decision):**
- (a) Scope the smallest components to `--font-heading` (Space Grotesk) —
  cheapest (~11 selectors); face-is-by-role arguably makes labels sans
  anyway. Architect-recommended; aligns with the spec's own
  "keep the smallest text out of the serif" recommendation.
- (b) Bump 11-12px to 14px per the spec's literal alternative — changes
  board density materially.
- (c) Accept and record on localhost-dev-tool grounds.

**Acceptance criteria** (assuming (a); revise if (b)/(c) chosen)
- [x] The sub-13px selector set renders in a sans face (or the chosen
      alternative), enumerated mechanically (every selector under 13px)
- [x] Guard pinning the face-floor rule so future small text can't
      silently go serif
- [x] Spec's Accessibility section updated to record the resolution

## Comments

### @team-lead — 2026-08-27

Observation pass (browser, 2026-08-28), converting the finding from spec citation to measurement:
- `.chip` computes to **Space Grotesk at 11px** — chips are ALREADY sans (the PT-57 badge migration carried a face along), so the finding's headline case is moot in computed reality. The architect's enumeration was of rule-level inheritance; computed style disagrees for chips.
- The genuine serif-at-small set is the 12px form labels (`.filters label` = Merriweather 12px, drawer labels likewise).
- Zoomed screenshot on record: at 12px the serif labels are legible on a modern display — softer than the sans, not broken. Evidence leans (c)-tolerable; (a) remains cheap and would make labels consistent with the dashboard's sans eyebrow treatment.
Scope if (a) is chosen is smaller than filed: the label selectors only, not eleven.

### @team-lead — 2026-08-28

**Ruled by Mosko (2026-08-28): option (a)** — scope the smallest text to Space Grotesk (`--font-heading`). Scope per the observation pass: the 12px form-label selectors only (`.filters label`, `.drawer-field label`, and any sibling label rules found by the mechanical sub-13px enumeration) — chips are already sans in computed reality. AC as filed applies with that narrowed set: the face-floor guard pins the rule, and the spec's Accessibility section records the resolution. Implementation next session (post-PT-60 pause).

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-64-small-text-sans`. Executing the 2026-08-28 ruling: option (a), narrowed scope — 12px form-label selectors to `--font-heading`, mechanical sub-13px enumeration, face-floor guard, spec Accessibility note.

### @team-lead — 2026-08-28

**Observation-pass correction (live computed-style read, 2026-08-28, current tree).** qa-engineer's cascade trace flagged that `.chip` has no font-family rule, contradicting my earlier "chips are ALREADY sans" note. Browser verification resolves it: the face is **placement-dependent inheritance** —
- chips inside `.swimlane-header` (status/progress/release) inherit the header's `--font-heading` → Space Grotesk 11px (what my original pass sampled);
- chips inside `.card-meta` (milestone/assignee, the bulk of board chips) inherit body serif → **Merriweather 11px**.

The ruling's chip exemption rested on a sampling artifact. Card chips are serif at 11px — smaller than the label set the (a) fix covers. Scope question re-opened to Mosko.

### @team-lead — 2026-08-28

**Ruled by Mosko (2026-08-28): extend option (a) to `.chip`.** Follows the corrected observation (card chips serif at 11px via placement-dependent inheritance). Delta: `.chip` → `font-family: var(--font-heading)` (`.chip.repo` keeps its mono override); qa's guard broadens from label-only to the full mechanical sub-13px enumeration; spec's Accessibility note updated to close the badge/chip half of the flag. Rides the same branch/PR.

### @team-lead — 2026-08-28

**Ruled by Mosko (2026-08-28): full face floor.** Supersedes the chip-only delta: EVERY sub-13px board selector goes to `--font-heading` (Space Grotesk) — the chip family plus the ~10 further genuine violations qa's mechanical enumeration surfaced (drawer links, `.comment-meta`, `.connection-state`, `table.issue-list th`, `.drawer-progress`, `.record-readonly-note`, etc.). The two UA-default-Arial buttons (`.major-tab-open`, `.view-state-btn`) get styled into the system in the same pass. Exemptions, encoded deliberately in the guard: `--font-mono` faces pass (mono is not the serif risk); `font-family: inherit` inside a sans-scoped container passes where the guard can prove it, else the rule gets an explicit face. Guard stays fully mechanical per the AC's literal wording. Live computed-style evidence on the issue thread (team-lead browser pass, 2026-08-28).

### @team-lead — 2026-08-28

PR opened. Awaiting Validate.

### @qa-engineer — 2026-08-28

Validate: confirmed PR #134 head (eedbfcf) matches what I checked locally
— no drift. Read the board.css diff selector-by-selector against my
enumerated 23-selector list: `.chip` base rule (single edit, covers the
whole family via cascade; `.chip.repo`'s mono override untouched), the
two `font-family: inherit` toggles swapped for explicit
`var(--font-heading)`, and all 10 remaining standalone selectors each got
the declaration on their own rule. One correction to comment @ 2026-08-28
above: the guard does NOT pass `inherit` "where it can prove it" — it
requires an explicit face on every sub-13px selector unconditionally,
including `.swimlane-toggle`/`.repo-group-toggle` (which do compute
Space Grotesk today via their sans-scoped header, per team-lead's live
read, but the guard treats `inherit` as unresolved/failing regardless,
to stay placement-independent rather than repeat the sampling-trap shape
that produced the original chip exemption). Re-ran the guard (4/4 green)
and the full suite (810/810 green) myself from a clean shell against this
exact commit. All three acceptance criteria confirmed met — checking
them off. Merge approved.

### @team-lead — 2026-08-28

Validate passed; merging via PR #134. Closing. (QA's correction to my full-face-floor ruling comment stands: the guard requires an explicit face unconditionally — no provable-inherit carve-out.)
