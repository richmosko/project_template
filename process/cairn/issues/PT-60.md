---
id: PT-60
title: Dashboard polish: milestone_paths helper, single git-tags read per request, Skeleton at fetch boundaries, aria-live on loading
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: null
created: 2026-08-26
updated: 2026-08-28
---


Also fold in (architect, post-block-lift): one assertion in
LatestSemverTagTests pinning that a pre-release of a HIGHER line outranks
a lower final — `{"v2.0.0-rc.1", "v1.9.3"} → "v2.0.0-rc.1"`. Behavior
verified correct today by direct call; the assertion guards against a
future "simplification" demoting pre-releases globally instead of only
within their own version triple.

Also fold in (architect, PT-56 review): structured `work` in the roster
payload — `{id, title, status, kind}` with `kind ∈ current|stale|history` —
replacing the server-composed English strings the component re-parses with
`.split(':')`. Today "last shipped " is load-bearing framing that exists
only as prose; restructure when the panel next grows.

## Comments

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-60-dashboard-polish`. Four items from accumulated review debt: (1) milestone_paths(data_dir, include_archived) helper single-sourcing the live+archived milestone reads (now FOUR call sites incl. PT-59's lint); (2) single git-tags read per /api/dashboard request; (3) Skeleton components at the dashboard's fetch boundaries per the spec; (4) aria-live on the loading badge; plus the structured-roster-work debt folded in earlier (architect, PT-56 review) if it fits — implementer judgment, may split out. qa tests first; architect diff review; dist rule applies (dashboard src will change). Standing merge authorization.
