---
id: PT-60
title: Dashboard polish: milestone_paths helper, single git-tags read per request, Skeleton at fetch boundaries, aria-live on loading
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: null
created: 2026-08-26
updated: 2026-08-28
---


Also fold in (architect, post-block-lift): one assertion in
LatestSemverTagTests pinning that a pre-release of a HIGHER line outranks
a lower final — `{"v2.0.0-rc.1", "v1.9.3"} → "v2.0.0-rc.1"`. Behavior
verified correct today by direct call; the assertion guards against a
future "simplification" demoting pre-releases globally instead of only
within their own version triple.

Also fold in (architect, PT-56 review): structured `work` in the roster
payload — `{id, title, status, kind}` with `kind ∈ current|stale|history` —
replacing the server-composed English strings the component re-parses with
`.split(':')`. Today "last shipped " is load-bearing framing that exists
only as prose; restructure when the panel next grows.

## Comments

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-60-dashboard-polish`. Four items from accumulated review debt: (1) milestone_paths(data_dir, include_archived) helper single-sourcing the live+archived milestone reads (now FOUR call sites incl. PT-59's lint); (2) single git-tags read per /api/dashboard request; (3) Skeleton components at the dashboard's fetch boundaries per the spec; (4) aria-live on the loading badge; plus the structured-roster-work debt folded in earlier (architect, PT-56 review) if it fits — implementer judgment, may split out. qa tests first; architect diff review; dist rule applies (dashboard src will change). Standing merge authorization.

### @implementation-lead — 2026-08-28

All four items done, plus the LatestSemverTagTests fold-in (qa's test,
green with no code change needed). Split the optional structured-work
item out to PT-65 (implementer's judgment, per your "take it only if it
doesn't balloon the PR" latitude) -- it's a real payload-shape change
with its own test surface, not a same-PR fit.

- Item 1: milestone_paths(data_dir, include_archived) helper,
  single-sourcing the pattern across _find_release_milestone,
  build_board_payload, compute_etag. Pure refactor; didn't touch
  check_repo's loop structure, so the PT-59 mutation check wasn't
  re-run (not needed).
- Item 2: build_dashboard_payload now reads git tags exactly once per
  request, shared with read_git_state + build_board_payload via an
  optional pre-fetched (tag_set, warning) param. qa's spy test green.
- Item 3: shadcn-svelte Skeleton wired at status cards, tracker table,
  and agent roster (replacing the roster's bare "Loading…" text).
- Item 4: aria-live="polite" on the header's status-badge cluster.

Commits: b26c816 (items 1+2), ed5f5c1 (items 3+4 + dist rebuild).
One self-caught wrinkle: my first attempt over-staged dist into the
backend commit before App.svelte's own commit landed, which would have
made PT-58's freshness gate read this dist as stale (dist's last commit
predating source's last commit, even though the bytes were correct).
Caught by running check_dist_freshness.py myself before reporting;
fixed via `git reset --soft` back to qa's test commit and re-split
cleanly (both commits still local/unpushed/unreported at that point,
nobody had referenced the bad hashes) -- flagging transparently per
this team's standing practice on git history changes.

Suites: 802 Python + 384 JS, all green. svelte-check: 0 errors. cairn
check: clean. Dist-freshness gate: fresh.
