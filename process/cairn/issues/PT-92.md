---
id: PT-92
title: Ratify or replace the interim --chart-flow-opened/closed/wip tokens against the design system (ux-designer)
status: in-progress
milestone: PT-0.12.1
parent: null
blocked_by: []
assignee: ux-designer
labels: [dashboard]
priority: null
pr: null
created: 2026-09-05
updated: 2026-09-08
---


## Problem

PT-85 introduced three dedicated categorical tokens for the throughput chart —
`--chart-flow-opened`, `--chart-flow-closed`, `--chart-flow-wip` — with interim values taken
from the dataviz skill's validated categorical palette, chosen by implementation-lead under two
constraints only: mutually distinguishable in every theme variant, and not coinciding with a
`--chart-role-*` hue used by the token/cost chart on the same dashboard page. No design review.

## Ask

ux-designer ratifies or replaces the three values against the design system (docs/DESIGN,
tokens.css preset). Names and the per-variant distinguishability guard are the contract; values
may change. If replaced, update app.css and every variants.css entry, rebuild dist, and re-run the
guard.

## Acceptance criteria

1. A decision recorded on this issue: ratified as-is, or replaced with the new values and why.
2. If replaced: the guard test green in every variant, light and dark; dist rebuilt and committed.
3. Browser-visibility pass on the throughput chart in at least two variants (light + dark).

## Comments

### @board — 2026-09-05

Preference from human operator: the overlapping opened/closed/wip bars only show if wip > opened > closed. IE: they overlap on the chart. We should have these as overlapping chart lines instead so all 3 are always visible. lines should have different defining colors for visibility.

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-92-chart-flow-tokens-lines`. Merge pre-approved by Mosko (2026-09-08) on a passing verdict + qa approve-merge. **First loop under PT-82's worktree protocol:** ux-designer is a fresh spawn and works from its own worktree; architect / qa-engineer / implementation-lead finish this milestone on the shared tree.

**Decided lines (lead).** (1) Mosko's board comment (2026-09-05) is in scope and binding on the rendering: the throughput chart draws opened, closed and WIP as **three overlapping lines in distinct, defining colours** — today opened/closed are bars and WIP is a spline, so bars that overlap hide each other unless wip > opened > closed. (2) The token contract stays: names `--chart-flow-opened/closed/wip` and the per-variant distinguishability guard; **ux-designer is the design authority** on the three values (ratify or replace against docs/DESIGN and the tokens.css preset) and on the line treatment (weight, markers, legend); the architect frames the seam and tests. (3) Lines must be visibly distinct in every theme variant, light and dark — the existing guard (`test_flow_series_distinctness.py`) stays the mechanical check; the lead's browser leg covers AC3 on at least two variants.

**Gate 1 brief (architect).** Rule on (a) the seam in `IssueFlowChart.svelte` (~L229 Bars → line marks for all three series; keep the Day/Week + milestone controls and the Show-table path untouched); (b) how ux-designer's decision enters the loop under the worktree protocol: ux-designer commits its decision + any token changes to `app.css` / `variants.css` + a DESIGN note on this branch via pull-rebase/push (its worktree), before qa's red; (c) red tests — the distinctness guard re-run against the final values, a component-level scan that the three series are line marks not bars, caption/legend strings; (d) writers: ux-designer app.css/variants.css/docs/DESIGN; implementation-lead IssueFlowChart.svelte + rebuilt dist; qa the tests; (e) AC2's guard in every variant; (f) the lead's visual leg: two variants × light/dark, all three lines visible at once on the live data, colours distinguishable, no line hidden by another, elementFromPoint + screenshots, count every mark before any filter. ≤ 40 lines.
