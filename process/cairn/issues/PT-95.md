---
id: PT-95
title: tearDownModule guard in test_milestone_overhead.py uses a bare assert — stripped under python -O; raise explicitly
status: in-review
milestone: PT-0.12.1
parent: null
blocked_by: []
assignee: qa-engineer
labels: [cairn, telemetry]
priority: null
pr: https://github.com/richmosko/project_template/pull/177
created: 2026-09-05
updated: 2026-09-06
---


## Comments

### @team-lead — 2026-09-05

Found by the architect during the PT-91 gate-1 ruling (measured @93ba8ee): `test_milestone_overhead.py:710,716` — the module-level guard's `tearDownModule` uses a bare `assert`, which `python -O` strips: exit 1 without `-O`, exit 0 with `-O`. Fix: raise `AssertionError` explicitly (PT-91 lands the same shape for `test_otel_receiver_self_stop.py`). Assignee: qa-engineer.

### @team-lead — 2026-09-06

Feature started. Branch: `feature/pt-95-explicit-raise-guard`.

**Gate 1 ruling (team-lead — the claim is already measured on PT-91, ruling 5ccfbdd amendment 1 and verdict d70b25b).** Seam: `tearDownModule` in `scripts/cairn/tests/test_milestone_overhead.py` (lines 710 and 716 @806b535) replaces each bare `assert X, msg` with `if not X: raise AssertionError(msg)`; the `_git` helper's `assert result.returncode == 0` at line 253 gets the same treatment (a stripped assert there hides a failed git command). Proof for AC 1: one test that runs a generated module using the guard shape under `python3 -O` as a subprocess and asserts `returncode != 0` when the guarded state is clobbered — the same construction as PT-91's `GuardBracketDifferentialTests`, which may be reused by import rather than copied. Thresholds: no bare `assert` remains in `setUpModule`/`tearDownModule` (grep), and the guard fires with and without `-O` (exit codes, never output text). AC 2: module green, full suite green once at build-green, `cairn check` ok, real file/pidfile/registry untouched.

### @qa-engineer — 2026-09-06

fix @9028374. All three bare `assert` sites converted to `if not X: raise AssertionError(...)` (_git line ~254, tearDownModule lines ~711/717).

AC1 proof: ExplicitRaiseDifferentialTests (subprocess, exit codes only) -- bare-assert template: rc!=0 without -O, WRONGLY rc==0 under -O (file confirmed mutated); explicit-raise template: rc!=0 both with and without -O.

Red/green: added NoBareAssertInGuardFunctionsTests (AST-based, scoped to _git/tearDownModule) -- failed red pre-fix (3 offenders), green post-fix.

Module: 26 tests OK. Full suite: 1379 tests OK (skipped=1, pre-existing). cairn check: ok (unrelated PT-94 size warning only). Real token-usage.jsonl/.receiver.pid/.sessions untouched by this change.

### @team-lead — 2026-09-06

PR opened: https://github.com/richmosko/project_template/pull/177. Awaiting Validate. Lead re-verified against the tree @ae61905: module 26/26 with and without -O, full suite 1379 OK (1 skip), JS 458 pass, cairn check ok, real metrics files untouched.

### @qa-engineer — 2026-09-06

Validate verdict: APPROVE MERGE.

PR #177 diff for scripts/cairn/tests/test_milestone_overhead.py is byte-identical to 9028374 (confirmed via git diff against origin/feature/pt-95-explicit-raise-guard) -- no drift since build-green. Remaining diff (process/STATE.md, PT-92.md comment, token-usage.jsonl, PT-95.md tracker updates) is tracker/STATE bookkeeping, out of test scope.

Gates: build-green stands from 9028374 (module 26 tests OK, full suite 1379 tests OK skipped=1 pre-existing, cairn check ok, real files untouched). No further test run needed per team-lead's head-match confirmation.
