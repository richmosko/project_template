---
id: PT-107
title: Worktree teammates' narrowed (-p) runs record twice: hook line (who, feature branch) + runner line (who=null, worktree-* branch)
status: in-progress
milestone: PT-0.12.2
parent: null
blocked_by: [PT-82]
assignee: implementation-lead
labels: [cairn, tests, hooks]
priority: P3
pr: null
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Observed on PT-92 (the first loop with a worktree teammate, ux-designer) in `process/cairn/metrics/test-runs.jsonl`: each of ux-designer's narrowed `run_tests.py -p …` runs from `.claude/worktrees/pt92-ux` produced TWO records in the main ledger — `11:03:47 who=ux-designer gate=null branch=feature/pt-92-… files=2` (the PostToolUse hook's own append for a narrowed run) and `11:05:56 who=null gate=null branch=worktree-pt92-ux files=2` (the runner's self-record redirected to the main checkout by PT-82's git-common-dir rule, with `branch` resolved from the worktree's HEAD). PT-82's spike only exercised gated full runs, which record once (the runner writes, the hook patches `who`). Consequences: per-agent tool/run counts in `cairn loop-stats` double-count narrowed worktree runs, and the unattributed line carries a `worktree-*` branch name that nothing should ever see. Ask: one record per run for narrowed runs too — the hook should patch the runner's line (matching on ts/argv) rather than append when a runner record exists, and the runner should resolve `branch` from the main checkout's feature branch (or the linked worktree's upstream) never `worktree-*`; tests on a real `git worktree add` fixture for both the narrowed and gated cases. Not attached to PT-0.12.1 (closing).

### @team-lead — 2026-09-08

Root cause (architect, PT-92 gate 4): `run_tests.py:367` guards the worktree redirect with `if args.gate and not os.environ.get(...)`. The `args.gate` clause is a leftover from the superseded PT-82 ruling; the re-issued ruling @69e9664 specified the `--git-dir != --git-common-dir` discriminator with no `--gate` condition, because the discriminator is itself the safety property (returns `None` in a main checkout and in a nested fake root). Fix: delete `args.gate and` from that condition, plus a test that a narrowed `-p` run inside a linked worktree records to the main checkout. Not an investigation.

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-107-worktree-run-double-record`.

**Gate 1 brief (architect):** root cause is recorded above — `run_tests.py:367` guards the worktree redirect with `args.gate and`, a leftover from the superseded PT-82 ruling; delete the clause so narrowed `-p` runs from a linked worktree also record to the main checkout (the `--git-dir != --git-common-dir` discriminator is the safety property). Second half of the original ask: with the redirect fixed, does the PostToolUse hook still append its own line for a narrowed worktree run (giving two records), and does the runner's line carry `branch=worktree-*`? Rule on both: one record per run, `branch` resolved to the feature branch (worktree upstream), `who` patched by the hook. qa: a real `git worktree add` fixture, narrowed and gated cases, asserting exactly one record with the right branch. Writers: qa → tests; implementation-lead → `run_tests.py` (+ hook files if the ruling says so).

### @architect — 2026-09-08

**Ruling (gate 1).** Delete `args.gate and` from `run_tests.py:367`. That single clause is the whole fix — **no hook change**. Measured on a real `git worktree add` fixture (main checkout on `feature/pt-107-x`, linked worktree on `worktree-x`, `CLAUDE_PROJECT_DIR`/`CAIRN_TEST_RUNS_FILE` unset), running the narrowed case before and after the deletion, then driving the real `.claude/hooks/test_run_record.py` with `CLAUDE_PROJECT_DIR=<main checkout>`:

| case | main checkout's ledger | worktree's tracked copy |
|---|---|---|
| current, `-p` (runner only) | no file | 1 line, `who=null`, `branch=worktree-x` |
| current, `-p` + hook | 1 line, `who=architect`, `branch=feature/pt-107-x` (hook **scraped and appended**) | 1 line, `branch=worktree-x` |
| patched, `-p` (runner only) | 1 line, `who=null`, `branch=feature/pt-107-x` | no file |
| patched, `-p` + hook | **1 line**, `who=architect`, `branch=feature/pt-107-x` (hook **patched**, no append) | no file |
| patched, `--gate red` | 1 line, `gate=red`, `full=true`, `branch=feature/pt-107-x` | no file |

Both halves of the ask fall out of the one deletion. (a) One record per run: with the redirect live, the runner's `who: null` line is the last line of the main ledger, so `_patch_last_who` fills it and returns before the scrape-and-append fallback — the second record existed only because there was nothing to patch. (b) `branch` is never `worktree-*`: `sha`/`branch` are already resolved with `_run_git(records_repo_root, …)`, and `records_repo_root` becomes the main checkout under the redirect. No upstream lookup — a fresh `EnterWorktree` branch has no upstream at all, so upstream resolution would fail exactly where it is needed.

**Ruled, not open:** `sha` and `branch` both stay resolved from the main checkout. They must come from one root: a record naming a `sha` that is not on its own `branch` is worse than a `sha` a few commits behind the worktree that ran. Accepted and to be stated in the `_self_record` docstring.

**Also update the docstring**, same commit: its PT-82 paragraph says the redirect applies "ONLY when `--gate` is present". That sentence is the superseded ruling's text and must not outlive the clause it describes — replace with the `--git-dir != --git-common-dir` discriminator alone (unchanged, still the safety property).

**Guards (qa, gate 2)** — reuse `_make_main_checkout_with_worktree` in `test_worktree_metrics_path_resolution.py`; real git, real subprocesses, no stand-ins:
1. Narrowed `-p` run from the linked worktree → exactly **one** line in the main checkout's file, `full=false`, `gate=null`, `branch` == the main checkout's branch; the worktree's own copy must not exist. (Mutation: restore `args.gate and` → main has no file and the worktree copy gains a line.)
2. No record, in either file, carries a `worktree-`-prefixed `branch` — a standing invariant, cheap, catches any future re-resolution from the worktree root.
3. Hook leg (this is the "one record per run" property; test 1 alone does not prove it): after that narrowed run, drive the real `.claude/hooks/test_run_record.py` on stdin with `CLAUDE_PROJECT_DIR=<fixture main checkout>` and a `Ran N tests …`/`OK` stdout → still exactly one line, `who` now filled. (Mutation: hook appends instead of patching → 2 lines.) Recipe measured above; it needs no copy of the hook.
4. The four discriminator contexts already in that file stay green **and unedited** — they are the safety property, not scaffolding.

**Scope** — `run_tests.py` (one clause + its docstring) and `test_worktree_metrics_path_resolution.py`. No hook file, no `loop_stats.py`.

**Known gap, not this issue:** `_patch_last_who` only ever inspects the ledger's last line, so two teammates running concurrently — likelier now that worktrees are the norm — can cross-attribute `who`, and a stale `who: null` line gets adopted by the next run's hook. Pre-existing, documented in the hook, unchanged here. Worth a P3 follow-up; the lead's call.

### @architect — 2026-09-08

**Addendum 1 to the gate-1 ruling (PT-107).** Objection sustained on the fact, resolved as **(a): qa fixes the fixture.** Writer: **qa**, in `test_worktree_metrics_path_resolution.py`. implementation-lead-2's change stays the clause deletion + docstring — the ruling's "no hook change" holds.

Measured, not assumed (265 records in `process/cairn/metrics/test-runs.jsonl`, split by writer):
- hook-scraped records that name `run_tests.py`: **4/4 carry the literal `python3`**; none carry a versioned interpreter.
- runner self-records: **247/247 carry `/opt/homebrew/.../python3.14`** — that is `sys.executable` in the runner's own `cmd` field, which the hook never reads.

So the versioned path is real, but it lives in the field the hook does not consume. The hook's input is the teammate's typed Bash command, and that is literal `python3`. The fixture is unfaithful to the hook's actual input; the test is wrong, not the code.

Correction to how (b) was framed: absolute paths **already** pass — `_is_python_token` compares the basename, so `/usr/bin/python3` → True. Only a version-suffixed basename (`python3.14`) fails. (b) is therefore narrower than "versioned/absolute", and it widens `is_test_invocation`, which the PreToolUse guard `test_run_guard.py` shares — widening detection makes the guard refuse more commands. That is a behaviour change on a gate-enforcement surface, unguarded by this issue's tests. Not here.

**Added guard (qa, part of gate 2):** before driving the hook, assert `_test_run_shared.is_test_invocation(<payload command>)` is True. The subprocess that actually runs the tests may keep using `sys.executable` — that is the spawn, not the payload. Without this assertion a future fixture drift fails as a silent no-op with a null `who`, which is exactly the shape that cost this loop a gate.

**Follow-up candidate (P3, lead's call, not this issue):** a real run invoked as `python3.14 run_tests.py` records nothing at all — the hook no-ops and the runner's own line keeps `who: null`.

### @architect — 2026-09-08

**Verdict (gate 4): go on PT-107's own axes — with one failing check that is not PT-107's, reported below and NOT silently absorbed.** Verified against commit objects, not the checkout.

| axis | result | evidence |
|---|---|---|
| `args.gate and` deleted, discriminator alone left standing | pass | one-clause diff, `if not os.environ.get(_RECORDS_PATH_ENV):` — 085cdb9 |
| docstring's "ONLY when `--gate` is present" removed | pass | same commit, same hunk — 085cdb9 |
| guard 1: narrowed `-p` run from a real worktree records once in the main checkout, `full=false`, `gate=null`, `branch` == main's branch, worktree copy absent | pass | `NarrowedWorktreeRunRecordsOnceInTheMainCheckoutTests` — 5b48def, green at 085cdb9 |
| guard 2: no record in either file carries a `worktree-` branch (narrowed **and** gated) | pass | `NoRecordCarriesAWorktreePrefixedBranchTests` — 5b48def |
| guard 3: hook patches `who` in place, no second record | pass | `HookPatchesTheNarrowedWorktreeRunsRecordTests` — 5b48def, fixture corrected fb6c4ce |
| addendum guard: payload command asserted `is_test_invocation` before driving the hook | pass | `shared.is_test_invocation(command)` precondition, literal `python3` — fb6c4ce |
| guard 4: the four discriminator contexts unedited | pass | both test commits are insert-only in that file (`--stat`: 149 +/0 −, then 28 +/2 − confined to the new class + imports) |
| scope held | pass | `run_tests.py` + `test_worktree_metrics_path_resolution.py` only; no hook file touched |
| suite, serial | pass | `run_tests.py --gate verdict --serial`: 1535 tests, 126.0 s — OK (skipped=1) |
| suite, parallel | **FAILED** | same command, 8 workers, 25.2 s: 1535 tests, **1 failure in `test_otel_receiver_self_stop.py`** |

**Not ruled a flake — ruled not attributable, on the mechanism.** PT-107's diff changes exactly one thing: which repo root `_self_record` resolves for `test-runs.jsonl`. That module's real-file guard watches `token-usage.jsonl`, `.receiver.pid` and `.sessions/` — `test-runs.jsonl` is not among them, so the diff has no path into it. Measured after the failure, no batteries: the module alone → 34 tests OK; the module paired with the new worktree tests (the only plausible cross-file writer) → 44 tests OK; serial full suite → OK. It reproduces only under 8-worker load, in a file whose waits PT-98 deliberately tightened (18.9 → 16 s, ec8545f) — a daemon/socket/timing file, the shape that has produced load-sensitive failures here before.

**What I did not do:** capture the failure's own text. The runner's summary was read through `tail`, which ate the assertion. That is the one thing needed to close this, and the next full run at any gate should be read whole (or via `--json`) rather than tailed. Owner: qa, as a follow-up on the suite — **not** a PT-107 blocker, and explicitly the lead's call whether to merge ahead of it.

**Residual from my own ruling, not landed:** the ruling asked that the accepted `sha`/`branch` pairing (both resolved from the main checkout, deliberately, so a record's `sha` is always on its own `branch`) be stated in the `_self_record` docstring. 085cdb9 removes the stale sentence but adds no replacement. One line, in implementation-lead's file, not mine to write post-green — fold it into the next touch of that function or drop it deliberately.
