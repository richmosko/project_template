---
id: PT-108
title: loop-stats crashes on a structured SendMessage body (dict) in the transcript audit
status: in-progress
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: null
pr: null
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Found on PT-92 finish-feature pre-flight (2026-09-08). `scripts/cairn/cairn loop-stats PT-92` raises `TypeError: expected string or bytes-like object, got 'dict'` at `loop_stats.py:344` (`_CONFIRM_RE.search(body)`). Cause: `audit_agent` reads `inp.get("message")` from SendMessage tool calls and assumes a string; the shutdown/plan-approval protocol messages carry a JSON object (`{"type":"shutdown_response",...}`), so any loop whose window contains a team teardown crashes the scorecard.

Fix: coerce a non-string body (`json.dumps`) or skip protocol messages entirely (they are not prose and should not count toward CONFIRM_ROUNDTRIP / long-message flags); one regression test with a dict-bodied SendMessage record in a fixture transcript. Workaround used on PT-92: `--transcripts-dir <empty dir>` (scorecard without the transcript cross-check).

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-108-loop-stats-dict-body`. Drive: Mosko 2026-09-08 — all PT-0.12.2 issues, merges pre-approved, work time recorded per issue, exec summary at the end.

**Gate 1 brief (architect):** `loop_stats.audit_agent` (`loop_stats.py:344`) assumes SendMessage `message` is a string; protocol messages (`shutdown_request`/`shutdown_response`/`plan_approval_*`) carry a JSON object. Decide: coerce (`json.dumps`) vs skip protocol messages (they are not prose — should not count toward CONFIRM_ROUNDTRIP / long-message / msgs_to_lead). Recommendation: skip when `isinstance(body, dict)` and `body.get("type")` is a protocol type; coerce anything else non-string. One regression test with a dict-bodied SendMessage record in a fixture transcript, asserting no exception and no message-flag rows for it. Writers: qa → test file; implementation-lead → `loop_stats.py`.
