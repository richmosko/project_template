---
id: PT-81
title: OTel receiver hardening: telemetry gate, bind verification, port/endpoint agreement
status: in-review
milestone: PT-0.11
parent: null
blocked_by: []
assignee: architect
labels: [cairn, telemetry]
priority: null
pr: https://github.com/richmosko/project_template/pull/160
created: 2026-09-03
updated: 2026-09-03
---


## Problem

Three receiver hazards found by the architect on 2026-09-04 while writing the v0.11.0 migration
note, each verified by construction with two fake project roots. One of them produced a real
contamination during PT-79: a payload POSTed from an unrelated directory was accepted and written
into this repo's committed `token-usage.jsonl` as a PT-79 row (removed before commit).

- **H1** The SessionStart hook starts the receiver even when telemetry is off. `ensure_running`
  gates only on `config.yml`, so every downstream cairn project gets a bound port and an idle
  daemon it never opted into.
- **H2** A second project on the same port dies silently. `ensure_running` exits 0 while its child
  fails with `Address already in use`; no pidfile, no listener, the only trace in a gitignored log.
- **H3** The receiver's port (`otel_port` in `config.yml`) and the exporter's destination
  (`OTEL_EXPORTER_OTLP_ENDPOINT`) are configured independently and nothing checks they agree. A
  mismatch does not lose telemetry; it delivers it to whatever else is listening, which writes
  rows into its own data file under its own branch.

## Rulings (architect, 2026-09-04; lead adjustment on H3)

1. `ensure_running` requires `CLAUDE_CODE_ENABLE_TELEMETRY` truthy in its environment; otherwise
   it returns False, spawns nothing, exits 0 quietly. The settings `env` block reaches hook-spawned
   subprocesses, so one block controls exporter and receiver.
2. `ensure_running` verifies the child bound. On failure it distinguishes "port held by another
   process" from "spawn failed", prints a message naming the port and `otel_port`, returns False.
   The hook still exits 0 but stops redirecting stderr to /dev/null. Add `--status` (running?,
   port, out-file).
3. `otel_port` in `config.yml` is the single source of truth. At start the receiver compares it with
   the port in its own inherited `OTEL_EXPORTER_OTLP_ENDPOINT` (not by parsing settings.json —
   telemetry env may legitimately live in settings.local.json or the shell). If the variable is set
   and the ports disagree, the receiver refuses to start, naming both. If unset, H1 already
   declined. `/setup-tracker` writes both from the single `otel_port` answer.

## Acceptance criteria

1. Tests with two fake project roots (each with `process/cairn/config.yml` + engine scripts):
   telemetry off → no process, exit 0; second project on a held port → return False, message names
   the port, exit 0 from the hook line; endpoint/port mismatch → refusal naming both; match → start.
2. `--status` output covered by a test.
3. Hook line in `.claude/settings.json` keeps stderr; `TRACKER.md` documents the three behaviours
   and `/setup-tracker` writes `otel_port` and the endpoint from one answer.
4. Full suite green; `cairn check` ok; committed data file byte-identical to a4f702c.

## Comments

### @team-lead — 2026-09-03

Feature started. Branch: `feature/pt-81-receiver-hardening`. Rulings H1–H3 are in the body (architect, with the lead's H3 adjustment: compare against the receiver's own inherited OTEL_EXPORTER_OTLP_ENDPOINT, not settings.json). Last loop before v0.11.0.

### @architect — 2026-09-03

**On the lead's H3 adjustment: agreed on the mechanism, one branch I'd change.**

**Agreed, and it is better than what I ruled.** Comparing `otel_port` against the receiver's **own inherited `OTEL_EXPORTER_OTLP_ENDPOINT`** beats parsing `.claude/settings.json`, for a reason my own H3 missed: `process/TRACKER.md` says the engine "touches no project file outside `process/cairn/`", and having `otel_receiver.py` read `.claude/settings.json` would put it outside that boundary for no gain. The env approach also works wherever the value legitimately comes from — `settings.json`, `settings.local.json`, a shell export, CI — which is exactly the case the file-parsing version would have got wrong.

**The one branch I'd change: `OTEL_EXPORTER_OTLP_ENDPOINT` unset is not "nothing to compare".** When it is unset, a conforming exporter does not stop exporting — it falls back to the OTLP default, **`http://localhost:4318`** (OpenTelemetry protocol exporter spec). So a project that sets `otel_port: 4319` in `config.yml` and enables telemetry **without** setting the endpoint gets a receiver bound to 4319 and an exporter posting to 4318 — H3's exact silent mismatch, waved through by the "nothing to compare" branch. H1 does not cover this: telemetry *is* enabled, so H1 has already let the receiver start.

**Proposed rule:** when `OTEL_EXPORTER_OTLP_ENDPOINT` is unset, compare `otel_port` against the OTLP default `4318` rather than skipping the check. Equal → start. Different → refuse, naming both and saying the endpoint is unset so the default applies. Everything else in the adjustment stands unchanged.

**Verify this in the loop rather than taking it from me.** I confirmed `http://localhost:4318` is the documented default in the OTel spec, and PT-78 established that Claude Code uses the standard `OTEL_EXPORTER_OTLP_*` variables — but I did **not** confirm empirically that Claude Code falls back to it, because a receiver is currently listening on 4318 for this repo and sending a probe there is how I contaminated `token-usage.jsonl` on 2026-09-04. Stop the receiver first, then run one `claude -p` with telemetry on and the endpoint unset against a scratch sink on 4318. If Claude Code does *not* fall back to the default, the lead's original branch is correct and this comment is moot.

**Incidental corroboration for H1, observable right now:** `.claude/settings.json` contains no `CLAUDE_CODE_ENABLE_TELEMETRY`, and a receiver process is nonetheless running and holding port 4318. That is H1's symptom in the live tree, not a hypothetical.

### @team-lead — 2026-09-03

PR opened: https://github.com/richmosko/project_template/pull/160. Fallback to 4318 confirmed empirically by implementation-lead; awaiting architect review and QA validate from f61c3e5 (+ qa's unset-branch test).
