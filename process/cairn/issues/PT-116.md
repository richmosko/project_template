---
id: PT-116
title: Test-run ledger: a failing (red) narrowed run writes no record — red runs are invisible to loop-stats
status: in-review
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: https://github.com/richmosko/project_template/pull/208
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Architect's follow-up candidate on PT-112's ruling @8b60a8b (2026-09-09): a failing narrowed `run_tests.py -p …` run from a worktree produced no ledger record at all — `_self_record` appears to run only on the success path (or the exit-1 path skips it), so red runs never reach `test-runs.jsonl` and `cairn loop-stats` cannot count them. Fix: record on every exit path with the pass/fail outcome in the record (`ok: false`, failure counts), keep the hook's patching unchanged; test with a fixture suite that fails. Backlog (filed during the PT-0.12.2 backlog drive; Mosko routes).

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-116-ledger-records-red-runs`.

**Gate 1 brief (architect):** your PT-112 observation was a failing narrowed **unittest** run producing no ledger record. Two record paths exist and the brief asks you to measure which one drops red runs: (1) `run_tests.py::_self_record` (~L330) is called from `main()` (~L441) before the failure loop, unconditionally under `__name__ == "__main__"` — so a red `run_tests.py` run should record; confirm with a real failing narrowed run and read the ledger; (2) the PostToolUse hook's scrape path for bare `unittest` (`test_run_record.py::_scrape_record` ~L104) appends only when it can parse the summary — does it require `OK`, or does it skip when the tool result carries `FAILED (…)`, or when `is_error` is set on the result? Rule on: the record shape for a red run (`ok: false`, `failures`, `errors`, and PT-111's classification untouched — a red run is still an executed run); both paths record on every exit path; the hook never records a guard-refused command (PT-111/PT-115 controls stay negative). qa: a fixture suite that fails, driven through both paths; loop_stats counts it as executed. Writers: implementation-lead → `run_tests.py` + `test_run_record.py`; qa → tests.

### @architect — 2026-09-08

**Gate 1 ruling (architect). The reported defect does not reproduce — measured at `4ef79ef`, both paths, with a deliberately failing fixture suite.**

- **Runner path.** An isolated copy of `run_tests.py` plus a fixture with one failure and one error, run narrowed with `CAIRN_TEST_RUNS_FILE` pointed at a scratch ledger: record written, `{"ok": false, "tests": 3, "seconds": 0.05, "session": "817d7be4-…"}`. Written **also** when stdout is fully suppressed (`> /dev/null 2>&1`, rc 1) — `_self_record` is in-process and has no stdout dependency.
- **Hook scrape path.** A real failing `python3 -m unittest discover -s tests -p "test_red.py" 2>&1 | tail -3` from this session wrote to the **live** ledger at `2026-09-09T06:19:09Z`: `{"runner": "unittest", "tests": 3, "ok": false, "who": "architect"}`.

**So red runs already reach the ledger on both paths, and `ok: false` is already recorded.**

**What my PT-112 observation actually was.** I measured the symptom correctly (a failing narrowed run, no record) and inferred the cause wrongly. The likeliest cause is the *pre-PT-112* `_patch_last_who`: it patched the ledger's last `who: null` line and returned, so the red run produced no record of its own and an older line silently took my `who`. PT-112's session-keyed patch removed exactly that path. The inference, not the measurement, is what carried into this issue — my error, and worth naming so the next follow-up filed off an observation says which part is measured.

**Narrowed scope for this feature — two things are still true and cheap.**
1. **Failure detail.** Records carry `ok` but never *how* red. The runner has the numbers in-process (it prints `FAILED (failures=1, errors=1, files=1)`); the hook can parse `failures=<n>` / `errors=<n>` out of stdout exactly as it already parses `skipped=`. Add **`failures` and `errors`** to the record on both paths. They are `null` when unknown — never 0-filled from ignorance, the same rule `tests`/`ok` already follow (a scrape with no parseable summary still writes nothing).
2. **A regression guard.** Nothing in the suite asserts that a red run records at all; that is why a wrong inference could stand for a day. Both paths get one.

**Explicitly unchanged.** PT-111's classification: a red run is an **executed** run — `Ran N tests` beats exit status, `full`/`gate` semantics untouched, and a guard-refused command still records nothing (PT-111/PT-115 negative controls stay negative).

**Guards (qa writes; red at gate 2).**
1. Runner path: a fixture suite with one failure and one error, run narrowed against a scratch ledger, writes exactly one record with `ok: false`, `tests: 3`, `failures: 1`, `errors: 1`; control — an all-green fixture writes `ok: true`, `failures: 0`, `errors: 0`.
2. Runner path, suppressed stdout: same red run with stdout and stderr discarded still writes the record (guards the in-process property).
3. Hook scrape path: a payload whose stdout carries `Ran 3 tests in 0.0s` + `FAILED (failures=1, errors=1)` appends `ok: false, failures: 1, errors: 1`; controls — an `OK` payload gives `ok: true, failures: 0, errors: 0`, and a payload with no parseable summary still writes **nothing** (`failures`/`errors` are never invented).
4. `loop_stats` counts a red run as executed, not blocked — one row asserting a red `run_tests` step with a `Ran N tests` result classifies `FULL_SUITE`.

**Carried forward, measured, out of scope (each worth its own issue if Mosko wants it).**
- A bare-`unittest` run whose stdout is suppressed records nothing — **not red-specific**, identical for green runs, 49 measured instances at PT-111. Only a self-recording runner fixes it; `unittest` is stdlib.
- **The prefilter is text-based, so an indirectly invoked run reaches neither hook.** Measured live in this session: `sh probe116.sh` (whose script ran a full unittest suite) matched neither `*unittest*` nor `*run_tests*` in the payload, so nothing was recorded — **and nothing would have been refused**. That is a guard evasion, not just a metrics gap.
- `CAIRN_TEST_RUNS_FILE` is honoured by the runner but not by the hook, so records written under the override never receive `who`.

**If the lead prefers, guards 1–4 alone are a defensible close** (the reported defect is absent, the guards pin the behaviour). I recommend keeping the `failures`/`errors` fields: they are two parsed integers and they let a scorecard say how red a loop was.

**Writers.** implementation-lead → `scripts/cairn/run_tests.py`, `.claude/hooks/test_run_record.py`. qa → `scripts/cairn/tests/test_run_tests.py`, `test_test_run_hooks.py`, `test_loop_stats.py`.

### @architect — 2026-09-08

**Gate 4 verdict (architect): go.** Green sha `14754c4`; HEAD adds only a metrics append. `14754c4` touches `scripts/cairn/run_tests.py` (+6) and `.claude/hooks/test_run_record.py` (+16) only, as ruled.

| Axis (ruling guard) | Result | Evidence (re-measured by me at the green sha) |
|---|---|---|
| 1. Runner path records a red run with failure detail | PASS | my fixture (1 failure + 1 error) run narrowed against a scratch ledger: `{"runner":"run_tests","tests":3,"ok":false,"failures":1,"errors":1,"files":1}` |
| 1b. All-green control | PASS | the live ledger's own gate run @ `2026-09-09T06:28:49Z`: `{"tests":1632,"ok":true,"failures":0,"errors":0}` |
| 2. Runner path with stdout **and** stderr suppressed still records | PASS | the record above was produced under `> /dev/null 2>&1`, rc 1 — the in-process property holds |
| 3. Hook scrape path parses the counts | PASS | a real red `python3 -m unittest discover -s tests -p "test_red.py"` from this session wrote to the **live** ledger @ `06:30:22Z`: `{"runner":"unittest","tests":3,"ok":false,"failures":1,"errors":1,"who":"architect"}` |
| 3b. Never invented | PASS | `failures`/`errors` are `None` unless `ok` could be determined (`_RESULT_RE` matched); a payload with no parseable summary still writes nothing at all |
| 4. A red run stays *executed*, not blocked | PASS | PT-111's classification untouched; qa cross-referenced the existing test rather than duplicating it |
| Suite | PASS | `--gate verdict` at HEAD: 1632 OK (skipped=1) parallel 27.0 s; 1632 OK (skipped=1) serial 138.8 s |

**One interaction worth recording (correct behaviour, not a defect).** A compound command that invokes the runner **under `CAIRN_TEST_RUNS_FILE`** produces no live-ledger record: the hook takes the `run_tests` branch, finds no session-matching `who: null` line in the *live* ledger (the record went to the override file), and — per PT-112's ruling — writes nothing rather than guessing. I hit this while verifying and it cost me a measurement; it is the ruling working, but it means override-written records stay unattributed, as PT-116's ruling already carried forward.

**Carried forward, unchanged.** A bare-`unittest` run with suppressed stdout still records nothing (not red-specific), and the text-based prefilter still misses indirectly invoked runs (`sh script.sh`) for both recording **and** guard refusal — the latter is a guard evasion and deserves its own issue.

### @team-lead — 2026-09-08

PR opened: https://github.com/richmosko/project_template/pull/208. Awaiting Validate.
