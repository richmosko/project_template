---
id: PT-69
title: Dashboard: theme/color settings dropdown (shadcn settings pull-down pattern)
status: in-progress
milestone: PT-0.9
parent: null
blocked_by: []
assignee: null
labels: []
priority: null
pr: null
created: 2026-08-28
updated: 2026-08-29
---


## Comments

### @team-lead — 2026-08-28

Scope (Mosko, 2026-08-28): a dropdown menu somewhere on the dashboard to select major theme/color settings, modeled after the top-right settings pull-down on https://www.shadcn-svelte.com/docs/components/dropdown-menu. PT-0.9 candidate — full scoping when the milestone goes live.

### @team-lead — 2026-08-28

Feature started. Branch: `feature/pt-69-theme-settings-dropdown`. Milestone PT-0.9 goes live with it. Scoping question (settings surface + reach) put to Mosko before team dispatch.

### @team-lead — 2026-08-29

**Scoped by Mosko (2026-08-29).** The dropdown exposes FOUR of the reference menu's controls: **Mode** (light/dark toggle), **Base Color**, **Theme**, **Chart Color** — per the settings pull-down on shadcn-svelte's docs site (team-lead browser pass, 2026-08-29: full reference anatomy is Style / Base Color / Theme / Chart Color / Icon Library / Heading / Font / Radius / Menu / Menu Accent / Shuffle / Reset / Mode / Undo-Redo; each row shows label + current value + a right-side indicator dot/icon). Reach: **dashboard + board** — one persisted choice (localStorage), honored by both surfaces.

**Consequences to design for:**
- Relaxes the 2026-08-26 preset-pure ruling into "preset b6XadDxmQS as DEFAULT, user-selectable variants along shadcn's own dimensions" — ledger entry required at finish.
- Chart Color interacts with PT-61's validated ordinal ramp: every offered chart-color variant needs its own validator-passing re-stepped ramp (the ordinal light-end floor failure that hit the golden ramp will recur per hue). The dataviz validator run per variant is mandatory, not optional.
- Board reach means the vanilla board's tokens.css must also honor the variant choice — mechanism to be ruled (architect).

### @architect — 2026-08-29

**Architect ruling — theme-variant architecture (gating).** Read first: `dashboard/src/app.css`, `board/tokens.css`, `board.html`, `board.js` §PT-30 storage, `board-logic.js` `viewStateKey`, `test_board_tokens_parity.py`, `test_css_parse_sanity.py`, `test_dashboard_chart_ramp.py`, `cairn.py` static routes.

**Facts this ruling rests on** (verified, not recalled): both surfaces are served by ONE Python process on ONE port (`/board/<rel>` and `/dashboard/<rel>`), so they share a localStorage origin. `board.css` is fully tokenized — zero hex, and the only literal colors left are three black-alpha shadows/scrim (the `rgb(9, 30, 66)` hit is inside a comment). **Nothing in either surface ever adds the `.dark` class today** — both files carry `.dark` token blocks that are, right now, dead code. Mode is therefore a new mechanism on both surfaces, not a toggle of an existing one.

### 1. Variant token source — hand-vendored, generated from checked-in data. No network, ever.

Registry-fetch-at-build is rejected outright: the board is zero-build and "clone and it works" is the template's strongest property. A fetch on the user's machine breaks it; a fetch in CI makes the palette a moving target.

Source of truth: **`scripts/cairn/design/variants.json`** — vendored palette data (option sets + their token values), with `scripts/cairn/design/NOTICE.md` carrying provenance/extraction date per dimension, exactly the PT-63 font-vendoring precedent. Values are *extracted from shadcn-svelte's own generator* (browser pass, per the 2026-08-26 preset-extraction precedent — WebFetch does not work for this), not computed by us: these are shadcn's dimensions, and inventing our own hues would defeat the point.

Emission: **`scripts/cairn/design/gen_variants.py`** — stdlib-only, zero deps, no network — writes three generated CSS files. It is an *authoring* tool run by an agent, never on the clone path.

Guard: a test regenerates to a temp dir and asserts **byte-identical** to the committed files. That is strictly stronger than `test_board_tokens_parity.py`'s value-comparison and it retires hand-editing as a drift class for everything it covers. Generator output must be deterministic (sorted keys, fixed block order).

### 2. Runtime mechanism — one shared CSS file per surface, `data-*` on `<html>`, cascade composition.

**Do not swap stylesheets.** FOUC on every change, N requests, and for the dashboard it turns per-variant CSS into an enumerated build output.

The dimensions are near-orthogonal *token subsets*, and that is what keeps this from exploding:

| Dimension | Attribute | Owns |
|---|---|---|
| Base Color | `data-cairn-base` | neutrals: `--background --foreground --card(-foreground) --popover(-foreground) --secondary(-foreground) --muted(-foreground) --accent(-foreground) --border --input --ring --destructive(-foreground) --sidebar --sidebar-foreground --sidebar-accent(-foreground) --sidebar-border --sidebar-ring` |
| Theme | `data-cairn-theme` | brand hue: `--primary --primary-foreground --sidebar-primary --sidebar-primary-foreground` |
| Chart Color | `data-cairn-chart` | `--chart-1..5` + `--chart-flow-*` |
| Mode | `.dark` class (unchanged) | selects the light/dark half of each block above |

Blocks are **O(B + T + C)**, not O(B×T×C) — ~2×(B+T+C) blocks total, order ~16KB uncompressed. That is the whole reason to compose in the cascade rather than ship whole themes.

**Hard invariant: the three groups must PARTITION the token set.** A variable declared under two dimensions is a cascade race whose winner depends on source order. Require a test asserting no variable name appears in more than one dimension's blocks. This invariant is what makes the composition sound; it is not a style preference.

Selectors — attributes and `.dark` both live on `<html>`, so they compound on one element:
```
:root { /* existing defaults — unchanged */ }
.dark { /* existing defaults — unchanged */ }
:root[data-cairn-base="zinc"] { … }          /* (0,2,0) */
:root.dark[data-cairn-base="zinc"] { … }     /* (0,3,0) */
```
Specificity does the work: every variant block outranks the defaults, and dark outranks light, so nothing depends on inter-block source order beyond "variants come after defaults." No `!important`.

**`data-cairn-*`, not `data-base`/`data-theme`/`data-chart`.** `data-chart` is already taken — `lib/components/ui/chart/chart-container.svelte` sets it and `chart-style.svelte` generates `[data-chart=<id>]` rules. Namespacing is not cosmetic here.

Mode keeps the **`.dark` class**, not a `data-*` attribute. Changing it would mean rewriting app.css's `@custom-variant dark (&:is(.dark *))`, both existing token files, and the parity test, and buys nothing. `<html>` is the carrier: `:is(.dark *)` covers everything under `<body>`.

Files (generated): `scripts/cairn/board/variants.css` (linked in `board.html` **after** `tokens.css`, before `board.css`), `scripts/cairn/dashboard/src/variants.css` (`@import './variants.css';` immediately after app.css's existing three imports — still ahead of any rule, so Tailwind v4's import-ordering rule holds), `docs/DESIGN/variants.css` (spec copy, ux-designer documents the option sets prose-side in `design-system-spec.md`). **All three go into `test_css_parse_sanity.py`'s file list** — a generated stylesheet is exactly as capable of shipping dead as a hand-written one, and a `0 cssRules` variants.css would fail silently as "theme just doesn't change."

### 3. Persistence — one origin-global key, per-dimension fallback, inline head bootstrap.

Key: **`cairn.theme`** (not under `cairn.board.*` — it spans surfaces). Value: `{"v":1,"mode":"system|light|dark","base":"…","theme":"…","chart":"…"}`.

**Deliberately NOT repo-scoped**, and this is the one place I am departing from PT-30. `expandedLanes` is keyed by primary root id because it is a fact *about a project*. A theme is a fact about *the person looking* — a user who picks Zinc does not want it to revert when they open a different repo's board on the same port. Wrong-dimension keying is this codebase's recurring defect class, so: the dimension here is the user/origin, and origin-global is the correct scope. If someone later wants per-repo themes, that is a new key, not a re-scoping of this one.

Access: reuse PT-30's contract verbatim — `localStorage` referenced *inside* each `try`, no hoisted module-level reference, no cached `storageAvailable` flag, silent per-call degrade to in-memory.

Read discipline: **per-dimension fallback**, not whole-blob rejection. An unrecognized or absent `base` falls back to the default base while leaving the user's `chart` choice intact; a future v2 adding a dimension must not wipe the other three. A `v` this reader doesn't know → ignore the blob entirely.

**FOUC:** a small **inline** `<script>` in `<head>` — before the stylesheet links in `board.html`, before the module script in `dashboard/index.html` — reads the key and sets `document.documentElement.className` / `dataset` synchronously. It must be inline: an external file is a second blocking request on the board and a Vite asset on the dashboard. That snippet is a fourth accepted copy; **generate it from the same `variants.json` and assert both heads contain it verbatim.**

**Defaults disagreeing is a build-time failure, not a runtime one.** The default tuple (`{mode:"system", base:"stone", theme:"sky", chart:"yellow"}` — preset b6XadDxmQS) is emitted into both bootstrap snippets from `variants.json`, and the byte-identity test is the enforcement. The offered option sets likewise come from one file, so "dashboard offers a variant the board doesn't" cannot arise; the per-dimension fallback above is the defensive backstop, not the plan.

**Cross-surface propagation:** same origin ⇒ same bucket, so a reload is already correct. For two live tabs, listen for the `storage` event (fires in *other* tabs on write) and re-apply. Cheap, and without it board-and-dashboard-side-by-side — the obvious way to demo this — looks broken.

### 4. Chart-ramp validation — per-variant, extremal-surface, test-enforced.

Each Chart Color variant ships its own 6-step `--chart-flow-*` ordinal ramp, authored into `variants.json` alongside its `--chart-1..5`, derived with the dataviz skill's `validate_ordinal` (see implementation-lead's technique notes — the ≥0.065 gap for hex-quantization noise, and the OKLCH→hex inverse you have to write yourself). PT-61's light-end floor failure is a property of the hue, so **it will recur per hue**; assume every variant needs re-stepping, not just some.

The gate is structural: **parameterize `test_dashboard_chart_ramp.py`'s `ChartRampOrdinalValidationTests` over every chart variant.** A promise to run the validator is not a gate.

**Which surface to validate against** — the ramp's surface is `--card`, which is Base-Color-dependent, so the naive obligation is C×B×2 validator runs. It collapses to **C×2**, exactly and not heuristically: for a fixed ramp, contrast against the card is monotone in card luminance on each side. So take the set of all `--card` values across every (base × mode) combination and validate every ramp's **light end against the maximum-luminance card** and its **dark end against the minimum-luminance card**. If the extremes pass, every base passes. **Compute the extrema from the data in the test** — do not hardcode which base is extremal; that is a fact about the option set, and the option set will change.

**Budget, ux-designer:** Chart Color is the expensive dimension — PT-61 was a whole feature for *one* ramp. Cap it at **≤5 chart variants** for PT-69; more is a follow-up issue, not scope creep here. Base Color and Theme are cheap (vendored, no derivation) — spend the option-count budget there.

### 5. Scope fence

**Out, explicitly:** Style, Radius, Font, Icon Library, Heading, Menu, Menu Accent, Shuffle, Reset, Undo/Redo — every reference-menu row except the four Mosko named. Also out: per-repo or per-major theming, any server-side persistence or cross-device sync (localStorage only), and the docs HTML artifacts (`docs/_assets/toc.js` keeps its own independent `doc-theme` toggle — do not merge them; different surface, different lifecycle).

**In, and under-estimated:** *dark mode on the board has never been rendered.* The `.dark` block in `board/tokens.css` has been dead since PT-57. `board.css` is token-clean so this should be mostly mechanical, and the three remaining black-alpha shadows/scrim are mode-tolerant — but "should be" is not a measurement. Per this project's browser-visibility precedent, the board in dark mode needs an **actual rendered check** (screenshot / computed-style-at-point), not a token-presence assertion. If it turns out worse than mechanical, split it out rather than letting it swallow PT-69.

**Ledger at finish:** the 2026-08-26 preset-pure ruling relaxes to "b6XadDxmQS as the default point in a variant space along shadcn's own dimensions" — `process/DECISIONS.md` entry required, along with the origin-global key-scoping departure from PT-30 in §3.

### @ux-designer — 2026-08-29

## UX proposal — theme/color settings dropdown (Mode / Base Color / Theme / Chart Color)

**Method note:** no browser tool was available in this session (`ToolSearch`/claude-in-chrome disabled), so I could not re-scrape the live shadcn-svelte.com settings menu myself — this proposal is grounded in the design-system-spec's already-*confirmed* preset extraction (Style Mira / Base Stone / Theme Sky / Chart Yellow) plus shadcn's well-documented, stable neutral/accent vocabulary, not a fresh live read. Recommend a quick confirm pass against the reference page before frontend-lead locks exact swatch order — team-lead's 2026-08-28 anatomy pass already covers row structure, so this is a narrow follow-up, not a re-do.

### 1. Option sets

**Base Color** — swaps only the neutral family (`--background`/`--foreground`/`--muted`/`--border`/`--card`/`--sidebar` etc.), no accent or chart impact, no new a11y risk beyond what Stone already carries. This is shadcn's own **closed** 5-way set, so "curated" and "complete" are the same thing here — ship all 5, no cost to trim:
- **Stone** — DEFAULT (current preset)
- Neutral
- Zinc
- Gray
- Slate

**Theme** — swaps the accent family (`--primary`, `--primary-foreground`, `--sidebar-primary(-foreground)`, `--ring`). Each addition costs one contrast check (primary-foreground on primary, same flag the spec already raises for the current Sky pair), not a full ramp. Recommend **4 total**, spread across hue families so the choices actually read as different rather than clustering near Sky:
- **Sky** — DEFAULT (current preset)
- Violet
- Rose
- Green

**Chart Color** — swaps the chart hue (`--chart-1`…`--chart-5` sequential ramp **and** PT-61's 6-step ordinal chart-flow ramp). This is the expensive one: every variant needs both ramps re-derived and run through the dataviz validator (`validate_ordinal` — monotone lightness, ≥0.06 ΔL, ≤40° hue spread, ≥2:1 light-end contrast) against **both** light and dark `--card` surfaces, per the PT-61 precedent. Recommend the smallest defensible set, **3 total**:
- **Yellow** — DEFAULT (current preset, already validated)
- Blue
- Violet

Flagging for architect: Theme=4 / Chart=3 (both counts include the default) is my judgment call on "small and curated" — trim further if the validator-run budget is tighter than I'm assuming, especially on Chart Color.

### 2. Dropdown anatomy

- **Row order:** Mode (top — most-used, single toggle), then Base Color, Theme, Chart Color — each a **submenu**, not cycle-on-click.
- **Submenu over cycle-on-click, recommended:** cycle-on-click makes reaching option 3-of-4 cost 3 blind clicks with no visibility into what's ahead. A submenu (`DropdownMenu.Sub` + `DropdownMenu.RadioGroup`) shows the full option set and the current selection (native checkmark) in one view — this also reuses shadcn's existing RadioItem/CheckboxItem selection idiom rather than inventing a stateless cycling interaction with no precedent elsewhere in this design system.
- **Row content:** label + current-value text + right-side indicator (swatch dot for Base/Theme/Chart, sun/moon icon for Mode) — matches the reference anatomy exactly.
- **Placement — dashboard:** `Sidebar.Footer`. This is the natural home per the spec's own Sidebar section ("dashboards are assumed to ship a persistent nav sidebar as a first-class surface") — always visible, doesn't compete with the page header. Trigger = icon button (Lucide `Settings2`, this system's stated icon library) with an "Appearance" label when the sidebar is expanded, icon-only when collapsed (mirrors how Sidebar already handles nav items at collapsed width).
- **Placement — board:** the board is vanilla HTML/CSS/JS with no Svelte sidebar (`board.css`, per Legacy/migration). Recommend a small trigger pinned to the board's own top-right header — the natural fallback for a page with no sidebar, and it happens to match the reference pattern's own top-right placement. Style it with the same OKLCH values already mapped in the Legacy/migration table (popover surface, `radius-lg`, `shadow-sm`) so it reads as intentional rather than bolted-on, even though it can't literally mount the Svelte `DropdownMenu` primitive. Both surfaces read/write the same `localStorage` key per the issue's "one persisted choice, both surfaces" ruling — the swap *mechanism* (data-attribute vs. class vs. full token replacement) is architect's call, not mine; this proposal only fixes the UI/visual contract.
- **Mode row:** **light/dark toggle (2-state)**, not a 3-state light/dark/system — this matches Mosko's own scope note ("Mode (light/dark toggle)"), so it's confirmation not a new recommendation. Rationale for the record: a toggle keeps Mode a single click with an immediate dot/icon flip, consistent with every other row being one visible current-value indicator; a 3-way "system" option would need its own submenu, which breaks the row's single-toggle simplicity. Adding "follow OS" later is a strict additive change (promote Mode from toggle to submenu), not a redesign.

### 3. States

- **Indicator dot:** ~8px circle, filled with the *token* the row currently resolves to — Theme dot shows current `--primary`, Base Color dot shows current `--foreground` (reads cleanest as a neutral chip at that size), Chart Color dot shows current `--chart-3` (a representative mid-ramp step). Mode swaps a Lucide `Sun`/`Moon` icon instead of a dot, since it isn't a hue choice.
- **Hover/focus:** stock shadcn `DropdownMenu.Item` — `accent`/`accent-foreground` hover surface, `focus-visible:ring-ring` on keyboard focus, per the Components table. No new CSS.
- **Selected submenu row:** native checkmark + bold label, not a border/fill change — stays lightweight at 3–5 rows per submenu.
- **Reset:** recommend **OUT**, confirming the issue's own steer. It's not one of the four named controls; at only 4 settings (vs. the reference's 10+), manually re-selecting defaults is ≤4 clicks; and "reset to what" (page default vs. last-saved vs. the b6XadDxmQS preset) is a real open question with no forcing note to answer it here. Revisit if a future ticket grows this dropdown past ~4 knobs.
- **First paint:** before `localStorage` is read (SSR/hydration gap), fall back to preset defaults (Stone/Sky/Yellow/light) — no flash-of-unstyled-content, no skeleton needed since this is a synchronous local read, not a network fetch.

### Bubble-up
- `design-system-spec.md`'s Components table has no `DropdownMenu` row today — once architect rules the variant mechanism, I'll add one (component install path + this dropdown's specific anatomy) alongside any spec updates the ruling requires.
- Every new Theme/Chart Color option needs the same contrast/ordinal-validator treatment the spec already runs for Sky/Yellow before it's ship-ready — not a rubber stamp on the 5 new hues named above.

### @ux-designer — 2026-08-29

## Addendum — Mode UI/schema discrepancy (flagging, not resolving)

Architect's persistence schema (§3) sets `"mode":"system|light|dark"` with **default `"system"`** — a 3-state model. My proposal above recommends a **2-state light/dark toggle row**, deferring to team-lead's 2026-08-29 scope note ("Mode (light/dark toggle)"). These two aren't reconciled: if the stored default is genuinely `system`, the UI needs a way to represent/select that third state (or the toggle's own two positions need to double as "light" and "dark" while `system` is only ever a *pre-first-interaction* fallback value, never a row the user can navigate back to). Recommend team-lead confirm with Mosko which is intended — I can spec either (2-state toggle with `system` as an invisible until-first-click fallback, or a 3-row submenu matching the stored schema) once it's settled; not blocking the rest of this proposal either way.

### @team-lead — 2026-08-29

**Ruled (team-lead, 2026-08-29): Mode is 3-state — system | light | dark, default system** — adopting architect's persistence schema over the reference's 2-state toggle. Rationale: "system" is the incumbent behavior for every user; a 2-state toggle would make the first menu touch a permanent departure from OS-following with no path back (Reset is ruled OUT per ux's proposal, so nothing would restore it). The reference's 2-state row is a docs-site convenience, not a design principle; the row presents in the reference's visual pattern regardless. ux-designer specs the 3-state shape. Mosko may veto.

Also noted from ux's proposal: the swatch-value confirm pass (live scrape vs documented vocab) lands in my Validate browser leg; option sets adopted as proposed — Base ×5 (Stone default), Theme ×4 (Sky default), Chart ×3 (Yellow default, each dual-ramp validated per architect's §4).

### @ux-designer — 2026-08-29

## Addendum — Theme option set revised up (4 → 7) per architect's budget note

Architect's follow-up: Theme is cheap (4 tokens, vendored, no derivation) and explicitly flagged "being generous on Theme costs almost nothing." Revising my Theme recommendation from 4 to **7 total**, matching shadcn's own long-standing accent-color set size rather than inventing a new count:

- **Sky** — DEFAULT (current preset)
- Blue
- Violet
- Rose
- Orange
- Green
- Amber

Deliberately not "Yellow" (kept off the Theme list even though it's a valid shadcn accent) — Chart Color's default is already named Yellow, and having the same color word mean two different things in two different submenus of the same dropdown is a real mixup risk for a user with both menus open. Amber sits close enough in the palette to cover that hue territory without the name collision.

Base Color (5, unchanged) and Chart Color (3, unchanged, still under the ≤5 cap) stand as proposed above — Base is already shadcn's complete closed set, and Chart stays intentionally lean given the per-variant validator cost architect flagged.

### @ux-designer — 2026-08-29

## Addendum — Mode row, 3-state shape (per team-lead's ruling)

Ruled: `system|light|dark`, default `system`. Spec'ing the row to match the reference anatomy exactly, consistent with the other three rows rather than as a special case:

- **Main row:** label "Mode" + current-value text (`System` / `Light` / `Dark`) + right-side icon indicator — same label+value+indicator shape as Base Color/Theme/Chart Color, not a toggle. (Supersedes my earlier 2-state-toggle recommendation, which is now moot.)
- **Submenu:** `DropdownMenu.Sub` + `DropdownMenu.RadioGroup`, three items in this order — **System, Light, Dark** (System first: it's the default, and matches the System→Light→Dark ordering convention most apps with this exact pattern use, e.g. GitHub/Vercel settings menus). Each item: icon + label, native checkmark on the active one — identical selection idiom to the other three submenus.
- **Icons (Lucide, this system's stated icon library):** `Monitor` = System, `Sun` = Light, `Moon` = Dark. The main row's right-side indicator icon mirrors whichever of the three is currently *selected* (not the OS-resolved effective appearance) — i.e. if the user has picked System and the OS happens to be in dark mode right now, the row still shows the Monitor icon, not Moon. Showing the resolved appearance instead of the selection would make the row lie about what's actually stored, and would flicker on every OS-level theme change without the user touching anything.
- **Live-follow requirement (state, not just visual):** when `mode="system"`, the effective `.dark` class must track `prefers-color-scheme` at runtime, including *while the page is open* — listen for the media query's `change` event, not just read it once at load. This is in addition to architect's cross-tab `storage` event listener (§3 of the ruling) — two different triggers for the same "the visible mode can change without a click" requirement, both needed.
- **First paint / FOUC:** covered by architect's inline bootstrap script (§3) — for `mode="system"` specifically, that script must evaluate `matchMedia('(prefers-color-scheme: dark)').matches` synchronously, not just fall through to the light default, or a system-dark user gets a light flash on every load.

This closes out the anatomy proposal — option sets (Base 5 / Theme 7 / Chart 3) and all four rows' shapes are now fully specified. Adding the DropdownMenu component + these dimensions to `design-system-spec.md` next.

### @architect — 2026-08-29

**Architect addendum — three corrections, all gating.** Read the tree after ux-designer's proposal and team-lead's Mode ruling. Option-set counts stand as adopted: Base ×5, Theme ×7, Chart ×3 — 2×(5+7+3) = 30 variant blocks, well inside the mechanism's budget. Three things in the proposal will build wrong as written.

**1. `--ring` belongs to Base, not Theme — this is the partition violation, and it's not theoretical.** ux-designer's §1 lists `--ring` under Theme; my §2 table assigns it to Base. Base wins, on the data: `--ring` is `oklch(0.709 0.01 56.259)` light / `oklch(0.553 0.013 58.071)` dark — chroma 0.010–0.013 at hue ~57, which is the stone *neutral* hue, whereas `--primary` is chroma 0.134 at hue ~241. `--ring` is a neutral that tracks the base scale; it is not the accent. Same for `--sidebar-ring` (identical values). Theme owns exactly four tokens: `--primary`, `--primary-foreground`, `--sidebar-primary`, `--sidebar-primary-foreground`.

This is precisely the cascade race §2's partition invariant exists to prevent — had it shipped, `--ring` would be declared under two dimensions and the focus-ring color would depend on which block the generator happened to emit last. The partition test is what catches the next one; it isn't ceremony.

**2. First paint must resolve `system`, not fall back to light.** ux-designer's §3 specifies the first-paint fallback as "Stone/Sky/Yellow/**light**". With Mode now ruled 3-state defaulting to `system`, forcing light on first paint hands every dark-OS user a white flash on every cold load — the exact FOUC the inline `<head>` bootstrap exists to prevent, just relocated. Correct fallback: **Stone/Sky/Yellow + `prefers-color-scheme` resolved synchronously in the bootstrap.**

Related, and worth stating so nobody builds for it: there is **no SSR/hydration gap** on either surface. The dashboard is a static Vite SPA, not SSR, and the bootstrap is an inline `<head>` script that runs before first paint on both surfaces. The `localStorage` read is synchronous and complete before anything renders. No skeleton, no deferred-apply, no hydration reconciliation.

**3. The 3-state ruling adds a runtime obligation nobody has written down yet: a `matchMedia` change listener.** If stored mode is `system` (or absent), the OS flipping light↔dark *mid-session* must re-resolve `.dark` live — `window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', …)`, attached only while mode resolves to system, and torn down or no-op'd once the user picks an explicit light/dark. `docs/_assets/toc.js` already does exactly this for its own `auto` state; copy that shape. Without it, "System" is a lie that only tells the truth at load time. Both `"system"` stored explicitly and the key being absent resolve identically — no need to distinguish them.

**Correcting my own earlier note to ux-designer: Theme is cheap, but I called it "almost nothing" and that was imprecise.** Each Theme variant is two text-on-fill contrast pairs per mode (`--primary-foreground` on `--primary`, `--sidebar-primary-foreground` on `--sidebar-primary`); each Base variant likewise carries `--foreground`/`--background`, `--card-foreground`/`--card`, `--muted-foreground`/`--muted`. Cheap *per check* and trivially parameterized — one test walking every (dimension, variant, mode) tuple from `variants.json` and asserting its declared foreground/background pairs clear the WCAG floor, reusing the OKLCH→hex bridge the chart-ramp tests already carry. But not zero, and not assumable: PT-61's whole lesson is that a published shadcn palette *did* fail a floor nobody thought to check. Seven Theme variants is still the right call — the cost is one more row in a parameterized test, not one more derivation.

**Scope fence on that gate — and this one matters for schedule.** If a Base or Theme variant fails its contrast check, **drop the variant from the option set; do not re-derive it.** We are not in the business of repairing shadcn's published neutrals, and a re-derived "Zinc" that isn't shadcn's Zinc is worse than four base colors. Chart Color is the deliberate exception: `--chart-flow-*` is our own token and PT-61 already established we re-step it per hue, so a failing chart variant gets re-derived, not dropped.

**Non-gating, ux-designer's call — the Base Color indicator dot won't discriminate.** §3 has the Base dot filled with `--foreground`. Across all five shadcn base scales `--foreground` is near-black in light mode, separated only by a fraction of a chroma unit at ~0.004; at 8px, all five dots will look identical and the indicator conveys nothing. Neutral hue tint is most visible at *mid* lightness, so `--muted-foreground` (L≈0.553, C≈0.013) or a two-tone chip (`--muted` fill with a `--border` ring) will actually separate the five. Your row to spec — flagging the perceptual fact, not overriding the design.

**Names remain provisional.** Per §1 of the main ruling, the authority for option ids and display labels is the browser-extraction pass into `variants.json`; the proposal's hue names are placeholders. If a proposed name has no counterpart in shadcn's published set, take the nearest published hue rather than authoring our own — the point of this dimension is that it *is* shadcn's. Display labels ship from `variants.json` too, so board and dashboard cannot drift on wording.

### @ux-designer — 2026-08-29

## Addendum — corrections applied

Acknowledging architect's 2026-08-29 corrections:

1. **`--ring` under Theme** — that was a mistake in my original proposal comment (§1). `docs/DESIGN/design-system-spec.md`'s dimension table already has Theme correctly scoped to the 4 tokens (`--primary`, `--primary-foreground`, `--sidebar-primary`, `--sidebar-primary-foreground`) — I'd copied architect's own partition table verbatim when writing the spec doc, so the spec was never wrong, only my earlier issue comment. No file change needed there.
2. **First paint / system resolution** — same story: the spec doc's Mode section already specifies a synchronous `matchMedia` check for the `system` case with no SSR/hydration framing (it's a static SPA + inline head script, no gap to design around). My *original* issue comment's "SSR/hydration gap... fall back to Stone/Sky/Yellow/light" line is superseded by the later Mode addendum + the spec doc — flagging it superseded rather than editing history on the issue.
3. **Theme "almost nothing" framing** — noted, no count change; 7 stands.
4. **Base Color indicator dot — fixed.** Changed the dot fill from `--foreground` to `--muted-foreground` (mid-lightness, where hue/chroma differences across Stone/Zinc/Slate/Gray/Neutral are actually visible) directly in `docs/DESIGN/design-system-spec.md`'s Dropdown UI spec section, with a documented fallback (two-tone `--muted`/`--border` chip) if `--muted-foreground` still doesn't discriminate once real `variants.json` values land. Also added the per-row dot-token mapping (Theme → `--primary`, Chart → `--chart-3`, Base → `--muted-foreground`) to the spec doc — it wasn't pinned there before, only in this issue thread.

`design-system-spec.md` is current. Nothing further pending from me on PT-69 unless build turns up something new.

### @ux-designer — 2026-08-29

## Addendum — architect's two spec gaps landed

Dropped both pieces of commit-ready text from architect's verification pass into `docs/DESIGN/design-system-spec.md`, verbatim: the chart-validation surface clarified to the full Base×Mode `--card` extremes (not just default Stone), and the Base/Theme contrast-gate + drop-don't-repair fence added right after the sizing-logic paragraph. Spec doc is current with both.

### @ux-designer — 2026-08-29

## Ruling — Base Color: ship all 7 real options (option B)

implementation-lead's live-generator verification (`temp/2026-08-29-implementation-lead-pt69-basecolor-finding.md`) found "Gray" and "Slate" don't exist in the OKLCH preset system this app is built on — the real, complete `PRESET_BASE_COLOR_KEYS` set is **Stone, Neutral, Zinc, Mauve, Olive, Mist, Taupe** (7). The classic shadcn/ui 5-name vocabulary I originally proposed (Stone/Neutral/Zinc/Gray/Slate) was a reasonable assumption from general shadcn familiarity but doesn't match this specific preset generator; Gray/Slate only exist in a separate legacy HSL system that also redefines Theme/Chart-owned tokens per base color (a partition violation) — not usable.

**Ruling: ship all 7**, not a picked-5 substitution. This is the same reasoning I used originally — "shadcn's own closed set, ship all of it, trimming costs nothing" — and it applies more cleanly to the real 7 than my invented 5 ever did. Architect already flagged Base as derivation-free/vendored, and the newly-landed per-variant contrast gate covers all 7 at the same cost class as 5 (one more row per variant in one parameterized test, not new work).

**Base Color, corrected:** Stone (DEFAULT) / Neutral / Zinc / Mauve / Olive / Mist / Taupe.

`docs/DESIGN/design-system-spec.md` updated: the dimension table, sizing-logic paragraph (with a correction note explaining the Gray/Slate mistake and pointing to implementation-lead's finding), and the Base Color dot-fill rationale (now "all seven" instead of "all five") are all current. Theme (7) and Chart (3) are unaffected, per implementation-lead's note.

### @architect — 2026-08-29

**Architect ruling — contrast gate semantics (gating).** The gate that's failing is mine, and the first finding is that I specified it wrong. Verified against the tree, not reasoned from the token names.

## The error

I derived the gate's pair list from **token-name symmetry** — "`X-foreground` renders on `X`" — and wrote it out as a cross-product. That's a naming convention, not evidence that anything renders it. Gating a pair nothing renders asserts a property of something that doesn't exist: the same class of mistake as fabricating a measured value, pointed the other way. It also caused the opposite error in the same stroke — the cross-product **missed** the most common real pair in both surfaces. I over-tested and under-tested simultaneously.

## Q1 — Pair reality: the gate tests RENDERED pairs, derived from usage

**DROP `--sidebar-primary-foreground` / `--sidebar-primary`.** Verified zero references across `board.css`, `board.js`, `board.html`, and all of `dashboard/src` — *including* the vendored shadcn `lib/components/ui/sidebar/` component, which carries no `sidebar-primary` reference at all and styles its active item off `--sidebar-accent`. These two tokens are declared in all three token files and referenced by nothing. They are dead tokens in this project.

This one removal restores the **entire Theme dimension**. Every one of the 7 Theme failures was on this pair; `--primary-foreground`/`--primary` — the pair that *is* rendered (`bg-primary`) — passes everywhere, both modes. Theme goes from 0 viable options back to 7.

**KEEP `--muted-foreground` / `--muted`.** This one is real and does not get waved away. The board's `body { background: var(--muted) }` makes the entire canvas a muted surface, as do `.column`, `.swimlane`, `.drawer pre`, `.markdown-body`; `--muted-foreground` text sits inside them (`.column-header`, `.swimlane-header`, `.empty-state`, and ~20 more rules). The dashboard's body is `bg-muted` with 8 `text-muted-foreground` usages. Genuinely rendered, heavily, on both surfaces.

**ADD `--muted-foreground` / `--card`** — meta text inside cards, the single most common foreground/surface combination in both surfaces, which my cross-product never tested. It passes at **4.79:1**, and that number is the sharpest fact in this whole finding: *the identical text passes on cards and fails on the canvas.* The defect is the surface, not the ink.

**`--foreground` / `--background`:** `--background` is never used as a surface on the board and the dashboard's body is `bg-muted`. Keep it only if the browser confirmation below finds it actually rendered; otherwise it's another naming-convention pair.

**The rule going forward:** the gate's pair list is **derived from source usage and re-derived on every run**, never hand-maintained — same discipline as `test_board_small_label_face.py`, which re-derives its selector set from source rather than pinning today's list. A pair enters the gate when something actually renders that foreground on that background, and leaves when nothing does.

**Method caveat, stated because it is exactly the trap this project keeps stepping in:** statically deriving "what surface does this text sit on" is text-level inference about a DOM that doesn't exist until it's rendered. The derived pair list must be **confirmed once in a real browser** — walk the rendered DOM, take each text node's computed color against its effective *composited* background — and that confirmation is the authority. The static derivation is the regression guard afterwards, not the source of truth. Do not let a passing text-level test stand in for that first confirmation.

## Q2 — Role-dependent floors: by the standard, not by convenience

Each pair carries a **role**, and the gate applies the floor the standard gives that role:
- **Normal text — 4.5:1** (WCAG 1.4.3)
- **Large text — 3:1** (≥24px, or ≥18.66px bold)
- **Non-text UI components / graphical objects — 3:1** (WCAG 1.4.11)

**`--muted-foreground`/`--muted` stays at 4.5:1.** `.column-header` renders at **12px**. That is normal text under any reading — 12px is nowhere near the 18.66px-bold or 24px thresholds, and uppercase does not move them. implementation-lead's "arguably the 3:1 large-text floor" reading doesn't survive contact with the numbers, and adopting it would be relaxing a floor by convenience, which is precisely what I was asked not to do. I'm not doing it.

**Where 3:1 legitimately applies:** these tokens are also used as *graphics*, not text — `board.css` fills a status dot with `--muted-foreground` (line 229), and the dashboard has a `bg-muted-foreground`. Those are graphical objects under 1.4.11 and take 3:1. So the gate stores a role per (pair, usage) and floors accordingly, rather than applying one number everywhere.

## Result under the corrected gate

- **Theme: all 7 pass.** Dimension restored, no drops.
- **Base: every variant still fails `--muted-foreground`/`--muted` in light mode** — Stone 4.39 (default), Neutral 4.35, Zinc 4.39, Mist 4.14, Olive 3.86, Taupe 4.40; Mauve (4.54) alone clears. Independently recomputed the two headline numbers from the hex values rather than inheriting them: 4.39 on `--muted`, 4.79 on `--card`, 3.77 for the dead sidebar pair. implementation-lead's arithmetic is correct.

**The drop fence is untouched and currently has nothing to act on.** It was written for "this hue has a per-variant defect" (PT-61's shape). What's left is not that: it's one genuinely-rendered pair failing across shadcn's entire neutral family, default included. Dropping 6 of 7 neutrals is not the fence working, it's the fence being pointed at a systemic issue it wasn't written for. My pair list was the problem; the fence stays as-is for real per-variant failures.

## Residue — explicitly NOT my call

A genuinely-rendered pair on a shipped default fails AA by 0.11. Per team-lead's framing that goes to Mosko as a product decision, and I'm not pre-empting it. The options, with the fact that sharpens them — *the same text passes on `--card` (4.79) and fails on the canvas `--muted` (4.39)*:

1. **Accept and record.** It's shadcn's own published neutral scale, live here since 2026-08-26, missing by 0.11. Record as a known deviation with the numbers.
2. **Deviate from the preset** — darken `--muted-foreground` a step per base. Small, derivable, mechanically checkable, and cheaper than it would have been a week ago: PT-69 has *already* relaxed preset-purity into "preset as default, variants on top," so this is no longer a first departure.
3. **Move that meta text onto `--card` surfaces.** Passes today without touching a token, but it's a design change across both surfaces with real blast radius.

## Follow-ons

- **`docs/DESIGN/design-system-spec.md`'s new contrast-gate paragraph carries my original pair list verbatim** — I handed ux-designer that text, so the spec now names the dead sidebar pair as a gate requirement. It needs the corrected list and the per-pair role/floor table.
- **qa-engineer:** `test_base_theme_contrast_gate.py` moves from a hand-listed cross-product to a usage-derived pair list with per-pair roles. Push back if that shape fights the suite — I'd rather amend this ruling than have you write a test you don't believe in.
- Worth noting for whoever picks up the residue: `--sidebar-primary`/`--sidebar-primary-foreground` being dead tokens is independently interesting. They're shipped in all three token files and referenced nowhere. Not PT-69's problem, but someone should decide whether they're pruned or wired up.

### @ux-designer — 2026-08-29

## Addendum — contrast-gate paragraph corrected (architect's dead-pair finding)

Applied architect's correction to `docs/DESIGN/design-system-spec.md`'s contrast-gate paragraph:
- Removed `--sidebar-primary-foreground`/`--sidebar-primary` from the gate (zero references anywhere in the codebase — dead tokens, documented as such in the **Sidebar tokens** section now).
- Added `--muted-foreground`/`--card` to the gated pair list.
- Added the role-dependent floor note (4.5:1 normal text / 3:1 large text / 3:1 non-text objects — this is what sets the Base Color indicator dot's actual floor at 3:1, not 4.5:1).
- Stated the derivation rule explicitly: pairs come from source usage, re-derived each run, never hand-maintained.
- Noted the consequence: with the corrected list, all 7 Theme options pass — the dimension was never at risk.

Also recorded the open accessibility finding in the **Accessibility** section: `--muted-foreground` on `--muted` measures 4.39:1 (fails AA), while the same text on `--card` passes at 4.79:1 — surface defect, affects the whole neutral family including default Stone. Marked explicitly open/unresolved, routed to team-lead → Mosko as a product call, no resolution written in.

`docs/DESIGN/design-system-spec.md` is current.

### @ux-designer — 2026-08-29

## Addendum — ownership-vs-gating note added

Added architect's suggested line to `docs/DESIGN/design-system-spec.md`, right after the dimension/ownership table: Theme still *owns* `--sidebar-primary`/`--sidebar-primary-foreground` (declared per variant so they track the accent hue), the contrast gate just doesn't check them (nothing renders them today) — explicitly warns against pruning them as "dead," since that would leave non-Sky themes with an unguarded sky-blue sidebar-primary. Spec doc current.

### @architect — 2026-08-29

**Architect ruling — keep the generic derivation; and it has found a hole I left in my own ruling (gating).** qa-engineer asked whether to keep the fully-generic usage derivation or narrow back to the four pairs I'd manually discussed. **Keep it generic.** Narrowing to a hand-named list would reinstate the exact defect that produced the last ruling — hand-listing is how the dead `--sidebar-primary` pair got in, and a list I curate is a list that goes stale the moment anyone adds a rule. The scope growing past what I'd manually verified is the mechanism working, not the mechanism overreaching. That was the literal ask and it was the right reading.

## The destructive finding is real — verified independently

Recomputed from the OKLCH source rather than inheriting qa's figures: `--destructive-foreground` on `--destructive` is **4.35:1 light** and **2.64:1 dark** (qa reported 4.37/2.65; the deltas are OKLCH→hex quantization, not a disagreement). Render context confirmed at `board.css`: `.chip.blocked` and `.chip.status[data-status="cancelled"]` both set `background: var(--destructive)` with `color: var(--destructive-foreground)`. `.chip` is **11px** — normal text by any reading, so the 4.5:1 floor governs. Dark mode at 2.64:1 misses AA by 1.86 and falls below even the 3:1 non-text floor. Not a naming-symmetry ghost.

## And a hole the generic derivation exposed — my ruling's, again

I said "derive the pair list from usage," then implicitly scoped derivation **per dimension**, generating (X-foreground, X) within each dimension's owned set. Real CSS doesn't respect that boundary. Rules pair a background token from one dimension with a text token from another, and a per-dimension derivation **structurally cannot generate those pairs**:

`.chip.status[data-status="paused"]` sets `background: var(--chart-2)` with `color: var(--foreground)` — a **Chart Color** surface under **Base Color** text. Measured: **10.32:1 light, 1.83:1 dark.** Worse than destructive, and invisible to the gate as specified. (`.chip.status[data-status="done"]` is the same cross-dimension shape — `--foreground` fill, `--background` text — and passes comfortably at 18.92:1 dark, which is why the pattern isn't obviously dangerous on inspection.)

That's the third time this session I've under-specified how a gate derives its subjects: first naming symmetry instead of usage, then per-dimension instead of per-rule. The correction:

**Derive pairs from CO-OCCURRENCE IN A RULE, not from dimension membership.** A pair exists when a rule (or a Svelte class list) sets a background token and a foreground token together, whatever dimensions they come from. That is what "derive from usage" actually means; dimension membership was me smuggling the old structure back in under a new name.

**Cross-dimension pairs are checked across the cross-product of the dimensions they span** — the paused chip is Chart × Base, so 3 × 5 = 15 combinations. That's cheap: it's contrast arithmetic on a handful of pairs, not per-variant ramp derivation. No budget concern, and no reason to approximate.

## Disposition — and a distinction that changes the decision

Both dark-mode failures need framing I don't think anyone has stated yet: **dark mode on the board has never been rendered.** The `.dark` block has been dead code since PT-57 — nothing on either surface has ever added the class. So these are **not inherited defects PT-69 happens to notice. PT-69 is what makes them reachable.**

That splits the residue into two genuinely different decisions:

1. **`--muted-foreground`/`--muted`, 4.39:1 light** — already live, already visible to every user since 2026-08-26, missing AA by 0.11. "Accept and record" is a defensible product call.
2. **`--destructive-foreground`/`--destructive` at 2.64:1 dark, and `--foreground`/`--chart-2` at 1.83:1 dark** — reachable for the first time *because of this feature*. Accepting these means knowingly shipping a Mode row that renders 11px status-chip text below every WCAG floor, including the non-text one. That is a different question from accepting an inherited miss, and it should not be waved through on the first decision's momentum.

I flagged at the start of PT-69 that the board's never-rendered dark mode was the biggest under-estimated risk in the feature and might warrant splitting out. This is that risk materialising, now with numbers instead of a prediction. I'm not ruling the scope call — but if the answer is "fix the dark chip palette," that is a real chunk of work with its own validator obligations, and it belongs in its own issue rather than swelling PT-69.

## Actions

- **qa-engineer:** keep the generic derivation; extend it from per-dimension symmetry to **per-rule co-occurrence**, and check cross-dimension pairs over the spanned dimensions' cross-product. Keep `PENDING_BROWSER_CONFIRMATION` for `--foreground`/`--background` exactly as you built it — holding a pair out pending evidence, explicitly and in the docstring, is the right shape. Your `_token_role()` "text role wins when a token has both" is correct and matches the ruling.
- **Everyone:** expect the failing set to grow again when co-occurrence derivation lands. A gate finding more than I hand-verified is the gate being right.
- **Residue to Mosko** via team-lead, as two separate questions per the split above — not one bundled "the palette has some contrast issues."

### @architect — 2026-08-29

**Architect ruling — the `--foreground`/`--chart-2` failure is a coupling defect, not a contrast product-call (gating).** qa's cross-dimension gate is correct and its numbers reproduce mine. But the disposition I implied last time — "third item for Mosko's contrast decision" — is wrong, and I'm correcting it before it reaches him as one.

## Mechanism: this is a mode-inversion, not a hue problem

`--chart-1..5` is declared **identically in `:root` and `.dark`** — the base chart ramp does not change between modes (by design; it's a data-viz ramp read against `--card`). `--foreground` *does* invert: near-black in light, near-white in dark. So `--foreground` on `--chart-2` swings from 10.32:1 to 1.83:1 purely because one side of the pair flips and the other doesn't. That is why qa sees *every* Base × Chart combination fail in dark rather than a few hues — nothing hue-specific is happening. No re-stepping of any ramp fixes a pair whose two halves track different mode behaviour.

## The real defect: PT-69 makes status legibility depend on the Chart Color setting

Verified across both surfaces — `--chart-2` is used as a **UI surface carrying text**, not as a chart mark:
- `board.css:676` — `.chip.status[data-status="paused"]`, `background: var(--chart-2)` + `color: var(--foreground)`, at 11px
- `badge.svelte:19` — a `chart` badge variant, `bg-chart-2 text-foreground`
- `App.svelte:65` — idle status, `bg-chart-2`

Before PT-69 this was a defensible design choice, and `design-system-spec.md` records it deliberately ("Paused / In Review … pointed at an existing preset token rather than a new color" — the no-new-hues rule). It was safe because `--chart-2` was **fixed**.

PT-69 makes `--chart-2` **user-selectable** via the Chart Color dimension. The consequence is architectural, and it holds *independently of any contrast number*: **a status chip's appearance and legibility become a function of the user's chart-palette choice.** A person picking a Violet chart palette to make their burndown read better should not thereby restyle — or render illegible — the Paused status chip. Those two things have no product relationship. The gate found this by measuring contrast, but the defect would be real even if every combination passed.

## Ruling

1. **Do not re-derive the chart ramp to fix this.** `--chart-1..5` is a sequential data-viz ramp; PT-61 validated the chart-local ramp against `--card` for *marks*, and its re-derive fence covers `--chart-flow-*`, not the base ramp. Re-stepping a validated sequential ramp so that an 11px *status chip* stays readable is the tail wagging the dog — and it would have to be redone per chart variant, permanently coupling data-viz palette work to chip legibility.

2. **Repoint the status affordances off the Chart dimension.** The constraint is architectural and I'm ruling it: **a status chip must not draw its surface from a user-selectable Chart Color token.** Which token replaces it is a design call — ux-designer's, against the badge-variant vocabulary already in the spec (the "variant weight encodes meaning, not hue" rule is the relevant one, and it points at Base/Theme-dimension tokens that invert correctly with mode). Sites: `board.css:676`, `badge.svelte`'s `chart` variant, `App.svelte:65`.

3. **This removes `--foreground`/`--chart-2` from the failing set rather than accepting it.** It is not a third question for Mosko. The residue going to him stays the two genuine items — the already-live `--muted-foreground`/`--muted` light-mode miss, and the newly-reachable dark-mode `--destructive` failure.

4. **Not in scope, deliberately:** the *graphical* uses of chart tokens stay. `board.css:233`'s `.major-status-dot` and `ThemeSettings.svelte`'s `bg-chart-3` swatch are non-text objects under the 3:1 floor, and the latter is *supposed* to track the Chart dimension — it's the Chart Color indicator dot. Don't sweep those up in the repointing.

## Note on scope

This is a small diff (three sites) but it edits a design decision the spec records explicitly, so it needs ux-designer's sign-off on the replacement token and a spec amendment — not a silent find-and-replace. If it turns out the badge vocabulary has no clean home for "paused", that is worth its own issue rather than an improvised token here.

**qa-engineer:** keep the cross-dimension gate exactly as built. Once the repointing lands, this pair stops being generated at all (the co-occurrence disappears from source), which is the correct way for it to leave the gate — the derivation notices, and nobody maintains an exclusion list. Your `KnownCrossDimensionPairIsDetectedTests` will need retargeting at whatever cross-dimension pair remains, or retiring if none does; say so if none does, because that is itself worth knowing.

### @ux-designer — 2026-08-29

## Ruling — Paused/In Review status chip moves off `--chart-2` to `--accent`/`--accent-foreground`

Per architect's ruling: a status chip can't draw its surface from a user-selectable Chart Color token, since a chart-palette pick has no product relationship to a status chip's readability. Design decision on the replacement:

**New pairing: `--accent`/`--accent-foreground`.** Base-Color-owned (inverts correctly per mode, unlike `--chart-2`), unused elsewhere in the badge vocabulary, and a clean trivial contrast pass in both modes — light 0.216 vs 0.97, dark 0.985 vs 0.268, low chroma throughout. Deliberately **not** `--muted`/`--muted-foreground`: that pairing is separately recorded in this doc's Accessibility section as *failing* AA (4.39:1) — routing a new badge through an already-broken pair would've been the wrong direction.

**Judgment call, flagging explicitly:** `--accent` doubles as this system's hover/active surface (DropdownMenu items, etc.) elsewhere. Reusing it as a static badge fill is a role overload I'm accepting rather than proposing a new token — badges live on cards, not inside interactive menus, so I don't think the two contexts actually collide for a user, but it's a real overload and someone may see it differently.

**Three call sites** (per architect's read of the tree): `board.css`'s `.chip.status[data-status="paused"]`, `badge.svelte`'s custom variant (**recommend renaming it off its old `chart` name** — keeping "chart" in the name after the token moves off Chart Color would mislead the next reader), `App.svelte`'s idle state. Also updated the row's dot/accent color to match the new fill (`--accent`), consistent with every other row in the table (dot = fill token, except where fill is transparent).

**Confirmed out of scope, unchanged:** `.major-status-dot` and the theme-settings dropdown's own Chart Color indicator swatch — non-text graphical objects that are *supposed* to track Chart Color.

`docs/DESIGN/design-system-spec.md` updated: record-status table row, new rationale paragraph (with the contrast numbers architect gave — 10.32:1 light → 1.83:1 dark on the old pairing), the "chart/foreground-tier" → "accent/foreground-tier" phrasing everywhere it appeared (Components table, Legacy/migration table, the variant-weight rule), and the Accessibility contrast-table row + open-items bullet.

The badge vocabulary had a clean home for "paused" — no follow-up issue needed.

### @ux-designer — 2026-08-29

## Addendum — border guard adopted, spec updated with verification numbers

Taking architect's structural suggestion: the Paused/In Review chip keeps its `1px solid var(--border)` outline rather than following the other filled variants' `border-color: transparent` pattern. Reasoning I'm landing on: it's cheap insurance against the accepted `--accent` role-overload — if status chips ever land in a hoverable row (plausible; the list currently renders status as plain text), the border keeps the chip visible instead of silently disappearing into an identical hover fill. I'd rather have a guard that doesn't depend on anyone remembering this thread exists than optimize for pattern-consistency with `.chip.assignee`/`.chip.release`.

Also folded architect's verification into the spec: contrast checked across all 7 Base variants × both modes (worst case 13.79:1, best 16.42:1 — not just the Stone default), and the hover-collision check (--accent renders as a surface in exactly 3 places, none of which render chips today, so the role overload has no live collision site). `docs/DESIGN/design-system-spec.md` is current.

### @team-lead — 2026-08-29

**Ruled by Mosko (2026-08-29): darken the ink.** Resolution of the AA residue (muted-foreground on muted canvas, 4.39:1 light vs the 4.5:1 anchor, Stone default + 5/6 alt bases): derive `--muted-foreground` slightly darker per base until every RENDERED pair clears 4.5:1 in both modes, validator-checked — the gate then holds with zero exemptions and no recorded debt. Precedent: the chart-flow ramps (this same feature) already deviate from the preset with recorded rationale; one derived ink token per base is the same class. The derivation is a per-base mechanical darkening (minimum shift that clears the floor on that base's --muted), owned by gen_variants.py so it's reproducible, with before/after values recorded in the spec's Accessibility section. Applies to the shipped default too — this FIXES a live A11y miss dating to 2026-08-26, not just the new variants. (Architect's drop fence untouched: it now governs only genuine per-variant defects that survive the ruled gate.)

### @architect — 2026-08-29

**Architect ruling — `--destructive-foreground` splits per mode (gating).** implementation-lead's premise is **correct and I verified it**: no single ink value clears both modes. It is only a blocker if the ink is constrained to one value, and nothing constrains it.

## The premise checks out

Computed from the OKLCH source:

| ink | on light fill `#e7000b` | on dark fill `#ff6467` |
|---|---|---|
| current `oklch(0.97 0.01 17)` | 4.35 ✗ | 2.64 ✗ |
| pure white | **4.76 ✓** | 2.89 ✗ |
| near-black `L≈0.15` | 4.16 ✗ | **6.85 ✓** |

Every single-ink option fails one mode. The two fills sit on **opposite sides of the lightness midpoint** — light mode's `--destructive` is a *dark* red (L 0.577), dark mode's is a *light* red (L 0.704) — so they demand ink in opposite directions. No amount of searching one value finds an answer, which is exactly what implementation-lead hit.

## The fix: the ink was never required to be one value

`:root` and `.dark` are separate blocks and **each already declares its own `--destructive-foreground`**. They happen to carry identical values today; nothing requires that. The preset itself splits `--destructive` per mode. **Keeping the ink identical while the fill inverts is the anomaly — not the fix.**

Both directions have headroom once the ink is allowed to differ:
- **Light mode** — ink must go *lighter*: pure white gives **4.76:1**.
- **Dark mode** — ink must go *darker*: `L≈0.30` gives **4.81:1**, `L≈0.25` gives **5.62:1**.

**Principle, stated because it will recur:** per-mode *identity* of a token is a **result, never an input**. PT-61's chart ramp is identical in `:root`/`.dark` precisely because it was validated in both and passed — "verified, not assumed," in that ruling's own words. Run the same check here and it yields the opposite answer. Same discipline, different outcome; don't read PT-61's identical blocks as a convention to preserve.

## Mechanism

Add `derive_destructive_foreground()` beside the existing `derive_muted_foreground()` in `gen_variants.py`, with one difference that must not be copy-pasted away: **`derive_muted_foreground()` only moves lightness downward** (all its failures are in that direction). This one is **directional per mode** — lighten in light mode, darken in dark mode. A function that only darkens will fail light mode; one that only lightens will fail dark mode. Same `raise` on exhaustion.

`--destructive` takes exactly two distinct values across the whole matrix (per-mode, constant across all 7 Base variants — verified in `variants.css`), so this derives **per mode only**, not per (base, mode). Two values, not fourteen.

## Two consequences worth naming

1. **Light mode's ink is pinned at essentially pure white.** White is 4.76:1 and near-black is 4.16:1, so lighter is the *only* viable direction and there's no room left for the preset's slight warm tint (`oklch(0.97 0.01 17)` → ~`oklch(1 0 0)`). That is a small, real visual change — the pink cast on destructive-chip text disappears in light mode. ux-designer should know rather than discover it. If losing the tint is unacceptable, the alternative is darkening the light-mode `--destructive` *fill* to buy the ink room — but that changes the brand danger colour, and Mosko's `--muted` precedent chose ink over fill, so ink is the default unless she objects.
2. **This is not chip-only.** `.engine-stale-banner` (13px, still normal text) also pairs `--destructive` with `--destructive-foreground`, so the split fixes it in the same stroke. Don't scope the change to the chips.

## Status

With this, the dark-mode `--destructive` failure stops being a Mosko product call — it has a clean structural fix, same as `--chart-2` did. That empties the escalation queue: `--muted-foreground` was already ruled and shipped, and this one resolves in-team. **qa-engineer:** expect the gate to go green by the values changing, not by any threshold moving.

**And the standing caveat, unchanged and now the last thing between PT-69 and done:** every dark-mode number in this feature — including all of the above — comes from computed token values. The board's dark mode has still never been rendered in a browser. That leg is load-bearing.

### @ux-designer — 2026-08-29

## Ruling — light-mode `--destructive-foreground`: pure white, tint dropped

Design call on architect's ruling: accepted the direction-correct fix — light-mode ink moves to pure white (`oklch(1 0 0)`, 4.76:1), dropping the preset's slight warm-pink tint on destructive text. Rejected darkening the `--destructive` fill instead: that changes the brand danger hue itself, a bigger and more visible departure than losing a chroma-0.01 tint that's barely distinguishable from white to begin with, and it cuts against this doc's "preset tokens only" discipline more than moving the ink does.

Checked for a middle ground before settling: a faint tint is technically still possible (e.g. `oklch(0.99 0.01 17)` clears at 4.58:1), but with less margin for a benefit nobody would likely notice — not worth the fragility. Going with clean pure white.

`docs/DESIGN/design-system-spec.md` updated: Foundations light-mode table row, the Accessibility contrast table, and the dedicated light-mode resolution paragraph (previously "intentionally left open"). Nothing open on this pair anymore.

**Separate flag, not a ruling:** independently re-derived the dark-mode value's contrast using my own OKLab→linear-sRGB implementation and got **4.4955:1** for `oklch(0.3145 0.01 17.0)` vs the dark `--destructive` fill — a hair *under* the 4.5:1 floor, not the claimed 4.53:1 (~0.8% relative gap). Likely a matrix/rounding difference between two independently-written contrast bridges rather than a real defect, but it's a hard-floor gate, so worth a re-check against whichever tool `gen_variants.py`'s guarding test actually uses before trusting either number. Noted inline in the spec; not blocking, not touching the dark-mode value myself since I don't own that derivation.

### @architect — 2026-08-29

**Architect ruling — derived inks must clear the floor under BOTH colour models (gating; affects an already-shipped value).** ux-designer flagged a 0.8% disagreement on the dark destructive ink and declined to dismiss it as rounding. That was the right call. Resolved, and it is not rounding — it is two *different, both-correct* computations, and it has already shipped in a second token.

## Nobody's bridge is wrong

For `oklch(0.3145 0.01 17.0)` on the dark `--destructive` fill:

| model | result |
|---|---|
| unquantized float sRGB | **4.4955:1** — fails |
| quantized 8-bit (`#362f30` on `#ff6467`) | **4.5268:1** — passes |

ux-designer's implementation computes contrast on continuous sRGB. The generator's rounds through 8-bit hex first. Both are internally correct; they answer different questions. The gap is ~0.03, and the floor sits inside it.

## Which is authoritative — and why that's the wrong question to settle

A browser on a standard sRGB display renders the 8-bit value, so **the quantized number is what a user actually sees today**, and by that measure the value passes. But the margin is **0.027**, higher-bit-depth and wide-gamut rendering paths don't necessarily quantize identically, and the difference between the two models is larger than the margin itself.

**Ruling: never ship a value whose pass/fail verdict depends on which colour-precision model you compute it in.** At a hard floor that isn't pedantry — it's the only claim that survives a change of display, browser, or measuring tool. The derivation must satisfy `min(float, quantized) ≥ floor`, with an explicit epsilon on top.

## The part that matters more: this already shipped

I ran the same two models against the **already-ruled, already-merged** `--muted-foreground` fix:

| `oklch(0.5485 0.013 58.071)` on `--muted` | result |
|---|---|
| unquantized float | **4.4921:1 — fails** |
| quantized 8-bit | 4.5121:1 — passes |

**Identical shape, identical razor margin, already in the tree.** This is not a coincidence: `derive_muted_foreground()` steps until the first value that clears *by its own single model* and stops. By construction it lands every derived token within a hair of the floor. Both derived values in this feature sit under 0.03 of margin, and both fail under one of the two models.

## What changes

1. **`derive_*_foreground()` gains a two-model check plus a margin.** Accept a candidate only when it clears under *both* the quantized and unquantized computations, and require `floor + ε` rather than `floor`. **ε = 0.05** — comfortably larger than the ~0.03 inter-model spread, small enough to cost nothing visually. Stopping at the first bare pass is the actual defect; the model disagreement is just what exposed it.
2. **Dark `--destructive-foreground`: `L 0.3145 → 0.3100`** (float 4.5694, quantized 4.5970 — clears both). Anything down to 0.29 also works if a darker ink reads better.
3. **`--muted-foreground` must be re-derived and re-shipped.** `L 0.5485` does not survive this rule. It is a small darkening on a value Mosko already ruled — the *decision* he made (move the ink, not the surface) is unchanged and does not need revisiting; only the derived number moves. Flagging it as a correction to shipped code, not a reopened question.
4. **qa-engineer:** the contrast gate should apply the same two-model rule. A guard that computes one model can certify a value the other model fails — which is precisely how both of these got through, since the guard and the generator share a bridge and therefore share a blind spot.

## Credit where it's due

This surfaced only because ux-designer wrote an **independent** implementation instead of reusing the generator's, and then reported a sub-1% disagreement rather than rounding it away. A shared bridge between generator and guard cannot detect its own bias; the second implementation is what made the bias visible. Worth remembering the next time a duplicated computation looks like waste.

**Unchanged and still outstanding:** every number above is computed, not rendered. The board's dark mode has still never been opened in a browser.
