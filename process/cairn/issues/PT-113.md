---
id: PT-113
title: Test-run hooks ignore versioned interpreters — a run invoked as python3.14 run_tests.py records nothing
status: in-progress
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: null
created: 2026-09-08
updated: 2026-09-08
---


## Comments

### @team-lead — 2026-09-08

Architect's follow-up candidate on PT-107 addendum 1 @ab936fa (2026-09-08): `_test_run_shared._is_python_token` matches literal `python`/`python3` (basename, so absolute paths pass) but not `python3.14`-style suffixes, so a real run typed that way passes the PreToolUse guard untouched and the PostToolUse hook no-ops — the runner's own line keeps `who: null`. Widening detection widens the guard too (a teammate's un-tiered `python3.14 -m unittest` would then be refused, which is correct). Fix: accept `python3(\.\d+)?` basenames in `_is_python_token`; tests for guard and record hooks with the versioned form. Backlog, not PT-0.12.2 (Mosko routes new follow-ups).

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-113-hooks-versioned-python`. Mosko (2026-09-09): backlog PT-111–114 folded into PT-0.12.2, merges pre-approved, summary at the end.

**Gate 1 brief (architect):** `.claude/hooks/_test_run_shared.py::_is_python_token` matches literal `python`/`python3` basenames only, so `python3.14 scripts/cairn/run_tests.py --gate green` passes the PreToolUse guard untouched and the PostToolUse hook no-ops (`who` stays null). Rule on: (1) the accepted basename shape — `python3(\.\d+)?` (and `python` bare), absolute paths already pass by basename; whether `py`/`pypy3` are in scope (recommend no); (2) both hooks share the token via `_test_run_shared`, so one change widens the guard too — confirm that is the intent (a teammate's un-tiered `python3.14 -m unittest` is then refused, which is correct); (3) guards: guard hook refuses an un-tiered versioned full run, record hook patches `who` for a versioned run, existing literal-`python3` cases unchanged, and the PT-107 fixture's `is_test_invocation` assertion still holds. Writers: qa → hook tests; implementation-lead → `_test_run_shared.py`.
