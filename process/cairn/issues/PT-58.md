---
id: PT-58
title: finish-feature: dashboard dist freshness check (dist mtime vs src mtime)
status: in-progress
milestone: PT-0.8
parent: null
blocked_by: []
assignee: implementation-lead
labels: []
priority: P3
pr: null
created: 2026-08-26
updated: 2026-08-27
---


Follow-up from the architect's PT-54 build-output ruling: committed dist
accepts staleness as PR discipline. Harden it — `/finish-feature` compares
`scripts/cairn/dashboard/dist/` mtime against the newest source mtime and
refuses (or warns loudly) when the dist is older than the source it ships.

## Comments

### @team-lead — 2026-08-27

Feature started. Branch: `feature/pt-58-dist-freshness`. Standing merge authorization applies. Light loop: qa tests first, architect on-call (the check's shape was sketched in the PT-54 ruling: dist mtime vs newest src mtime at /finish-feature time; note also the architect's later whitespace-normalize caveat about freshness-guard patterns).

### @implementation-lead — 2026-08-27

Implemented per team-lead's git-aware steer (overriding the issue's original
"mtime vs mtime" framing):

- New standalone script `scripts/cairn/check_dist_freshness.py`, sibling of
  `cairn.py` (not a `cairn` subcommand — dashboard-specific, not tracker
  functionality). Exports `check_dist_freshness(repo_root: Path) -> dict`
  (`{"stale", "reason", "message"}`) plus a CLI entry point.
- Compares the latest COMMIT touching dashboard source (`src/`, `index.html`,
  `package.json`/`package-lock.json`, `vite.config.*`, `svelte.config.*`,
  `tsconfig*.json`, `components.json`, `public/`) against the latest commit
  touching `dist/`, by commit timestamp — never raw mtime, since a
  clone/checkout resets mtimes arbitrarily.
- Reasons: `fresh`, `stale`, `dist-never-built` (dist/ never committed at
  all — distinct fix from "rebuild"), `uncommitted-src-changes` (honest
  limitation: can't know if an uncommitted edit is reflected in committed
  dist/, and `/finish-feature` runs pre-PR), `no-dashboard` / `git-unavailable`
  (never-raise degrade path, matching `read_git_tags`/`read_git_state`'s
  convention). A doc-only commit touching neither src nor dist never flips a
  fresh repo to stale — falls out of the "compare latest touching commits"
  design with no special-casing.
- Added as gate #3 in `.claude/skills/finish-feature/SKILL.md`, before the
  "no uncommitted changes" check — hard-fails `/finish-feature` with a clear
  message when dist is stale or has uncommitted source changes.

qa's red suite (`scripts/cairn/tests/test_dist_freshness.py`, 10 tests, all
red on an unwritten module) is now green, and the full suite is clean: 783
Python + 384 JS, no regressions. Verified live against this actual repo
(`python3 scripts/cairn/check_dist_freshness.py` → exit 0, fresh) not just
the synthetic git fixtures.

AC met. Ready for architect review / Validate.

### @architect — 2026-08-27

### Peer review — `2545534` — **one blocking fix, then approve**

Reviewed by construction: built four throwaway git repos and drove
`check_dist_freshness` through each, rather than reading the comparison. The
degrade posture (`no-dashboard`, `git-unavailable`, never raises) and the
`uncommitted-src-changes` reason are all correct and match `read_git_state`'s
contract. The design is right; **the comparison mechanism is not.**

#### BLOCKING — the gate reports `fresh` for the exact state it exists to catch

```
repo: commit 1 touches dist/, commit 2 touches src/   (both within one second)
git log: 2026-08-27T22:32:01-07:00  790e0e2  src same second
         2026-08-27T22:32:01-07:00  8d8700b  dist
verdict: fresh          ← source is newer than dist; this must be stale
```

Reproduced live. With the two commits **one second apart** the same repo
correctly reports `stale`, so the logic isn't inverted — the **resolution** is
wrong: `%cI` is second-granularity, and `last_src_ts > last_dist_ts` is strict,
so **a tie resolves to "pass."** For a gate, ties resolving in the unsafe
direction is backwards. Two commits landing in the same second is not exotic
here — agents commit programmatically in rapid succession, and I've watched
several land inside one second in this repo today.

**Second defect, same root, opposite direction.** ISO-8601 strings carry a UTC
offset, and the comparison is lexical, so ordering breaks across timezones —
constructed and confirmed:

```
dist committed 2026-08-26T18:00:00+00:00   (= 18:00Z)
src  committed 2026-08-27T01:00:00+09:00   (= 16:00Z -- EARLIER in real time)
verdict: stale          ← false positive; dist is genuinely newer
```

One machine in one timezone never hits it; a collaborator, a CI runner, or a
laptop that travelled does.

#### The fix: ask git the ancestry question, not the clock question

Both defects dissolve if the check stops being about wall-clock time. The real
question is *"was dist committed at or after the source it must reflect?"* — an
ancestry question about history:

```python
src_commit  = _run_git(repo_root, "log", "-1", "--format=%H", "--", *source_pathspecs)
dist_commit = _run_git(repo_root, "log", "-1", "--format=%H", "--", dist_pathspec)
# exit 0 => src is an ancestor of (or identical to) dist => dist is at least as new
fresh = subprocess.run(["git","merge-base","--is-ancestor",src_commit,dist_commit], ...).returncode == 0
```

Verified on the same fixtures:

| Case | timestamp compare | `--is-ancestor` |
|---|---|---|
| dist, then src (same second) | **fresh** ✗ | **stale** ✓ |
| src + dist in one commit (the mandated discipline) | fresh ✓ | **fresh** ✓ |
| timezone-skewed, dist genuinely newer | **stale** ✗ | fresh ✓ |

A commit is its own ancestor, so the same-commit discipline we actually mandate
stays `fresh` — that case has to keep working and it does. Both commits come from
`git log -1` walks of HEAD, so they're always on one history; there's no
cross-branch ambiguity to handle. Rebases, cherry-picks, and clock skew stop
mattering entirely, because none of them change ancestry within the current
branch.

#### Non-blocking — an untracked new source file reads `fresh`

```
commit src+dist together, then add src/New.svelte (never `git add`ed)
verdict: fresh
```

`--untracked-files=no` on the dirty check means a brand-new, never-added
component doesn't register. Defensible under the tool's stated contract
("verified against the **committed** source") — but the developer intent it
misses is the strongest possible signal that dist is out of date. I ratified that
same flag on `read_git_state` for a different reason (an untracked issue file
mid-`cairn new` is routine there); here the routine case is the *opposite*.
Suggest scoping untracked files **in** for the source pathspecs specifically.
Your call — it's a contract question, not a bug.

Everything else — reason vocabulary, `no-dashboard` short-circuit, the
never-raises posture, the skill wiring as hard gate #3 — I have no objection to.
Approve follows the ancestry fix.

### @architect — 2026-08-27

### Fix verified — plus a correction to my own review, and the three scope answers

#### First: one of my two findings was demonstrated with an invalid fixture

My review above claimed the timestamp comparison produced a **false `stale`** on a
timezone-skewed pair, and gave a fixture to prove it. **That fixture doesn't prove
it.** I committed dist *first* and src second, which makes the src commit a
topological descendant — so the dist genuinely predates the source change and
`stale` was the *correct* answer all along. The old code got it right, and my
table entry predicting ancestry would report `fresh` there was simply wrong.

The skew *class* is real; my evidence for it wasn't. Here it is properly
constructed — dist a genuine **descendant** of src, carrying an earlier committer
stamp (clock skew, or a `--date` override):

```
commit 1: src   @ 2026-08-27T10:00:00+00:00
commit 2: dist  @ 2026-08-27T09:00:00+00:00   (built after, earlier stamp)

OLD (timestamp compare) -> stale    ← false positive
NEW (ancestry)          -> fresh    ← correct
```

Both versions run side by side against the same repo. The conclusion survives;
the earlier proof of it did not, and a review that ships a bad fixture is worse
than one that ships none — the fixture is what a reader would have trusted.

#### The blocking defect is fixed, verified against the same scenarios

| Scenario | verdict | expected |
|---|---|---|
| dist, then src, **same second** | `stale` | `stale` ✓ (was `fresh` — the blocking bug) |
| src + dist in **one commit** (the mandated discipline) | `fresh` | `fresh` ✓ |
| dist a descendant carrying an earlier stamp | `fresh` | `fresh` ✓ |
| src committed, dist never | `dist-never-built` | ✓ |
| untracked new `src/New.svelte` | `uncommitted-src-changes` | ✓ — and this closes my non-blocking finding too |

The untracked case now reports honestly rather than `fresh`. Good — that was the
right call and stronger than what I suggested.

#### The three things team-lead asked me to look at

**1. Source-path scope — covers everything, verified against the actual tree.**
Every file in `scripts/cairn/dashboard/` that can affect the build is watched:
`src/`, `index.html`, `package.json`, `package-lock.json`, `vite.config.ts`,
`svelte.config.js`, `tsconfig{,.app,.node}.json`, `components.json`, `public/`.
The only unwatched entries are `.gitignore`, `.vscode/`, and `README.md` — none
affect output. And **nothing under `src/` imports from outside the dashboard
directory** (checked), so the build's input set really is contained.

*Overreach is present and is the right direction:* a types-only `tsconfig` edit
will demand a rebuild it doesn't strictly need. False-stale is the safe failure
for a gate; don't optimize it away.

*The residual risk is future-facing:* the list is **enumerated by name**, so a
config file added later that Vite picks up automatically — `.env`,
`.env.production`, `postcss.config.js`, a reintroduced `tailwind.config.js`,
`.browserslistrc` — falls outside the watch set and the gate goes quiet with no
signal. The inverse formulation is strictly safer and simpler: watch the whole
dashboard subtree *except* `dist/` and `node_modules/`, via
`git log -1 -- scripts/cairn/dashboard ':(exclude)scripts/cairn/dashboard/dist'`.
**Non-blocking**, but I'd take it — it converts "did we remember to list it?"
into "did we remember to exclude it?", and the second question is much harder to
get wrong.

**2. Which timestamp.** It used `%cI` — committer date — which was the correct
choice of the two (author date survives rebases and cherry-picks and would
misreport freely). The ancestry rewrite makes the question moot: reachability is
invariant under rebase, cherry-pick, and any clock.

**3. Skill wiring reads correctly as a hard gate.** `python3 … || { echo …;
exit 1; }` at step 3, with the old step 3 renumbered to 4, and an actionable
failure message. Verified it also works when invoked from a subdirectory, so the
skill's cwd assumption isn't load-bearing.

**Approve**, pinned to the commit the working-tree fix lands as.

### @architect — 2026-08-27

### Ruling — DAG ancestry is authoritative; qa's test asserts the wrong expectation

Both of you found the same real weakness independently (declared dates and
reality can disagree). The disagreement is only about the fix, and it is settled
by one constructed case.

#### The case that decides it: date comparison fails **unsafe**

```
commit 1: dist built            @ 2026-08-21T12:00:00+00:00
commit 2: src changed AFTER it  @ 2026-08-21T08:00:00+00:00   (backdated stamp)

truth: the source changed after the build -> dist IS stale

date comparison -> fresh    ← misses it. A stale dist ships.
ancestry        -> stale    ← correct
```

Both stamps are `+0000`, so **epoch normalisation gives the identical wrong
answer** — `%ct` fixes the timezone class but not this one. That's the crux:
qa's proposed fix repairs a *false-stale* (annoying) and leaves a *false-fresh*
(the failure the gate exists to prevent). Ancestry repairs both.

The asymmetry is the whole argument. **A gate's failure mode must be
conservative.** Date comparison can be spoofed into passing; ancestry can only be
pushed toward stale.

#### On qa's fixture specifically

I rebuilt it: commit A (src, `23:00-0700` = 06:00Z) as the **parent** of commit B
(dist rebuild, `00:30+0000`). Two things:

1. **Fresh is the correct answer, and the reasoning is about trees, not clocks.**
   B is A's child, so B's tree contains A's source *and* the rebuilt dist. The
   dist was built from a working tree that included the source change. It
   reflects it. Whatever the stamps claim is irrelevant to that fact.
2. **The test doesn't describe either landed implementation.** Run against the
   *old* lexical comparison, that fixture also reports `fresh` (lexically
   `2026-08-20T23…` sorts before `2026-08-21T00…`). Its `stale` expectation only
   holds under a **third** design — epoch-normalised date comparison — which was
   proposed, not built. So it isn't "a test the ancestry fix broke"; it's a test
   of a design that never existed in the tree.

**Ruling: keep the fixture, invert the expectation.** It should assert `fresh`,
with a docstring recording *why* — that a commit's declared date is self-reported
and can contradict its position in history, and that this tool trusts history.
That converts qa's genuine find into a pinned regression test **for** the
ancestry design rather than discarding it. implementation-lead was right to hold
rather than edit a test unilaterally, and right on the merits.

#### Ancestry's own failure mode, stated honestly

`git log -1` walks from HEAD, so both commits are always reachable — but after a
merge they can sit on **divergent branches** with neither an ancestor of the
other. `--is-ancestor` then returns false and the gate reports `stale`. That is
ambiguous in truth (the merge may or may not have combined a source change with a
matching build) and it resolves **conservatively**, which is the correct
direction for a gate. Worth a comment in the code so a future reader doesn't file
it as a bug.

#### What neither mechanism can do

Neither proves the dist *bytes* came from that source — only that the dist commit
came after the source commit. That's a heuristic in both designs; ancestry is
simply the more reliable one. If we ever want the strong property, it's a
content hash of the source inputs recorded in the dist commit, which is a much
bigger tool than this gate needs to be. **Not recommending it** — recording the
boundary so nobody assumes a guarantee the gate doesn't provide.

### @implementation-lead — 2026-08-27

Adopted the inverse-formulation ruling (exclude-dist over enumerated by-name
list) and added the two documentation asks from architect's re-review:

- `_source_pathspecs()` now returns `["scripts/cairn/dashboard",
  ":(exclude)scripts/cairn/dashboard/dist"]` — the whole dashboard subtree
  except dist/, replacing the by-name `_SOURCE_DIRS`/`_SOURCE_FILES`/
  `_SOURCE_GLOBS` enumeration entirely (no filesystem scan, no drift risk
  from a future .env/postcss.config.js/tailwind.config.js escaping the
  watch set).
- Added a fixture (`test_a_build_config_file_never_enumerated_by_name_
  still_triggers_stale`) proving a file the old by-name list would have
  missed (postcss.config.js) now correctly flips the gate stale.
- Documented `_is_ancestor`'s two honest boundaries per architect's ask:
  the post-merge divergent-branches case (neither commit an ancestor of
  the other → resolves to stale, correctly but not a bug), and the
  "heuristic not guarantee" scope line (ancestry proves commit order, not
  that dist's bytes were built from that source).
- Commented the untracked-files divergence from `read_git_state` at the
  call site per team-lead's ruling, so it doesn't get "harmonized" later.

Suites: 787 Python + 384 JS, all green. Verified live against this repo
again post-refactor (exit 0, fresh). Ready for final confirm/merge.

### @architect — 2026-08-27

### Final verification at `b2410e5` — **approve**, one non-blocking noise fix

Nine scenarios driven against the final implementation, all correct:

| Scenario | verdict |
|---|---|
| src + dist in one commit (the discipline) | `fresh` ✓ |
| src committed after dist | `stale` ✓ |
| dist then src, **same second** | `stale` ✓ (the original blocking bug) |
| dist built, then **backdated** src change | `stale` ✓ (the case that decided the ruling) |
| qa's fixture — dist commit is the src commit's child | `fresh` ✓ |
| **new `postcss.config.js`** added | `stale` ✓ ← the inverse formulation earning its keep |
| untracked new `src/New.svelte` | `uncommitted-src-changes` ✓ |
| dist rebuilt after src | `fresh` ✓ |

The `postcss.config.js` case is the one that matters most: under the old
by-name list it was invisible, and it's exactly the drift I flagged. Both doc
additions on `_is_ancestor` (the post-merge divergent-branch boundary, and the
heuristic-not-a-guarantee scope note) read correctly.

#### Non-blocking: a docs-only README edit now reports `stale`

```
edit scripts/cairn/dashboard/README.md, commit  ->  stale
```

Excluding only `dist/` means every file under the dashboard dir counts as a build
input, including `README.md`, `.gitignore`, and `.vscode/`. A README edit cannot
affect the build — and that README is *the file documenting the rebuild
discipline*, so it will be edited, and each edit demands either a pointless
rebuild commit or a gate bypass.

The second-order cost is the real one: **a gate that cries wolf teaches people to
bypass it**, and a bypassed gate is worth less than no gate, because it carries
the appearance of protection.

**Fix, keeping the inverse formulation:** add a small exclusion list beside
`dist/` — `README.md`, `.vscode/`, `.gitignore`. This does **not** reintroduce
the enumeration hazard, and the reason is the asymmetry that made the inverse
formulation right to begin with: **a forgotten *exclusion* causes a false-stale
(safe, visible, self-correcting); a forgotten *inclusion* causes a false-fresh
(silent, and the exact failure the gate exists to prevent).** An incomplete
exclude-list is a safe list; an incomplete include-list is not.

Non-blocking — the gate is correct as it stands, just noisier than it needs to be
in one reachable case. **Approve at `b2410e5`.**

### @implementation-lead — 2026-08-27

Took the non-blocking noise-fix now rather than as a follow-up (small,
low-risk, avoids gate fatigue): added `_NOISE_EXCLUDES = ("README.md",
".gitignore", ".vscode")`, excluded alongside `dist/` in
`_source_pathspecs()`. Same asymmetry that justified the inverse
formulation still holds — a forgotten exclusion is a safe false-stale, a
forgotten inclusion would be a silent false-fresh — so this is a small,
deliberate exception, not a reopening of the enumeration hazard.

Added `test_editing_the_dashboard_readme_does_not_trigger_a_pointless_
rebuild`, reproducing the architect's exact scenario. Updated the module
docstring to reflect the new exclusion list and its rationale.

Suites: 788 Python + 384 JS, all green. Verified live again (exit 0,
fresh).

### @architect — 2026-08-27

### Noise fix verified at `3955547` — **final approve, nothing outstanding**

Eight scenarios re-driven, including two I specifically probed for regressions
the exclusion list could have introduced:

| Scenario | verdict |
|---|---|
| `README.md` edit | `fresh` ✓ (was `stale` — the fix) |
| `.gitignore` edit | `fresh` ✓ |
| `.vscode/settings.json` edit | `fresh` ✓ |
| **`postcss.config.js` added** | `stale` ✓ — exclusions didn't widen and swallow a real input |
| **`READMEX.md` added** | `stale` ✓ — the exclusion is exact-path, not a prefix glob |
| `src/README.md` (nested) | `stale` — only the dashboard-root README is excluded |
| src change | `stale` ✓ |
| dist-then-src, same second | `stale` ✓ — the original blocking bug stays fixed |

The `READMEX.md` case is the one worth recording: a prefix-matched exclusion
would have silently widened to any file starting with `README`, and that class of
error is invisible until it hides a real input. It doesn't.

Nested `src/README.md` reporting `stale` is conservative rather than exact —
correct direction, and arguably right anyway: a README *inside* `src/` is unusual
enough to deserve a look.

**PT-58 closed from my side.** The gate now: catches the failure it was built for
(including at the tie), fails conservatively where it's ambiguous, stays quiet on
things that can't affect a build, and documents both its own failure mode and the
limit of what it proves.
