---
id: PT-74
title: Dashboard title bar horizontal padding should match the unified card width
status: done
milestone: PT-0.9
parent: null
blocked_by: []
assignee: null
labels: []
priority: null
pr: https://github.com/richmosko/project_template/pull/146
created: 2026-08-30
updated: 2026-08-30
---


## Comments

### @team-lead — 2026-08-30

Mosko's finding (2026-08-31), post-PT-73: the title bar (Project Dashboard header row — title, subtitle, branch/version chips, Refresh, theme trigger) still carries the old left/right padding; the cards below went full-bleed in PT-73. Align the header container's horizontal extent/padding to the same rule the unified sections use — no design decision, "match the cards below" is the spec.

### @team-lead — 2026-08-30

Feature started. Branch: `feature/pt-74-header-padding`. v0.9.0 waits on this micro-loop.

### @team-lead — 2026-08-30

**Scope note (Mosko, 2026-08-31): the header alignment must hold on BOTH shell routes** — the dashboard home AND /dashboard/issues. If the header wrapper is shared shell chrome the fix covers both automatically, but verify each route renders the aligned header rather than assuming; qa's guard and the browser leg check both pages.

### @qa-engineer — 2026-08-30

**QA sha-pinned approval — PR #146 @ 3371240 (last gate before PT-0.9 close)**

Confirmed head via `git rev-parse HEAD` — `3371240` (fix commit `518bb12` + tracker-status `3371240`), matching PR #146 exactly. Clean working tree.

Ran the full gate fresh, post-commit, clean shell:
- Python: `python3 -m unittest discover -s tests` — **1045 tests, 0 failures, 0 errors, 1 expected skip**.
- JS: `node --test tests/js/*.test.js` — **384/384 passing**.
- `npm run build` — clean.
- `npm run check` (svelte-check + tsc) — **0 errors, 0 warnings, 929 files**.

The one-line fix confirmed real in source: the header wrapper's class list now matches the unified sections wrapper exactly (`mx-auto flex w-full flex-col gap-6`, `max-w-6xl` dropped). Verified structurally — not just asserted — that the header renders as shared chrome before the `/dashboard` vs `/dashboard/issues` route switch (`</header>` closes well before `{#if !onIssueTracking}` opens), so one guard on the single shared wrapper correctly covers both routes with no second variant to check.

**Approved.** This is PT-0.9's final gate.

### @team-lead — 2026-08-30

Validate passed; merging via PR #146. Closing. PT-0.9's final issue — milestone flips in this commit.
