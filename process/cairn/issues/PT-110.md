---
id: PT-110
title: Dashboard header is fixed to the top of the viewport and does not scroll with the page
status: in-review
milestone: PT-0.12.2
parent: null
blocked_by: []
assignee: implementation-lead
labels: [dashboard, ux]
priority: null
pr: https://github.com/richmosko/project_template/pull/202
created: 2026-09-08
updated: 2026-09-08
---

Mosko (2026-09-08): "Dashboard header should be fixed to top and not scroll."

**Where:** the shell header in `scripts/cairn/dashboard/src/App.svelte` (`<header class="flex flex-wrap items-center justify-between gap-4">`, ~L333) — the Sidebar.Trigger + "Project Dashboard" title/subtitle on the left, the theme/settings cluster on the right. It renders before the route switch, so it is shared chrome on both the Dashboard and Issue Tracking pages. Today it sits inside the `min-h-screen` flex column under `Sidebar.Inset` and scrolls away with the content.

**Acceptance criteria**

1. On both routes (`/dashboard`, `/dashboard/board` or whatever the Issue Tracking route is), scrolling the page content keeps the header at the top of the viewport: after scrolling to the bottom, the header's bounding rect still has `top` ≈ 0 and the title is `elementFromPoint`-visible (not just `position` reads — PT-32/PT-88 rule).
2. Content is not hidden under it: the first card's top edge is fully visible at scroll 0 (no overlap; a spacer or padding equal to the header height), and the page has no horizontal scrollbar at 1280 px and 768 px widths.
3. The header keeps the card canvas convention (opaque `--muted`/`--background` surface with the design system's border or shadow token so scrolled content does not bleed through), in light and dark, in two variants — browser-verified by the lead, screenshots on the issue.
4. The sidebar (`Sidebar.Provider`/`Sidebar.Inset`) still opens/collapses correctly with the header fixed; the Sidebar.Trigger remains inside the header and reachable.
5. The embedded board iframe on the Dashboard page (fixed height, owns its own scrolling) is unaffected; the standalone `/board` + `/list` zero-build fallback is out of scope.
6. `dist/` rebuilt and committed by implementation-lead; `check_dist_freshness.py` rc 0; a source scan test pins the header's positioning classes (sticky/fixed + top-0 + z-index token) so a later layout pass cannot silently drop them.

**Notes for the ruling:** `sticky top-0` on the header inside the existing flex column is the smallest change if the scroll container is the document; if `Sidebar.Inset` owns the scrolling, the header may need to move outside the scroll container instead — the architect decides which, and whether the subtitle line collapses at narrow widths.

## Comments

### @board — 2026-09-08

From Principal:

There is already an example of this component on shadcn-svelte: https://www.shadcn-svelte.com/blocks?preset=b6TWNYiDGE#sidebar-07

<script lang="ts">
  import * as Sidebar from "$lib/components/ui/sidebar/index.js";
  import AppSidebar from "$lib/components/app-sidebar.svelte";
  import ChartAreaInteractive from "$lib/components/chart-area-interactive.svelte";
  import DataTable from "$lib/components/data-table.svelte";
  import SectionCards from "$lib/components/section-cards.svelte";
  import SiteHeader from "$lib/components/site-header.svelte";
  import data from "./data.js";
</script>

<Sidebar.Provider
  style="--sidebar-width: calc(var(--spacing) * 72); --header-height: calc(var(--spacing) * 12);"
>
  <AppSidebar variant="inset" />
  <Sidebar.Inset>
    <SiteHeader />
    <div class="flex flex-1 flex-col">
      <div class="@container/main flex flex-1 flex-col gap-2">
        <div class="flex flex-col gap-4 py-4 md:gap-6 md:py-6">
          <SectionCards />
          <div class="px-4 lg:px-6">
            <ChartAreaInteractive />
          </div>
          <DataTable {data} />
        </div>
      </div>
    </div>
  </Sidebar.Inset>
</Sidebar.Provider>

### @team-lead — 2026-09-08

Feature started. Branch: `feature/pt-110-fixed-dashboard-header`.

**Gate 1 brief (architect):** Mosko's ask plus the six acceptance criteria are in the description; Mosko's board comment names the reference: shadcn-svelte block sidebar-07 (`SiteHeader` as the first child of `Sidebar.Inset`, `--header-height` on the Provider). Rule on: (1) the mechanism — `sticky top-0` on the existing `<header>` inside the flex column (App.svelte ~L333) vs restructuring to the sidebar-07 shape (header outside the scrolling content) — decided by measuring which element actually scrolls (document vs `Sidebar.Inset`); (2) the surface: opaque `bg-background`/`bg-muted` + border token so content does not bleed through, z-index token above the charts' tooltips (the token chart's tooltip is `z-50` — name the header's layer relative to it); (3) narrow-width behaviour (subtitle line) and the spacer/padding so the first card is not hidden at scroll 0; (4) the scan test qa pins (positioning classes) and the dist rebuild. Both routes (Dashboard, Issue Tracking) share this header. Writers: implementation-lead → App.svelte + dist; qa → the scan test. Visual leg (lead): both routes, scrolled to bottom, header rect top ≈ 0 and title `elementFromPoint`-visible, 1280 and 768 px, light and dark, two variants; screenshots on the issue. ux-designer is not spawned — ask if the ruling needs a design call beyond the sidebar-07 reference.

### @architect — 2026-09-08

**Ruling (gate 1).** Measured first, statically, because the mechanism turns on which element scrolls: **nothing in the shell path establishes a scroll container**, so the document scrolls and `sticky` will work where the header already is.

| element | classes that matter | overflow |
|---|---|---|
| `body` (app.css:346) | `bg-muted text-foreground` | none |
| `div[data-slot=sidebar-wrapper]` | `flex min-h-svh w-full` | none |
| `main[data-slot=sidebar-inset]` | `relative flex w-full flex-1 flex-col` | none |
| App.svelte page root / inner wrapper | `flex min-h-screen flex-col gap-6 bg-muted px-7 py-7` / `mx-auto flex w-full flex-col gap-6` | none |

`grep overflow` over all four returns nothing. **Note the method's limit:** this is a source read, not a browser measurement — I have no browser tools this session. AC1's `elementFromPoint` leg on the live board is the confirmation, and if the lead's visual leg finds the document *not* scrolling, this ruling's mechanism is the thing to reopen, not the styling.

**(1) `sticky top-0` on the existing `<header>` (App.svelte ~L333). Do not restructure to sidebar-07's shape.** The reference block matters for *where the header lives* — first child inside `Sidebar.Inset`, shared by both routes — and it already does: the header renders before the route switch. Moving it out of the padded `max-w` wrapper would change the page's horizontal rhythm and duplicate the padding rules, for a layout the document-scroll makes unnecessary.

**(2) AC2 needs no spacer, and must not get one.** `sticky` keeps the element's box in normal flow — the first card is never underneath it at scroll 0. A spacer or `padding-top` equal to the header height would double-count and push the first card down by a header's worth of dead space. The AC's "a spacer or padding equal to the header height" is satisfied by the mechanism, not by an added element.

**(3) Surface: `bg-muted` + `border-b border-border`, plus its own vertical padding.** `bg-muted` is what `body` is (app.css:346) — the header is page chrome sitting on the canvas, not a card, so `bg-background` would read as a floating bar. Opaque is the requirement; the border is what separates it from content passing underneath. No negative margins: the header spans the same width as the content inside the same `px-7` wrapper, so nothing scrolls through the side strips.

**Layer: `z-50`, the same tier as the tooltips — deliberately, not by omission.** Ordinary content (cards, charts) is `z-auto`, so the header wins over everything that scrolls under it, which is what AC3 asks. Against the portal'd overlays that are also `z-50` (chart tooltips, dropdown/select/popover/sheet — the codebase's established overlay tier), DOM order breaks the tie in the portals' favour, and that is the behaviour to want: **a header that outranks its own settings menu clips it.** The cost is that a chart tooltip can paint over the header in the rare position where it flips upward near the top edge — transient, and it reads as normal overlay behaviour. If the lead's screenshots show that reading as a defect, raise the header to `z-[60]` and accept the same bump for header-owned menus; that is one line, and the visual evidence should make the call rather than a guess now.

**(4) Narrow widths:** keep `flex-wrap`, and hide the subtitle below `sm` (`hidden sm:block` on the `<p>`). A wrapped two-line header pinned to the top eats viewport on exactly the screens with least of it; the title and the trigger are what must survive. No other responsive change.

**Scan test (qa)** on the `<header>` in App.svelte: `sticky`, `top-0`, `z-50`, `bg-muted`, `border-b` all present — **and a negative: the element must not be `fixed`**. `fixed` would satisfy a naive "header stays at top" reading while silently reintroducing the overlap AC2 forbids, which is precisely the regression a scan test exists to catch.

**dist:** rebuild and commit (source change). Post-PT-106 the rebuild is deterministic; `check_dist_freshness.py` must report rc 0 in the same commit.

**ux-designer: not needed.** Mosko's reference block fixes the pattern and every value here is an existing token. The one thing I would escalate is aesthetic, not structural: if the pinned header reads as a floating bar rather than page chrome in the lead's light/dark screenshots, that is a design call and worth a spawn then — not now.

**Scope** — `App.svelte` (the header element and the subtitle class) plus `dist/`; qa → the scan test. Writers: implementation-lead → source + dist; qa → test.

### @team-lead — 2026-09-08

**Visual leg (lead) @5d94640 — AC1 FAILS on the live board; cause measured.**

`/dashboard`, mist/blue light, viewport 1746×929. Computed on the `<header>`: `position: sticky`, `z-index: 50`, `background oklch(0.963 0.002 197.1)` (muted), `border-bottom 1px`. Scrolled to the bottom (`scrollY` 2144 of 3073): header rect top = **−2116 px** — it scrolled away with the page; title `elementFromPoint` → null; screenshot `screenshot-1788916244805-18.jpg` shows no header.

**Cause (ancestor walk, every ancestor checked for overflow/contain):** no ancestor has any overflow other than `visible` — the ruling's scroll-container read is correct, the document scrolls. What defeats sticky is the **containing block**: the header's parent is the PT-74 wrapper `div.mx-auto.flex.w-full.flex-col.gap-6` (App.svelte ~L332), which wraps ONLY the header — measured height **89 px**. A sticky element cannot leave its parent's box, so it "sticks" within 89 px and then leaves with it. The tall ancestor is the page root `div.flex.min-h-screen.flex-col.gap-6.bg-muted.px-7.py-7` (3073 px).

**Fix candidates, architect's call:** (a) move `sticky top-0 z-50` (+ surface) onto that wrapper div, whose parent is the 3073 px page root — smallest change, but qa's scan pins the classes on `<header`; (b) drop the header-only wrapper (its PT-74 purpose was width-matching, which `w-full` on the header itself gives) so `<header>` is a direct child of the page root and the scan stays as written; (c) sidebar-07's shape after all. Note the ruling's caveat was explicit: source read, not measured — this is that measurement. Not proceeding to the other cells until the mechanism holds.

### @architect — 2026-09-08

**Verdict (gate 4): NO-GO — AC1 fails, and the ruling is why.** The measured objection is accepted whole: I checked which element *scrolls* and never checked the sticky element's *containing block*. Nothing in the shell path bounds the scroll — that part holds — but the header's parent is the PT-74 wrapper that contains only the header (89 px), and a sticky element cannot outlive its parent's box. The caveat I attached ("source read, not a browser measurement — AC1's `elementFromPoint` leg is the confirmation") did its job; this is the ruling reopening on it, re-issued whole below. Suite state at 5d94640, for the record: 1566 tests OK parallel (29.3 s) and serial (137.5 s), `check_dist_freshness` rc 0 — all green, and all beside the point while the mechanism is wrong.

**Ruling, re-issued whole.**

**(1) `sticky top-0` stays the mechanism — the element carrying it must be a child of the tall page root.** Fix candidate **(b)**: delete the header-only wrapper `div.mx-auto.flex.w-full.flex-col.gap-6` and move `mx-auto w-full` onto the `<header>` itself, so the header becomes a direct child of `div.flex.min-h-screen…px-7 py-7` (3073 px measured). The wrapper is a flex column with a single child, so its `flex flex-col gap-6` does nothing today; its whole purpose (PT-74) was to give the header the same width rule as the sections below, and the header can carry that rule itself. Not (a) — keeping a wrapper whose only remaining job is to be the sticky element splits one concern across two elements and moves the pinning away from the tag a reader inspects. Not (c) — sidebar-07 remains a style reference; nothing measured requires its layout.

**(2) PT-74's invariant survives in substance and must be re-pointed, not relaxed.** `HeaderWidthMatchesSectionsTests._header_wrapper_class` reads the wrapper immediately preceding `<header`; with the wrapper gone it must read the **header's own** class string and keep every existing assertion (no `max-w-6xl` cap; the same width rule as the `{#if !onIssueTracking}` sections wrapper). Subset assertions, never string equality — the header legitimately carries more classes than the sections wrapper.

**(3) Unchanged from the first issue:** surface `bg-muted` + `border-b` (colour resolves to `border-border` via app.css:340's universal rule — that is not an omission), `py-4`, layer `z-50` on the portal'd-overlay tier, subtitle `hidden sm:block`, and **no spacer** — sticky keeps its box in flow. Never `fixed`.

**(4) New guard, the one that would have caught this — structural, not stylistic.** Pinning classes are necessary and not sufficient; the containing block is the mechanism. qa asserts, in `App.svelte`, that the `<header` opening tag is an **immediate child of the page-root div** (`flex min-h-screen … px-7 py-7`) with no intervening element once comments are stripped. Mutation: reintroduce any wrapper between them → red. Keep the existing pinning scan (`sticky`, `top-0`, `z-50`, `bg-muted`, `border-b`, never `fixed`) as-is; it was right, it was just not enough alone.

**(5) The browser leg is the acceptance, not a formality.** AC1 re-run at the new sha — header rect `top` ≈ 0 after scrolling to the bottom **and** the title `elementFromPoint`-visible — before this gate is asked again. A static scan can prove the header is a child of the tall root; only the browser can prove it stays put. The remaining cells (both routes, 1280/768, light/dark, two variants, first card at scroll 0, settings popover not clipped) follow once the mechanism holds.

**dist:** rebuilt again with the fix; `check_dist_freshness.py` rc 0 in the same commit.

**Scope** — `App.svelte` (delete the wrapper, move `mx-auto w-full` to the header), `dist/`, the re-pointed padding test, the new structural guard. Writers: implementation-lead → source + dist; qa → both tests.

### @architect — 2026-09-08

**Addendum 1 to the re-issued ruling (PT-110).** Both inbound points are already answered at 4faae8f — fix **(b)**, writers named, and the padding test's landmark re-pointed to the header's own class string. This addendum adds only what 4faae8f missed: implementation-lead-2 is right that a **second** test breaks, and it must be restated deliberately rather than adjusted to fit.

`test_nothing_is_inserted_immediately_after_the_header_closes` (`test_dashboard_header_pinning.py:102`) asserts the text after `</header>` starts with `</div>` — that `</div>` is the header-only wrapper's own close, the element (b) deletes. Restate it to the property it exists to protect, not the shape it happened to have: **the first non-whitespace, non-comment token after `</header>` must be `{#if !onIssueTracking}`** — the sections branch that already follows the wrapper today. A spacer div, a `h-*`-only element, or an `aria-hidden` shim inserted there fails it exactly as before, and the assertion message keeps naming why (sticky keeps its box in flow; a spacer double-counts).

Do not weaken it to "does not look like a spacer" — a positive assertion about the next token is what makes the mutation loud. Both restatements (this one and the padding landmark) are qa's, in the same red commit as the new structural guard; implementation-lead-2 keeps `App.svelte` + `dist/`.

svelte-check clean under (b) is a useful signal and not the acceptance: AC1 re-run at the new sha is, per item (5).

### @architect — 2026-09-08

**Addendum 2 to the re-issued ruling (PT-110) — the header's layer is `z-40`, not `z-50`.** Measurement wins over my reasoning: I argued the equal-tier tie would break in the portal's favour by DOM order, and the live board says otherwise — the popover occupies y 90–222 against a header box of 0–117, and `elementFromPoint` in the popover's top 27 px returns the header. A header that clips its own settings menu is the failure mode I named as the one to avoid, and at `z-50` it is the one we have.

**Rule: the header sits strictly below the portal'd overlay tier and strictly above content — `z-40`.** Ordinary content is `z-auto`, so scrolled content still passes under the header (AC3 holds); every portal'd overlay (`z-50`: dropdown, select, popover, sheet, chart tooltip) now paints over it, which is correct for all of them. The mobile sidebar Sheet is `z-50` and must be above the header — it is. The fixed sidebar rail is `z-10` and stays below — it is. Do not resolve this by raising the overlays instead: those are shared shadcn primitives, and re-tiering them to satisfy one page's chrome is a change with no bound on its blast radius.

**The escape hatch in the first ruling ("raise the header to `z-[60]` if the visual leg disagrees") is withdrawn — it was backwards.** It would have deepened exactly this clipping. Chart tooltips painting over the header near the top edge is the accepted trade, unchanged and now the only one.

**qa:** the pinning scan's `z-50` assertion becomes `z-40`, with the message naming the tier relationship (below the overlay tier, above content) so a future bump has to argue with the reason rather than the number. **implementation-lead-2:** `App.svelte` + `dist/`. This is addendum 2 of 2 — anything further reopens the ruling rather than extending it.

The re-run cells you have left (dark, zinc/emerald, 768 px, first card at scroll 0, popover unclipped at `z-40`) are the acceptance; `/dashboard/issues` not scrolling at all is expected — the board iframe owns its scrolling per AC5, and a header at rest there is the correct observation, not a miss.

### @team-lead — 2026-09-08

**Visual leg (lead) @152eb82 — AC1 PASSES after fix (b); surface cells pass; one delta (z-tier), ruled in addendum 2.**

Live board, viewport 1746×929 (the harness's window resize did not take effect — see the width note). Probe per cell: scroll to top → overlap check between the header box and every card/section; scroll to bottom → header rect top, title `elementFromPoint`; horizontal scrollbar check.

| Cell | Route | Header top at bottom | Title hit | Overlap at scroll 0 | Header bg = body bg | h-scroll | Screenshot |
|---|---|---|---|---|---|---|---|
| mist/blue light | /dashboard | 0 | h1 | none (first card 141 vs header bottom 117) | oklch(0.963 0.002 197.1) both | no | `screenshot-1788916884501-19.jpg` |
| mist/blue light | /dashboard/issues | n/a — page does not scroll (board owns its scrolling; page root 929 px) | h1 | none | same | no | — |
| mist/blue dark | /dashboard | 0 | h1 | none | oklch(0.275 0.011 216.9) both, border oklch(1 0 0 / 0.1) | no | `screenshot-1788917014017-21.jpg` |
| zinc/emerald light | /dashboard | 0 | h1 | none | oklch(0.967 0.001 286.375) both | no | `screenshot-1788917042174-22.jpg` |
| zinc/emerald dark | /dashboard | 0 | h1 | none | oklch(0.274 0.006 286.033) both | no | `screenshot-1788917076293-23.jpg` |

Header's parent is now the 3073 px page root (`flex min-h-screen flex-col gap-6 bg-muted px-7 py-7`); sticky holds. Subtitle shown at this width (`hidden sm:block`).

**Delta 1 (ruled: addendum 2 @cfa5128, `z-40`):** with the header at `z-50`, the settings popover (portalled to body, wrapper `z-50`, rect y 90–222) overlapped the header box (0–117) and `elementFromPoint` in the popover's top 27 px hit the HEADER at two of three points — the header clipped its own settings menu at equal tier. Re-check after the z-40 delta lands.

**Width note:** the 1280 / 768 px cells could not be measured — `resize_window` reported success but `innerWidth` stayed 1746 (the window is maximised on this display). At 1746 the header wraps with `flex-wrap`, `hscroll` is false, and nothing in the change is width-dependent except the `hidden sm:block` subtitle, which is a source-pinned class. Recorded as not measured, not as passed.

Theme preference restored to mist/blue light.

### @team-lead — 2026-09-08

**Visual leg addendum @2f5fde9 (Delta 1 re-check) — z-40 does NOT fix the popover; cause measured one level deeper.**

Header computed `z-index: 40`, still sticky (top 0 at scroll bottom, title hit). Settings popover open at rect 1417,90 224×132; `elementFromPoint` at three points in its top strip → HEADER, HEADER, HEADER; centre → popover.

Stacking measured: header's only stacking ancestor is itself (`z=40@sticky`, root context). The popover content (`[data-slot="popover-content"]`, carries `z-50`) is `position: static` — z-index ineffective — inside bits-ui's floating wrapper `[data-bits-floating-content-wrapper]`: `position: absolute`, parent `BODY`, inline style `left:0; top:0; transform: translate(…); will-change: transform` and computed **`z-index: auto`** (also auto 600 ms after open). So every popover is at layer 0 in the root context; any positive z on the header paints above it. Header `z-auto` is not an option: with the header temporarily at z-auto, table cells (`th`, `td`) and chart `rect`s paint through the header at scroll 433 and 1083 (3 of 9 sample points each).

bits-ui 2.19 `use-floating-layer.svelte.js:137` sets `zIndex: this.contentZIndex` on the wrapper from the content's computed z-index (L219-221) — not landing here. Fix belongs on the popover primitive's wrapper z, not the header; routed to the architect as a verdict delta (caps spent: 2 addenda, 1 reopen). Lead's decision: fix in this loop — a header that covers its own settings menu is what Mosko would see first.

### @architect — 2026-09-08

**Verdict delta 2 (PT-110) — fix location ruled: the floating wrapper's own layer, in `app.css`. Not the header, not the shadcn content classes.**

Your measurement names a latent, app-wide defect that the header only made visible: `[data-bits-floating-content-wrapper]` computes `z-index: auto`, so **every** bits-ui floating overlay in this app — popover, dropdown, select, tooltip — has been sitting at layer 0 for its whole life. Nothing noticed because no page chrome carried a positive `z-index` until this loop. The header is not the defect; it is the first thing tall enough to stand on the overlays' toes.

**Rule: add one declarative rule to `src/app.css`, keyed on the vendor's own attribute:**
```css
/* PT-110 verdict delta 2: bits-ui 2.19 copies the content's computed
   z-index onto this wrapper inside a requestAnimationFrame
   (use-floating-layer.svelte.js:211-227); measured on the live board,
   it does not land, leaving every floating overlay at layer 0.
   shadcn's own `z-50` is on the content INSIDE the wrapper, which the
   wrapper's stacking position makes inert. */
[data-bits-floating-content-wrapper] { z-index: 50; }
```
No `!important`: if bits-ui's copy does land, its inline style wins with the same value, and this rule is simply the floor. **A style copied inside a RAF is a race by construction — our layering must not depend on winning it**, which is why this lands even if implementation-lead-2's trace finds a prop we should also be passing. If the trace does find a genuine misuse on our side, fix that too; it does not replace the floor.

**Rejected — raising the header back above the overlay tier.** That is addendum 2's mistake in the other direction and would re-clip the settings menu. **Rejected — per-component z props** (`Popover.Content` and friends): repetitive, silently missed by the next new overlay, and it leaves the systemic defect in place for every primitive not yet touched. **Rejected — making each content element positioned** so its `z-50` bites: same per-component churn, subtler mechanism, no better outcome.

**Header stays `z-40`** (addendum 2 stands). With the wrapper at 50 the tier relationship is finally real rather than nominal: content `z-auto` < sidebar rail `z-10` < header `z-40` < every portal'd overlay `z-50`.

**Guards (qa):** (a) scan `app.css` for the rule — the attribute selector and the `z-index: 50` together, with the assertion message naming the tier so a later cleanup has to argue with the reason; (b) the pinning scan's `z-40` from addendum 2, unchanged. **The scan is the floor, not the proof** — no test in either suite renders bits-ui, so the acceptance is your browser leg: `elementFromPoint` in the popover's top strip must return the popover, not the header, on both themes. Say so in the test's own docstring rather than letting a green scan imply more than it checks.

**Scope** — `src/app.css` (one rule), `dist/` rebuilt, the one new scan. Writers: implementation-lead-2 → `app.css` + `dist/`; qa → the scan.

**Worth filing separately, your call, not this loop:** every other bits-ui floating overlay in the app has been at layer 0 since PT-54. Nothing visibly overlaps them today, so there is no user-facing bug to chase — but the next positive `z-index` anyone adds anywhere will reproduce this from a different direction, and the `app.css` rule above is what keeps it from being a mystery when they do.
### @implementation-lead — 2026-09-08

**Trace (implementation-lead-2), no browser — source read of bits-ui 2.19.**

Our `z-50` (popover-content.svelte, dropdown-menu-content.svelte, select-content.svelte) lands on the CONTENT div via `mergeProps` (restProps → PopperLayer → PopperLayerInner → `popper({props, wrapperProps})` → `finalProps` → `<div {...finalProps}>`). The WRAPPER div (`[data-bits-floating-content-wrapper]`, the one with `position:fixed` from floating-ui) gets its z-index from a SEPARATE async path: `use-floating-layer.svelte.js`'s `FloatingContentState` watches `this.contentRef.current`; on mount it schedules one `requestAnimationFrame` that reads `getComputedStyle(contentNode).zIndex` and copies it into `this.contentZIndex` ($state), which feeds `wrapperProps.style.zIndex`.

Traced `contentRef`'s attachment (svelte-toolbelt's `attachRef`/`mergeProps`, symbol keys) through `PopperContent` → `FloatingLayerContent` → `FloatingContentState.props` (`contentAttachment`) → merged into `floatingProps` → merged again in `popper-layer-inner.svelte`'s final `mergeProps(restProps, floatingProps, dismissibleProps, focusScopeProps, {...})`. `mergeProps`'s symbol-key handling is "last one wins" (merge-props.js:103-109), not compose — checked whether `dismissibleProps`/`focusScopeProps` (from `DismissibleLayer`/`FocusScope`, both also threaded the SAME `{ref}` prop) could carry a colliding attachment symbol that overwrites `contentAttachment` before it reaches the DOM. `createAttachmentKey()` (Svelte's own attachment API) mints a fresh `Symbol()` per call, so distinct `attachRef()` calls should NOT collide — this makes the collision theory unlikely, but I can't rule out a timing race between the rAF read and the Portal's DOM move without a browser to instrument.

**Confirmed shared exposure:** `dropdown-menu-content.svelte` and `select-content.svelte` both route through the identical `PopperLayer` → `PopperContent` → `FloatingLayerContent` chain and apply `z-50` the same way (class on the content div, never the wrapper) — this is not popover-specific; whatever the cause, it affects all three portal'd overlays identically.

**Candidate fix (smallest, sidesteps the JS sync entirely):** a global CSS rule targeting the wrapper by its stable attribute, `[data-bits-floating-content-wrapper] { z-index: 50 }`, in `app.css`. Caveat: the wrapper also carries an INLINE `style` (from `wrapperProps.style`, including `zIndex: this.contentZIndex`) — inline styles win over stylesheet rules regardless of specificity/`!important` on the rule side, so if `contentZIndex` ever resolves to a real (non-auto) value later, the inline wins and this rule is moot; if it stays `auto`/unset, the inline declaration may not even set the CSS property at all (an `undefined` value in a Svelte style object is typically omitted), letting a stylesheet rule apply. This needs verifying live before landing — I can't confirm which case holds without a browser.

No fix applied — landed after the architect's verdict delta 2 (above) already ruled the fix location; consistent with it (same rule, same "no !important" reasoning). Confirms the trace found no additional per-component misuse to fix.

### @team-lead — 2026-09-08

**Visual leg addendum @c4e8cae (Delta 1 closed):** live board, mist/blue light, scroll 0, settings popover open at rect 1423,85 213×125 over the header box (bottom 117). Computed: header `z-index 40`, floating wrapper `[data-bits-floating-content-wrapper]` `z-index 50` (the app.css floor now lands). `elementFromPoint` at four points (three across the top strip, one centre) → popover, popover, popover, popover — was HEADER ×3 at both z-50 and z-40 headers. Zoom `screenshot-1788918126369-24.png`: the menu (Mode / Base Color / Theme / Chart Color) renders fully above the header. Header still sticky (verified in the same session). Tier ladder is now real: content z-auto < rail z-10 < header z-40 < overlays z-50. PT-114 filed for verifying the other overlay types and recording the ladder in the design spec.

### @architect — 2026-09-08

**Verdict (gate 4, reopened): go.** Verified against commit objects, with both visual-leg sections and the trace folded in.

| axis | result | evidence |
|---|---|---|
| header is a direct child of the tall page root (the re-issued ruling's mechanism) | pass | `<header class="sticky top-0 z-40 mx-auto flex w-full …">` at App.svelte:353, PT-74 wrapper deleted — 152eb82 |
| PT-74's width rule survives on the header itself | pass | `mx-auto w-full` moved onto the header; padding test re-pointed, assertions unchanged |
| structural guard — no wrapper may return between root and header | pass | red-2 @49619c4, the guard that would have caught the original miss |
| no-spacer test restated positively (addendum 1) | pass | anchored on the token after `</header>`, not the deleted `</div>` |
| header layer `z-40` (addendum 2) | pass | source + live board: computed `z-index 40` |
| floating-wrapper floor in `app.css`, no `!important` (delta 2) | pass | `[data-bits-floating-content-wrapper] { z-index: 50 }` with the cause recorded in place — c4e8cae; parse-level guard @7feb118 |
| the tier ladder is real, not nominal | pass | measured on the live board: content `auto` < rail `10` < header `40` < wrapper `50` |
| AC1 — pinned on scroll, title hit | pass | lead's five-cell table: header top 0 at scroll bottom, `elementFromPoint` → h1, both themes × two variants; `/dashboard/issues` does not scroll (board owns it, AC5) |
| AC2 — no overlap, no spacer, no h-scroll | pass | first card 141 vs header bottom 117 at scroll 0; no horizontal scrollbar in any cell |
| AC3 — opaque surface matches the canvas, light and dark | pass | header bg == body bg in all four cells (`oklch` values recorded per cell) |
| Delta 1 — settings popover no longer clipped | pass | `elementFromPoint` at four points → popover ×4, was HEADER ×3 at both `z-50` and `z-40` headers |
| suite, parallel / serial | pass | 1569 tests OK at 30.0 s (8 workers) and 142.5 s (`--serial`), skipped=1 both |
| dist + type check | pass | `check_dist_freshness.py` rc 0; svelte-check 932 files, 0 errors |

**On the one thing that had to be measured live.** implementation-lead-2's trace ended honestly at "the inline `style` may or may not carry a `zIndex`, and I cannot tell without a browser" — that is the correct place for a source read to stop. The live board answered it: the wrapper computes `z-index 50`, so the inline declaration is absent when `contentZIndex` never resolves, and a stylesheet rule does apply. The floor holds for the reason delta 2 gave, and the trace's other finding — dropdown and select route through the identical chain — is why it belongs in `app.css` rather than on one component.

**What this loop cost, and what it bought.** Three of my calls were wrong in sequence: the containing block (missed), the z-tier tie-break (backwards), and then z-40 alone (insufficient, because the overlay tier was nominal). Each was overturned by a browser measurement, and each correction is in the file above its fix. The AC1 `elementFromPoint` leg was not ceremony on this issue — it was the only thing standing between "the classes are right" and "the header is pinned", which is the same lesson PT-32/PT-57/PT-64/PT-76/PT-88 keep paying for. The structural guard at 49619c4 is the durable part: pinning classes are necessary and never sufficient.

**No residuals.** PT-114 carries the ladder's remaining verification and its spec entry, as the delta suggested.

### @team-lead — 2026-09-08

PR opened: https://github.com/richmosko/project_template/pull/202. Awaiting Validate.
