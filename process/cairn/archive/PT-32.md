---
id: PT-32
title: Board: pull-down-to-refresh gesture
status: done
milestone: PT-0.6.1
parent: null
blocked_by: []
assignee: null
labels: [board, ux]
priority: P3
pr: https://github.com/richmosko/project_template/pull/75
created: 2026-08-22
updated: 2026-08-22
---


## Problem

Feature request (Mosko, 2026-08-22): pull-down-to-refresh on the board — the mobile-style gesture (drag down from the top of the scroll container, release to reload board data). Today freshness comes from SSE live push with poll fallback (PT-1); pull-to-refresh adds an explicit manual gesture for when the viewer doubts freshness (SSE dropped, polling window, laptop resume) or is on a touch device where ⌘R is awkward.

## Acceptance criteria

- [ ] Pull-down past a threshold at scroll-top triggers a board data refetch (same path the poll fallback uses — no full page reload), with a visible pull indicator and release-to-refresh affordance
- [ ] Works with touch (trackpad overscroll deferred to PT-33 — Mosko ruling 2026-08-22, architect-recommended: wheel has no release signal, momentum heuristics fragile); never hijacks normal scrolling mid-list (gesture arms only at scroll-top)
- [ ] No interference with drag-and-drop card moves (gesture must not arm while a card drag is active)
- [ ] Degrades to nothing on browsers without the needed events — no errors, existing SSE/poll behavior untouched
- [ ] Pure logic (threshold/state machine) in board-logic.js, red-first via node:test

## Notes

- Unmilestoned at filing; one word from Mosko pulls it into 0.6.1.

## Comments

### @team-lead — 2026-08-22

Moved into 0.6.1 (Mosko, 2026-08-22). Runs sequentially after PT-30; pre-approved through /merge-pr on QA sign-off.

### @team-lead — 2026-08-22

Feature started. Branch: `feature/pt-32-pull-to-refresh`. Standing order applies: pre-approved through /merge-pr on QA sign-off.

### @architect — 2026-08-22

Design ruling — read before writing code. **One acceptance criterion needs
renegotiating, not just implementing** (§ 1) — that's the first thing to read.
Departures from the issue's literal text are labelled **Judgment call** with
cost and a one-line reversal.

**Ground truth established first, since the gesture depends on it:** the
board scrolls the **document**, not a nested container — nothing in board.css
sets `overflow-y`, and `header.app-header` is `position: sticky; top: 0`. So
"at scroll top" is `window.scrollY === 0`. Two other scroll surfaces exist and
both matter below: `.board` is `overflow-x: auto` (horizontal, inside a lane)
and `.drawer` is `position: fixed; overflow-y: auto` (its own scroller,
overlaying the page).

---

### 1. Event strategy — touch only; the trackpad half of AC2 should be dropped

Four mechanisms get discussed for this gesture. They are not four options:

- **Touch events** (`touchstart`/`touchmove`/`touchend`) — explicit finger
  position, so pull distance is a subtraction; and, critically, a **release
  signal**. Doesn't fire for trackpads.
- **Pointer events** — buys nothing here. Trackpad two-finger scroll does
  **not** emit pointer events (it emits `wheel`), so pointer events don't
  deliver the trackpad case they're usually proposed for. They *would* add
  mouse-drag on the page as a false-positive surface, where a click-drag is
  text selection, not a pull.
- **`wheel` events** — the *only* way to see trackpad overscroll, and the
  reason to say no (below).
- **`overscroll-behavior`** — **not a gesture mechanism at all.** It's a CSS
  property controlling whether overscroll chains or bounces; it exposes no
  event and no hook. The issue lists it alongside the other two as though it
  were an alternative. It isn't — it's a *companion*, and a mandatory one
  (§ 3).

**Ruling: implement the gesture on touch events only. Do not implement
trackpad overscroll.**

> **Judgment call — and this one changes scope, so it needs a decision from
> team-lead/Mosko rather than my say-so.** AC2 currently reads "Works with
> touch and trackpad overscroll". I'm ruling the trackpad half out:
>
> 1. `wheel` has **no release signal**. The whole gesture grammar — pull,
>    hold, decide, release — has no analogue. A wheel implementation must
>    accumulate `deltaY` at scroll-top and fire on a *timer*, guessing when
>    the user stopped.
> 2. **Momentum makes that guess wrong in the common case.** Flick up hard
>    through a long board, hit the top, and the OS keeps delivering negative
>    `deltaY` for hundreds of milliseconds. That is indistinguishable from a
>    deliberate pull without velocity-decay heuristics that are
>    platform-specific and fragile. The failure mode is a board that refetches
>    when you didn't ask — cheap in consequence, but it makes the UI feel
>    haunted, and "why did it just do that" is a worse bug than "it didn't do
>    anything".
> 3. **macOS already draws a native rubber-band bounce** at scroll-top on
>    trackpad overscroll. Layering a second indicator with different physics
>    on top of the OS one reads as breakage.
> 4. **The issue's own Problem statement names touch as the motivating case**
>    ("on a touch device where ⌘R is awkward"). On a trackpad, ⌘R is right
>    there, and SSE + poll already cover freshness.
>
> Cost: trackpad users don't get the gesture, and AC2 must be **amended
> rather than quietly failed** — I'd rather leave the criterion unmet and
> visible than reword it myself. Reversal is deliberately cheap: § 5's state
> machine is **input-agnostic** — it consumes a pull distance, not a touch —
> so adding trackpad later is a new *adapter* feeding the same machine, not a
> redesign.

---

### 2. Arming state machine

Phases: `idle → pulling → armed → refreshing → idle`.

| Transition | Trigger |
|---|---|
| `idle → pulling` | `touchstart` while `window.scrollY === 0` and no cancel condition, then a `touchmove` with net downward delta > 0 |
| `pulling → armed` | pull distance ≥ `PULL_THRESHOLD` |
| `armed → pulling` | pull distance falls back below the threshold |
| `armed → refreshing` | `touchend` |
| `pulling → idle` | `touchend` below threshold — snap back, no refetch |
| `refreshing → idle` | the refetch settles (§ 4) |

**`armed` must not latch.** Dragging past the threshold and then back up
returns to `pulling`, and releasing there does nothing. Users routinely change
their mind mid-gesture; a latched arm makes the gesture feel like a trap.

**Threshold: 64px** of finger travel. iOS native sits around 60–80px; below
~50px a sloppy scroll attempt at the top triggers it.

**Resistance.** The indicator moves at **half** finger speed, capped at 96px —
the rubber-band feel that tells the user the surface is resisting. This is
presentational and does not affect the phase decision, but it's computed by a
pure function so it's testable.

**Cancel conditions.** Any one of these means the gesture never arms and no
refetch happens:

1. **Not at scroll top on `touchstart`.** This is AC2's "never hijacks normal
   scrolling mid-list", and it's checked at gesture *start*, not continuously.
2. **A card drag is active.** HTML5 DnD is a mouse/trackpad affair and largely
   doesn't fire on touch, so this is mostly a same-device belt-and-braces —
   but it's AC3 and it's two lines. Add `state.cardDragActive`, set in the
   existing `dragstart` handler and cleared in `dragend`. **Do not** infer it
   by querying the DOM for `.dragging`; a state flag set at the same two call
   sites that already toggle that class is one fact, not two.
3. **The drawer is open.** This one is easy to miss and would be a real bug:
   `.drawer` is `position: fixed`, so scrolling *it* never changes
   `window.scrollY`. With the board at top and the drawer open, a pull inside
   the drawer sees `scrollY === 0`, bubbles to the document, and arms the
   gesture. Gate on `state.openIssueId !== null` — one flag already in state,
   no DOM containment test.
4. **Multi-touch** — `e.touches.length > 1`. Pinch-zoom is not a pull.
5. **Horizontal dominance** — `|dx| > |dy|` on the first significant move.
   `.board` is `overflow-x: auto`, so a horizontal swipe across a lane's
   columns is a real gesture that must not be stolen.
6. **Already refreshing.** Ignore new gestures until the in-flight refetch
   settles, so repeated pulls can't stack requests.

---

### 3. `preventDefault`, and the Android trap

The `touchmove` listener must be registered **non-passive**
(`{ passive: false }`) and call `preventDefault()` **only while in `pulling`
or `armed`**. Calling it unconditionally breaks normal scrolling; not being
able to call it at all means the native bounce fights the indicator.

**Add `overscroll-behavior-y: contain` to `body`.** This is not polish:
**Chrome on Android ships its own native pull-to-refresh that reloads the
whole page** — precisely what AC1 forbids ("no full page reload"). Without
`overscroll-behavior-y: contain`, a pull on Android Chrome can trigger both
the browser's gesture and ours. This single CSS line is the difference
between the feature working and the feature reloading the page out from under
itself, and it is the reason `overscroll-behavior` belongs in this design even
though it is not a gesture mechanism.

---

### 4. Indicator UX and the refetch path

**Indicator:** one `#pull-indicator` element, `position: fixed; top: 0`,
`z-index: 9` — one below the sticky header's 10, so it emerges from *under*
the header rather than covering it. `transform: translateY(<offset>)`.
Hidden entirely in `idle`.

| Phase | Copy |
|---|---|
| `pulling` | `↓ Pull to refresh` |
| `armed` | `↑ Release to refresh` |
| `refreshing` | `↻ Refreshing…` |

Copy lives in one pure function (§ 5) so there's a single home for it. Do
**not** overload the existing `.connection-state` pill — that reports the SSE
connection, a standing fact; this reports a transient gesture.

**Refetch path: reuse `refreshBoardSilently()`.** It already does the
ETag-conditional GET, applies data only when the server sends some, ignores
304, swallows errors, and re-renders. AC1's "same path the poll fallback
uses" is satisfied with zero new network code.

**But it currently resolves to `undefined`, and that's a problem for this
feature.** Because the GET is ETag-conditional, a pull when nothing has
changed produces a 304, no re-render, and therefore **no visible response at
all** — the user performs a deliberate gesture and the UI does nothing. That's
the dead-end case, and it's the *common* case in live mode (§ 6).

Ruling: **make `refreshBoardSilently()` resolve to a boolean** — `true` when
new data was applied, `false` on 304 or error. Every existing caller ignores
the return value, so this changes no existing behaviour. The pull path then
toasts through the existing `showToast`: "Board updated" or "Already up to
date". Do not give the pull path its own `apiGetBoard()` call to distinguish
the cases — that duplicates the refresh logic, which is how the two copies
start to drift.

**Minimum `refreshing` duration: 400ms.** A refetch against localhost can
resolve in 20ms; the spinner would flash and read as a glitch rather than as
confirmation.

> **Judgment call.** I'm ordinarily against deliberate delay. Here the delay
> *is* the feedback — the user asked a question and needs to see it being
> answered. Cost: every pull takes at least 400ms even when the answer is
> instant. Reversal: drop the `Promise.all` floor.

---

### 5. What lands where

**board-logic.js** — pure, tested, and **deliberately input-agnostic** so § 1's
trackpad reversal stays cheap: the machine consumes a *pull distance*, never a
touch event.

| Export | Contract |
|---|---|
| `PULL_THRESHOLD` = 64, `PULL_MAX_OFFSET` = 96, `PULL_RESISTANCE` = 0.5, `PULL_MIN_SPINNER_MS` = 400 | Constants, exported so tests assert the values the code uses |
| `pullPhase({atScrollTop, deltaY, cancelled})` → `"idle"\|"pulling"\|"armed"` | The phase decision |
| `pullIndicatorOffset(deltaY)` → number | `min(deltaY * PULL_RESISTANCE, PULL_MAX_OFFSET)`, floored at 0 |
| `pullIndicatorLabel(phase)` → string | The single home for the three strings |
| `shouldCancelPull({cardDragActive, drawerOpen, multiTouch, horizontalDominant, refreshing})` → bool | Any true ⇒ cancel |

**`pullPhase` is deliberately PARTIAL — it does not return `refreshing`.** That
phase is entered and left by promise resolution, which is the async caller's
business, not a pure function's. A reader will be tempted to "complete" the
machine; the reason not to is that doing so would require passing async state
into a synchronous decision. Say so in the comment.

**board.js** — listener wiring, the `passive: false` registration, the
`preventDefault` decision, `state.cardDragActive`, `state.pullRefreshing`, and
the indicator DOM. Bail early if `#pull-indicator` is absent.

**No feature detection.** Attach the touch listeners unconditionally; on a
device that never produces touches they never fire, which is exactly AC4's
"degrades to nothing". A `"ontouchstart" in window` branch is one more thing
that can be wrong for zero benefit.

---

### 6. SSE-live interaction — the gesture behaves identically in both modes

The question was whether a manual refresh is even meaningful when SSE is live.
**Yes, and it must not branch on mode.**

- **"Live" means the EventSource is open, not that every mutation arrived.** A
  stream can stall behind a proxy while the connection object still reports
  open; a suspend/resume can drop messages before `onerror` fires; a server
  restart leaves a window where the client still believes it's live. The pill
  says "live" in all of those.
- **The gesture exists for subjective doubt** — the issue says so directly. A
  user who doubts freshness and performs an explicit gesture should not be
  told by a status pill that their doubt is invalid.
- **Branching would be user-visible and worse.** "Sometimes the gesture does
  nothing, depending on a pill I wasn't looking at" is a worse experience than
  one extra conditional GET.

So: no mode gating, no different indicator, no different copy. The one real
interaction is a *good* one — in live mode a pull will almost always 304, which
is exactly why § 4's "Already up to date" toast earns its place. It converts
a silent no-op into the confirmation the user was asking for.

> **Judgment call.** I could have made the gesture a no-op while live to save
> a request. Cost of not doing so: one conditional GET per gesture — a 304 with
> an ETag, which is about as cheap as an HTTP round trip gets. Reversal: gate
> on the connection state. Don't.

---

### 7. Mode cross-product — every combination, including the ones that don't matter

Recording this explicitly because my last two rulings each shipped a gap by
covering only the modes the issue text named:

| Mode | Behaviour |
|---|---|
| Kanban (`/`) | Gesture active |
| List (`/list`) | **Gesture active — same behaviour, no branching.** Both views scroll the document and both are served the same `board.html`; the refetch path is view-agnostic and `render()` already dispatches correctly |
| Swimlanes on / off | No effect — the gesture is about the document scroller, not lane structure |
| Single-root / multi-root | No effect |
| Drawer open | **Cancelled** (§ 2 condition 3) |
| Card drag active | **Cancelled** (§ 2 condition 2) |
| SSE live / polling | Identical behaviour (§ 6) |
| Touch device | Gesture active |
| Trackpad / mouse | **Nothing.** No listeners fire; existing scroll and SSE/poll behaviour bit-for-bit unchanged (§ 1) |

---

### Red-first test surface (QA's lane)

1. `pullPhase`: `idle` when `atScrollTop` is false, for any `deltaY`
2. `pullPhase`: `idle` when `cancelled` is true, even at scroll top past threshold
3. `pullPhase`: `idle` for `deltaY <= 0` (an upward or zero move)
4. `pullPhase`: `pulling` for `0 < deltaY < PULL_THRESHOLD`
5. `pullPhase`: `armed` at exactly `PULL_THRESHOLD` (boundary — at, not past)
6. `pullPhase`: **`armed → pulling` when deltaY drops back below threshold** — the
   no-latch rule; this is the one a naive implementation gets wrong
7. `pullIndicatorOffset`: applies resistance, caps at `PULL_MAX_OFFSET`, floors
   at 0 for negative input
8. `pullIndicatorLabel`: a distinct non-empty string for each of the three
   phases, and something safe for an unknown phase
9. `shouldCancelPull`: true for each of the five flags **independently** (five
   cases, not one combined) and false when all are absent
10. `pullPhase` never returns `refreshing` for any input — the partiality is
    the contract, so it gets a test

### @team-lead — 2026-08-22

Mosko ruling (2026-08-22): AC2 scoped to touch-only per architect recommendation; trackpad overscroll deferred to PT-33 (backlog) as a wheel adapter on the same distance-based state machine.

### @architect — 2026-08-22

Architect diff review — reviewed `git diff main..295785c`, one frozen sha,
against the ruling @ 382f09d and the touch-only scope @ 7f5086a. Suite
independently re-run at that sha: 155 pass / 0 fail.

**One BLOCKING finding (finding 1) — and it is a defect in my ruling's
numbers, not a deviation from them.** Everything else is clean, and two
things are better than specified.

### What's correct

- **The state machine is faithful, and the implementation found a sharper
  reason for it than I gave.** My ruling said `pullPhase` must not latch; the
  code's comment observes that it's **memoryless** — a pure function of its
  current inputs with no "was this armed a moment ago" parameter — so no-latch
  isn't a rule the function has to remember to follow, it's a property of
  calling it twice with a shrinking `deltaY`. That's a better articulation of
  the invariant than mine.
- **Partiality honoured.** `pullPhase` never returns `"refreshing"`; the DOM
  layer owns that transition via `state.pullRefreshing`, and there's a test
  pinning the partiality as a contract.
- **All five cancel flags wired independently**, computed in board.js and
  handed to `shouldCancelPull` as a union — no subset re-derived at the
  predicate. The drawer trap is gated on `state.openIssueId !== null` as
  ruled. Multi-touch gets an extra guard I didn't ask for: a second finger
  landing *mid-gesture* cancels immediately at `touchstart` rather than
  waiting for the next `touchmove` to notice.
- **Axis decision is made once** past a 4px noise floor and held for the rest
  of the gesture, so a swipe that starts vertical and drifts horizontal
  doesn't flip allegiance mid-gesture. I specified "on the first significant
  move"; the noise floor is the right reading of it.
- **Listener strategy exactly as ruled:** `touchmove` alone is
  `{ passive: false }` — the only one that ever calls `preventDefault` — and
  that call is scoped to `pulling`/`armed`. No feature detection.
- **`overscroll-behavior-y: contain` is on `body`**, with the comment
  recording *why* it's load-bearing so a future tidy-up doesn't strip it.
- **`refreshBoardSilently`'s boolean has nil blast radius.** All six existing
  call sites are bare statement calls (including the `setInterval` and the
  `focus` listener, which discard it structurally). Nothing consumes the
  value but the pull path.

---

### BLOCKING — finding 1: the indicator is cropped or entirely invisible, worst on the devices this feature targets

`.pull-indicator` is `position: fixed; top: 0; z-index: 9`; the header is
`position: sticky; top: 0; z-index: 10` with an opaque `--panel` background,
so the header paints over the indicator. The indicator is revealed only by
`translateY(offset)` pushing it out from underneath.

The arithmetic doesn't work. Let `H_hdr` be the header's height and `H_ind`
the indicator's (padding 10 + 6 plus a ~17px line ⇒ **~33px**). Any part of
the indicator is visible only when

```
offset + H_ind > H_hdr      i.e.   offset > H_hdr − 33
```

and `offset` is capped at `PULL_MAX_OFFSET = 96`. So **nothing ever shows
once the header exceeds ~129px**, and only a cropped sliver shows below that.

The header is a column of three rows with 8px gaps and 20px of vertical
padding: `.app-header-row` (~30px), `.milestone-progress` (~18px), and
`.filters` (~28px at one line). That is ~112px with the filter bar on a
**single** line — already only a 17px sliver of a 33px element. `.filters` is
`flex-wrap: wrap` and now carries a text input, five selects, two checkboxes
and PT-30's two buttons, so it wraps to two lines on a narrow viewport,
putting `H_hdr` around ~148px — past the cutoff, indicator never visible.

**The failure correlates with the target platform.** This gesture is
touch-only by ruling; touch means a narrow viewport; a narrow viewport is
exactly when the filter bar wraps and the header is tallest. The indicator is
least visible precisely on the devices the feature exists for. AC1 requires
"a visible pull indicator and release-to-refresh affordance", so I'm calling
this blocking.

**This is my ruling's error.** § 4 specified `z-index: 9` and "emerges from
under the header" alongside a 96px offset cap, and I never checked the two
were compatible with a header whose height is dynamic. Third ruling gap of
this family (PT-29's surface table, PT-30's list view).

**Fix — make visibility independent of header height** rather than tuning the
cap against a moving target:

```css
.pull-indicator { top: 0; z-index: 11; transform: translateY(-100%); }
```

and have `updatePullIndicator` set
`translateY(calc(-100% + " + offset + "px))"`. At rest the element sits fully
above the viewport; pulling reveals exactly `offset` pixels of it, overlaying
the header — which is the conventional iOS/Android look, not a compromise.
It's fully revealed once `offset ≥ H_ind ≈ 33`, i.e. `deltaY ≈ 66` — which
lands within a couple of pixels of the 64px arm threshold, so "fully visible"
and "armed" coincide for free.

Raising `PULL_MAX_OFFSET` instead is the wrong fix: it re-couples the gesture
to a header height that changes with viewport width, majors-tab count, and
milestone-progress content.

*Chrome pass can overturn this with a measurement* — the analysis is
arithmetic plus estimated row heights, not a pixel reading. But the
height-independent fix is worth taking on robustness grounds even if a sliver
happens to be visible at today's default window size, because "visible" would
remain conditional on the header not growing.

---

### RECORDED — non-blocking

**2. The indicator lags the finger.** `.pull-indicator` carries
`transition: transform 0.15s ease` unconditionally, so during an active drag
the element eases toward each new offset instead of tracking the touch. A
pull indicator should follow the finger 1:1 (through the resistance curve)
and animate only on release/snap-back. Suppress the transition while
`pulling`/`armed` — e.g. a class toggled by `updatePullIndicator` — and keep
it for the return to idle. Feel issue, visible in the Chrome pass.

**3. `resetPullGesture` resets four of the six gesture variables.**
`pullAtScrollTop`, `pullLastDeltaY` and `pullLastPhase` are left standing;
they're re-initialised at `touchstart` instead, and every read is currently
guarded by the `pullStartY === null` early return, so nothing is wrong today.
But a function named "reset" that leaves a third of the state behind is a
trap for whoever next adds a path that reads those after a reset. Either move
the remaining three in, or rename it to say what it actually does.

**4. `updatePullIndicator` does a `getElementById` per `touchmove`.**
Up to ~60/sec during a pull. Cheap, and `wirePullToRefresh` already proves the
element exists, so it could be hoisted to a closure var. Noting, not pressing.

**5. `↻ Refreshing…` is a static glyph.** It holds for at least
`PULL_MIN_SPINNER_MS` (400ms) without motion, which reads as frozen rather
than working. A CSS rotation on the glyph during the refreshing phase would
cost a keyframe. Cosmetic; the Chrome pass is the right place to judge whether
it matters at 400ms.

**6. For the Chrome pass**, in priority order: (a) confirm or overturn finding
1 by pulling at a **narrow** viewport where the filter bar wraps — that's the
worst case, not the default window; (b) check whether the indicator visibly
trails the finger (finding 2); (c) confirm the gesture does not fire while the
drawer is open with the board at scroll-top, which is the non-obvious cancel
condition; (d) confirm a horizontal swipe across a lane's columns still scrolls
them rather than being stolen.

### @architect — 2026-08-22

Micro-ruling — three-state refresh outcome (requested by team-lead after the
Chrome pass drew a real 503 on the pull path).

**The bug is in my § 4 ruling, not the implementation.** I specified a boolean
for a question with three answers, and the two-way collapse put *failed* on
the same branch as *unchanged* — so the one outcome the user most needs to
know about renders as the most reassuring message the board can show, in a
gesture whose entire reason for existing is freshness doubt. Fix it inside
PT-32; this shouldn't ship as recorded polish.

### 1. Shape: a three-state string outcome, never a rejection

`refreshBoardSilently()` resolves to one of three values and **still never
rejects**.

Rejected alternatives:

- **Boolean + throw on failure.** Decisive objection: the function has six
  existing bare-statement call sites, including
  `setInterval(refreshBoardSilently, 4000)` and
  `window.addEventListener("focus", refreshBoardSilently)`. Both discard the
  returned promise *structurally* — there is nowhere to attach a `.catch`.
  Making it reject produces an unhandled promise rejection on every network
  blip at every one of those sites. The boolean's whole virtue was that
  existing callers can ignore the value; **a rejection is precisely the thing
  a caller cannot ignore.** Restoring safety would mean adding `.catch()` to
  all six — the blast radius the design was chosen to avoid.
- **An object, `{ok, changed}`.** Two independent booleans can express
  `{ok: false, changed: true}`, which means nothing. A three-state value makes
  the invalid combination unrepresentable — the same principle that made
  PT-30's serialized array unable to encode "closed".

### 2. The three values live in board-logic.js as exported constants

```
REFRESH_UPDATED   = "updated"
REFRESH_UNCHANGED = "unchanged"
REFRESH_FAILED    = "failed"
```

board.js returns those constants; board-logic.js's toast mapper switches on
them. Without this there are two lists of string literals — a producer in
board.js and a consumer in board-logic.js — that must agree and that no test
can compare, because `refreshBoardSilently` isn't reachable from the node
suite. Exporting the constants collapses them to one copy, consistent with
how `BOARD_COLUMNS`, `laneStateKey` and `isDraggable` were each settled.

Mapping in `refreshBoardSilently`: data applied ⇒ `REFRESH_UPDATED`; `null`
(a 304) ⇒ `REFRESH_UNCHANGED`; the existing `.catch` ⇒ `REFRESH_FAILED`
instead of `false`. The `.catch` also swallows a throw from inside `render()`,
which would report `failed` after data was in fact applied — an acceptable
inaccuracy, since a throwing render is a bug elsewhere and "failed" is the
honest thing to say about a render that didn't finish.

### 3. Toast mapping is a pure function, and its default is the UNsafe branch

`pullRefreshToast(outcome)` → `{message, isError}`, in board-logic.js:

| Outcome | Message | `isError` |
|---|---|---|
| `REFRESH_UPDATED` | `Board updated` | false |
| `REFRESH_UNCHANGED` | `Already up to date` | false |
| `REFRESH_FAILED` | `Refresh failed — showing last known data` | **true** |
| anything else | same as `REFRESH_FAILED` | **true** |

Two rulings inside that table:

**The failure copy has two clauses on purpose.** "Refresh failed" alone
answers the wrong question — the user's actual question is *"is what I'm
looking at current?"*, and after a failed refresh they need to know the board
is still showing pre-gesture data rather than something empty, partial, or
half-applied.

**An unrecognized outcome maps to the failed shape, not to an empty or
neutral one.** Defaulting an unknown state to a reassuring message is exactly
the defect being fixed here, one level up. Note this is the opposite default
from `pullIndicatorLabel`, which returns `""` for an unknown phase — and the
difference is principled: an unknown *phase* means "show nothing", while an
unknown *outcome* means "something went wrong and I can't characterise it".

`showToast`'s existing second parameter already applies `.error` styling; no
new toast machinery.

The `PULL_MIN_SPINNER_MS` floor applies to all three outcomes with no
branching — a failure shouldn't resolve faster than a success, or the failure
reads as "the gesture didn't register" rather than "the refresh was tried and
failed".

### 4. Prerequisite: `apiGetBoard` must check `resp.ok`

Without this, the third state is not trustworthy. `apiGetBoard` currently
does:

```js
if (resp.status === 304) return null;
var etag = resp.headers.get("ETag");
return resp.json().then(...)
```

There is **no `resp.ok` check**. The observed 503 only reached the `.catch` by
luck — its body wasn't valid JSON, so `resp.json()` rejected. Had the server
returned a JSON error body with a 5xx, `resp.json()` would have resolved and
**that error object would have been assigned to `state.board` and rendered as
if it were the board.** Add, before the parse:

```js
if (!resp.ok) throw new Error("board fetch failed: " + resp.status);
```

> **Judgment call.** This edits a function PT-32 otherwise doesn't touch, and
> it changes behaviour for the poll/SSE/focus paths too — a non-ok response
> that used to be parsed and applied is now caught and discarded, leaving the
> last-known-good board on screen. Cost: one more edited function in this
> diff, and a class of malformed response that used to render as garbage now
> renders as nothing (correctly). Reversal: drop the two lines; the three-state
> outcome still works for every non-JSON error body, just not for a
> well-formed JSON one.

### 5. Tests to add (red-first)

1. `REFRESH_UPDATED`/`REFRESH_UNCHANGED`/`REFRESH_FAILED` export the exact
   string values — a rename must break a test, since board.js's producer side
   can't be reached from the suite
2. `pullRefreshToast` returns the right `{message, isError}` for each of the
   three constants — three separate cases
3. `pullRefreshToast(REFRESH_FAILED).isError === true`, asserted on its own:
   this is the property the original defect violated
4. `pullRefreshToast` on an unknown/undefined/null outcome returns the failed
   shape with `isError === true` — not empty, not neutral
5. the three messages are pairwise distinct and non-empty

---

**Separately, for team-lead — PT-31's own trigger has fired.** PT-31 item 4
records the transient `GET /api/board` 503s from PT-29's Chrome pass with the
condition *"Investigate only if seen again."* This is the second sighting, so
that gate is met. The client-side three-state outcome above is symptom
treatment — correct and worth having regardless, since the client must handle
a failed refetch whatever the cause — but the suspected fetch/SSE race on the
single-threaded server is a real server-side defect and should now be
investigated on its own terms rather than left behind a condition that has
already been satisfied.

### @team-lead — 2026-08-22

Validate passed (QA combined sign-off GREEN @ 4ade3bb; architect lane closed @ 295785c + 4ade3bb). Merging via PR #75 per standing order. Closing.

### @team-lead — 2026-08-22

Placed from temp/ at PT-0.6.1 close (team-lead): QA's browser-verification checklist, kept as the regression script for future changes to this surface.

---
kind: finding
topic: PT-32 browser-verification checklist
---

# PT-32 browser-verification checklist (team-lead's Chrome pass)

Acted against sha `bc1544d` (red tests) on `feature/pt-32-pull-to-refresh`, on top of the
architect's ruling at `382f09d` and Mosko's touch-only scope decision at `7f5086a`. Run this
AFTER implementation-lead's task #14 lands and the JS suite
(`node --test scripts/cairn/tests/js/*.test.js`) is green.

**Tooling note up front, since real touch gestures may not be reliably simulable in your
automation** (same lesson as PT-29's drag-gesture flake): every section below has a
**synthetic-dispatch fallback** — use `javascript_tool` to construct and dispatch real
`TouchEvent`s (or, if Chrome's automation context won't construct `Touch`/`TouchEvent` objects
directly, dispatch plain `Event`s of type `touchstart`/`touchmove`/`touchend` with a hand-built
`touches`/`changedTouches` array monkey-patched onto the event object before dispatch — the
app's own listeners only read `e.touches[0].clientY` / `e.touches.length`, so they don't need a
spec-perfect `TouchEvent`, just an object shaped like one). If a real physical touch/trackpad
device is available to you, prefer that for at least the top-level pass/fail call; use synthetic
dispatch to fill in what the device can't isolate (e.g. deliberately stopping mid-gesture below
threshold). Note in your report which path each section used.

## Setup

1. Load the board fresh. Open DevTools → Console (watch for errors throughout) and Network
   (watch for the refetch request specifically during gesture sections).
2. If Chrome DevTools' device toolbar / touch emulation is available in your automation
   context, turn it on for this pass — it makes `touchstart`/`touchmove`/`touchend` fire from
   real mouse-drag input without needing raw synthetic events for the main flow.

## 1. Pull gesture: indicator states + threshold behavior

1. At the very top of the page (`window.scrollY === 0`), start a downward drag/pull and watch
   `#pull-indicator`.
2. Assert: below `PULL_THRESHOLD` (64px), the indicator is visible, reads "↓ Pull to refresh",
   and its vertical offset visibly lags the finger (resistance) — moving noticeably slower than
   1:1 with drag distance.
3. Continue past 64px. Assert: the label flips to "↑ Release to refresh" the moment the
   threshold is crossed — not before, not with a delay.
4. Assert: the indicator's offset stops growing once it visibly maxes out well before your
   drag distance would suggest 1:1 tracking (the 96px cap) — confirm it doesn't keep growing
   unbounded for a very long drag.
5. **No-latch check:** past the threshold (label reads "Release to refresh"), ease back upward
   without releasing. Assert: the label reverts to "↓ Pull to refresh" — releasing at this point
   must NOT trigger a refresh. This is the case a naive implementation gets wrong; it's the one
   thing worth a deliberate slow, careful drag-back rather than a fast gesture.
6. From below the threshold, release. Assert: the indicator snaps away, no refetch fires (watch
   Network — no request), no toast appears.
7. From past the threshold, release. Assert: the indicator shows "↻ Refreshing…", a refetch
   request fires in the Network tab (same endpoint the poll fallback already uses — confirm it's
   not a full page navigation/reload), and a toast appears reading either "Board updated" or
   "Already up to date" depending on whether the data actually changed.
8. Time the "Refreshing…" state on a fast localhost response. Assert: it holds for a
   perceptible minimum (~400ms) rather than flashing instantly even when the server responds in
   under 20ms.

## 2. Never hijacks normal scrolling mid-list

1. Scroll partway down the board (NOT at the top). Attempt a downward drag gesture there.
   Assert: it behaves as an ordinary scroll — no indicator appears, no resistance, nothing
   pull-related happens, regardless of how the drag starts.
2. Scroll back to the top, then start a pull. Mid-gesture (indicator visible, in `pulling` or
   `armed`), assert normal page scroll is suppressed while the gesture is active (no
   simultaneous native scroll fighting the indicator) but resumes normally once you release.

## 3. No interference with drag-and-drop (AC3)

1. At scroll-top, start dragging an issue card (HTML5 DnD — mousedown-and-drag on a `.card`).
   Assert: the pull indicator does NOT appear, even though the drag technically starts at
   `scrollY === 0`.
2. Drop the card normally. Assert: the drop completes as before (no PT-29/PT-30 regression),
   and immediately afterward a normal pull gesture works again (the cancel condition clears on
   `dragend`, not stuck permanently).

## 4. Drawer-open cancellation (the fixed-position trap)

1. Open the drawer (click any issue card). With the drawer open and the BOARD (not the drawer)
   scrolled to top, attempt a pull gesture starting from within the drawer's content area (which
   has its own internal scroll).
2. Assert: no pull indicator appears and no refetch fires — the gesture must be cancelled while
   the drawer is open, even though `window.scrollY` may read 0 underneath the fixed-position
   drawer.
3. Close the drawer, confirm a normal pull gesture works again immediately after.

## 5. Multi-touch and horizontal-swipe non-interference

1. (Synthetic-dispatch likely required.) Simulate a two-finger touch start at scroll-top with a
   downward move. Assert: no pull indicator appears — pinch/multi-touch must not arm the
   gesture.
2. Inside an expanded lane's horizontally-scrolling `.board` area, perform a horizontal swipe
   (dominant `dx` over `dy`) starting near scroll-top. Assert: the lane's own horizontal scroll
   happens normally and the pull indicator does NOT appear — a lane swipe must not be stolen by
   the vertical-pull gesture.

## 6. Already-refreshing guard

1. Trigger a pull-to-refresh (release past threshold, "Refreshing…" showing). While that's still
   in flight (within the ~400ms floor, or throttle the network in DevTools to widen the window),
   attempt a second pull gesture immediately.
2. Assert: the second gesture is ignored — no second overlapping request fires, no visual
   double-indicator glitch.

## 7. Android native pull-to-refresh trap (`overscroll-behavior-y: contain`)

1. If a real Android Chrome device/emulator is available, perform the gesture there specifically
   and confirm ONLY the app's own indicator/refetch happens — no full-page browser-native
   reload/flash accompanies it.
2. If not available, at minimum confirm in DevTools (any platform) that `body`'s computed style
   includes `overscroll-behavior-y: contain` — this is the one-line CSS the ruling calls
   load-bearing for Android specifically; note in your report if you couldn't test the live
   Android behavior itself, since that's the only way to see the actual trap this line prevents.

## 8. Degradation (no touch support / desktop mouse+keyboard)

1. On this desktop Chrome session (no touch emulation active), confirm: no console errors on
   load, existing SSE/poll refresh behavior is completely unaffected (watch for the normal 4s
   poll / SSE push still working), and nothing about normal mouse-based scrolling or interaction
   changed from pre-PT-32 behavior.
2. Confirm `#pull-indicator` exists in the DOM but stays hidden/inert the entire session absent
   any touch input.

## 9. List view + SSE-live parity (the ruling's "no branching" requirements)

1. Switch to `/list`. Repeat a basic pull-past-threshold-and-release cycle. Assert: identical
   behavior to Kanban — same indicator states, same refetch, same toast. This is a deliberate
   ruling ("gesture also active in List view by design"), not an oversight if it works — flag if
   it does NOT work in list view, since that WOULD be a gap.
2. With the SSE connection live (pill shows connected) and no actual server-side changes since
   last load, pull-to-refresh. Assert: the toast reads "Already up to date" (a 304 case) rather
   than doing nothing silently — this is the specific dead-end case the ruling's boolean-
   resolution change exists to fix. If you can force a change server-side (edit an issue via
   another tab) between two pulls, confirm the toast flips to "Board updated" on the pull that
   picks it up.

## Regressions to re-confirm (nothing here should have moved)

1. Drag-and-drop between columns still works (§3 covers the direct interaction; also just do a
   plain drag with no pull gesture involved).
2. Drawer open/close still works normally.
3. PT-29/PT-30 disclosure toggles, containment cards, and localStorage persistence all
   unaffected by this feature's listeners.
4. The `.connection-state` pill still reports SSE state only — confirm the pull gesture's toast
   is a SEPARATE, transient element, not an overload of that pill (ruling explicit on this).

## What must NOT happen (regression watch, whole session)

- No console error at any point, including the multi-touch/horizontal/drawer-cancel paths.
- No full-page reload/navigation ever fires from this gesture (Network tab: only the existing
  ETag-conditional board fetch, never a document navigation).
- No second overlapping refetch from a repeated gesture while one is in flight.
- Existing SSE/poll refresh, drag-and-drop, and localStorage persistence behavior all
  bit-for-bit unchanged by this feature.
