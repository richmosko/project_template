---
id: PT-24
title: finish-feature test gate is advisory — pytest errors at collection, never blocks
status: done
milestone: PT-0.5.1
parent: null
assignee: null
labels: []
priority: P1
pr: null
created: 2026-08-20
updated: 2026-08-20
---


## Comments

### @architect — 2026-08-20

Found during PT-22 harness analysis (2026-08-20). The /finish-feature skill's pre-flight test gate runs, from the repo root:

  npm test 2>/dev/null || yarn test 2>/dev/null || pytest 2>/dev/null || echo "WARNING: no test runner detected"

Verified from root: npm/yarn find nothing; `pytest` ERRORS at collection (10 errors, 0 tests — wrong interpreter/cwd); the chain falls through to the echo and EXITS 0. So the gate never blocks a red suite — it is advisory. The real suite only runs from scripts/cairn (`python3 -m unittest discover -s tests` → 258 OK). There is no .github/ either — no CI at all. Every "258/258 green" this project has cited was HUMAN-run, not enforced by the skill.

Fix: make /finish-feature invoke the cairn suite correctly (cd scripts/cairn && python3 -m unittest discover -s tests) and FAIL the gate on non-zero, and — once PT-22 lands — chain the JS suite (node --test scripts/cairn/tests/js) too. Hazard noted: `npm test` is tried FIRST in the chain, so any future package.json would silently become THE gate and skip Python unless the npm script chains both — a reason the PT-22 harness deliberately avoids package.json. Separate from PT-22 (missing harness) vs this (broken invocation); bundling would hide this behind that. Consider whether a real .github/ CI lane is also wanted (separate call). P1 — the guardrail doesn't guard.

### @team-lead — 2026-08-20

Folded into the PT-22 branch/PR (Mosko, 2026-08-20) — PT-22 adds the JS suite the gate must chain, so fixing the broken pytest invocation + chaining both suites is the natural same-arc cleanup. Team-lead owns the /finish-feature edit (workflow surface).

### @team-lead — 2026-08-20

Merging with PT-22. Gate validated end-to-end: `node --test scripts/cairn/tests/js/*.test.js` runs 44/44 green from repo root; Python suite hard-gates from scripts/cairn; no npm-first ordering. Closing.
