---
id: PT-76
title: New-issue form is permanently open on every board surface and visible in the read-only dashboard card
status: done
milestone: PT-0.10
parent: null
blocked_by: []
assignee: frontend-lead
labels: []
priority: null
pr: https://github.com/richmosko/project_template/pull/151
created: 2026-09-02
updated: 2026-09-03
---


Mosko, 2026-09-02: *"The issues Card on the main dashboard page still has a line to add
new issues. This should be removed, with only the ability to add issues on the dedicated
cairn page."*

The user-visible symptom is one half of a wider defect. The new-issue form has **never been
closable on any surface** — `+ New` has been a no-op since it shipped — and the read-only
dashboard card inherits that permanently-open form because nothing in the read-only rule set
covers it.

## Defect record — measured 2026-09-02

**Browser (team-lead, `claude-in-chrome`):**

| Surface | `body.readonly` | `#new-issue-btn` computed | `#new-issue-form` `hidden` attr | `#new-issue-form` computed `display` | bounding height |
|---|---|---|---|---|---|
| `/dashboard` read-only card (`/?embed=1&readonly=1` iframe, PT-72) | `true` | `none` ✅ | `true` | **`flex`** ❌ | **27px** |
| `/board` standalone | `false` | rendered (`+ New`) | `true` | **`flex`** ❌ | **27px** |

**Source (architect, verified against `daaa074`):**

- `scripts/cairn/board/board.html:176` — `<div class="new-issue-form" id="new-issue-form" hidden>`
- `scripts/cairn/board/board.css:346` — `.new-issue-form { display: flex; … }`, with **no** `[hidden]` counter-rule anywhere in the file
- `scripts/cairn/board/board.js:2910-2925` — `+ New` does `form.hidden = !form.hidden`; the JS state has been toggling correctly the whole time and nothing has ever obeyed it
- `scripts/cairn/board/board.css:181-187` — the `body.readonly` hide list is `.filters, .view-tabs, #new-issue-btn, #expand-all-btn, #collapse-all-btn, .swimlane-toggle, .repo-group-toggle`. **`#new-issue-form` is absent.**

**Age:** the `.new-issue-form { display: flex }` rule and the markup's `hidden` attribute are both present at v0.6.0, v0.7.1, v0.8.0 and v0.9.0 with no `[hidden]` counter-rule until PT-69 added two element-specific ones for the theme menu. Shipped broken in at least four releases.

## Root cause — and a correction to the mechanism recorded in PT-69

`board.css:1110-1119` (PT-69) explains the same bug for `.theme-settings-menu` as a
**specificity tie** broken by source order: *"the `hidden` attribute and this class's own
`display: flex` above are the SAME specificity … so without this rule the browser's
UA-stylesheet `[hidden] { display: none }` LOSES the cascade tie to whichever rule loads later."*

**That model is wrong, and the corrected one matters for the fix.** Specificity and source
order are never consulted here. **Cascade origin is compared first**, and author-origin
declarations unconditionally outrank UA-origin declarations (CSS Cascade §6.1 — Origin and
Importance precedes Specificity and Order of Appearance). So an author `display: flex` beats
the UA `[hidden] { display: none }` **always** — regardless of specificity, and regardless of
which loads later. That is why this reproduces 100% of the time rather than intermittently, and
why "just put your rule after theirs" would not have helped.

The consequence for the fix: **raising specificity is not a general remedy.** A
`[hidden][hidden]` rule (0-2-0) would still lose to any future `#some-id { display: block }`
(1-0-0). Only an author-origin `!important` declaration is both origin- and
specificity-independent.

## Architect ruling — one generic reset, retiring the per-element fixes

**(a) Fix:** add a single rule near the top of `board.css`:

```css
[hidden] { display: none !important; }
```

and **delete** the two element-specific counter-rules it subsumes — `.theme-settings-menu[hidden]`
(`board.css:1120`) and `.theme-settings-row-flyout[hidden]` (`board.css:1171`) — **moving PT-69's
explanatory comment to the generic rule, corrected per the section above.** Do not delete the
explanation; it is the reason the rule exists.

**Why generic, over three more per-element rules:**

1. **It makes the bug class unrepresentable rather than fixing one instance.** The per-element
   approach requires every future author who adds a `display:` declaration to remember to pair it
   with a `[hidden]` rule. That invariant has already been forgotten once out of three
   opportunities, silently, for four releases.
2. **`!important` is warranted and its blast radius is provably nil.** The generic rule can only
   change rendering for an element that *currently* has an author `display` beating the UA rule —
   i.e. exactly the buggy ones. Every element carrying `hidden` today with no author `display`
   declaration (`#pull-indicator`, `#engine-stale-banner`, `#filter-repo`, the
   `.view-state-btn` pair) already resolves to `display: none`, and the two PT-69 elements already
   resolve to `display: none`. **The rule is behaviour-preserving everywhere except where it is
   the fix.** Verify this claim during implementation rather than taking it from here.
3. **There is no legitimate override.** `hidden` is HTML's "not relevant" semantic. Present-but-
   invisible is expressed in this codebase with `transform` / `visibility` (`.pull-indicator` does
   exactly that), never by keeping `display` alive under `hidden`.
4. **This overrides PT-69's stated `!important`-free preference, deliberately.** That preference
   was correct for PT-69's scope — one element, where specificity-raising was sufficient and
   local. It does not generalise: specificity-raising is neither sufficient (an id rule defeats
   it) nor self-maintaining (it must be re-remembered per element). Record the override in the
   comment so it doesn't read as drift.

**(b) Read-only surface carries no create affordance.** Add `#new-issue-form` to the
`body.readonly` hide list at `board.css:181-187`, alongside `#new-issue-btn`. Keep it even
though (a) makes it redundant while the attribute is set: the read-only rule is the *policy*
statement ("this surface has no create affordance"), and it must hold even if the toggle is
reached some other way.

**This is a gap in my own PT-72 ruling, not an implementation deviation.** That ruling's
"hidden in read-only" list was presented as complete and named the "create-issue button" without
naming the form the button toggles. The implementation matched the ruling; the ruling was short.

## Acceptance criteria

**Fix**

- [x] `board.css` contains a single generic `[hidden] { display: none !important; }` rule.
- [x] `.theme-settings-menu[hidden]` and `.theme-settings-row-flyout[hidden]` are removed, and
      PT-69's explanatory comment survives on the generic rule with the cascade-origin correction
      (not the "specificity tie broken by source order" model).
- [x] `#new-issue-form` is present in the `body.readonly` hide list at `board.css:181-187`.
- [x] No behaviour change to the theme settings menu or the row flyout: both still open on
      trigger and close on outside-click / Escape / re-trigger.

**Browser verification — the acceptance leg, not ceremony**

A property or attribute read is **not** evidence here: `hidden` was `true` on both surfaces the
entire time the user was looking at the form. Every check below is `getComputedStyle` +
`getBoundingClientRect` + `document.elementFromPoint` at the element's centre.

- [x] `/board` standalone, before clicking `+ New`: `#new-issue-form` computed `display` is
      `none` and its bounding height is `0`.
- [x] `/board` standalone, after clicking `+ New`: form is visible (`display` not `none`,
      height > 0, `elementFromPoint` at its centre resolves inside it).
- [x] `/board` standalone, after clicking `+ New` twice: back to `display: none`, height `0`.
      **This is the assertion that has never passed.**
- [x] `/dashboard` read-only card (inside the `/?embed=1&readonly=1` iframe): `#new-issue-form`
      computed `display` is `none` and height `0`, and no create affordance is reachable —
      `#new-issue-btn` also `none`.
- [x] `/dashboard/issues` (the full board inside the shell, PT-72): behaves as the standalone
      `/board` — form closed on load, `+ New` toggles it. **Check this placement explicitly**;
      one sampled surface does not generalise across DOM placements.
- [x] **Census, not sample:** for every element in `board.html` carrying the `hidden` attribute
      (`#pull-indicator`, `#engine-stale-banner`, `#theme-settings-menu`, `#filter-repo`,
      `#expand-all-btn`, `#collapse-all-btn`, `#new-issue-form`) plus any JS-created
      `.theme-settings-row-flyout`, assert computed `display === "none"` while `hidden` is set.
      This is the proof of ruling claim (2) — behaviour-preserving everywhere except the fix.

**Automated regression — what the harness can and cannot do**

The JS harness has **no jsdom and no `document`**, so the "assert the form is not *rendered*
before `+ New` is clicked" test cannot be written as computed style. This is a documented,
re-confirmed limit, not an omission — see `scripts/cairn/tests/js/embed-visibility-scope.test.js`
lines 3-9 ("impossible — no jsdom, no document, confirmed against 8 existing test files that
already document the same limit"). The browser leg above is therefore **load-bearing acceptance,
not ceremony.** What the harness *can* assert, and must:

- [x] New test `scripts/cairn/tests/js/hidden-attribute-reset.test.js`, source-text guard in the
      same shape as `embed-visibility-scope.test.js`:
      (i) `board.css` declares a generic `[hidden]` rule whose `display` is `none` **and carries
      `!important`** — assert the `!important`, since dropping it silently reintroduces the bug
      for any id-based `display` rule;
      (ii) the `body.readonly` hide-list selector set is extracted and asserted to contain
      `#new-issue-form` **and** `#new-issue-btn`;
      (iii) no element-specific `X[hidden] { display: none }` rules remain (they are now dead
      weight and their return would signal the generic rule was lost);
      (iv) every extractor raises a **named `ExtractionError`** when its pattern matches nothing —
      never a silent empty list, never a vacuously-green pass. Same contract as PT-36 / PT-45.
- [x] `scripts/cairn/tests/test_css_parse_sanity.py` stays green. **Do not re-invent it** — the
      PT-57 parse-level gate (stray comment-closer, brace balance, top-level rule-count floor)
      already covers `board.css`. Net rule count moves by −1 (two removed, one added), well clear
      of the `min_rules=50` floor.
- [x] Full gate green: `( cd scripts/cairn && python3 -m unittest discover -s tests )` and
      `node --test scripts/cairn/tests/js/*.test.js`.
- [x] `scripts/cairn/cairn check` prints `ok`.

## Comments

### @team-lead — 2026-09-03

Feature started. Branch: `feature/pt-76-new-issue-form-hidden`. Delivery pre-authorized by Mosko (2026-09-02): merge PT-0.10 issues, then release v0.10.0. Shared-checkout rule in force: one writer per file; reviewers verify from commit objects or their own worktree.

### @team-lead — 2026-09-03

**Browser verification leg — team-lead, claude-in-chrome, 2026-09-03, against 691e7ae** (served board.css byte-identical to the commit: `git diff HEAD -- scripts/cairn/board/board.css` empty at probe time). Every check is `getComputedStyle` + `getBoundingClientRect` + `elementFromPoint` at the element's centre, never an attribute read.

| Placement | before `+ New` | after once | after twice |
|---|---|---|---|
| `/board` standalone | display `none`, h 0 | `flex`, h 27, centre resolves inside the form | `none`, h 0 |
| `/dashboard/issues` (`/?embed=1` iframe) | `none`, h 0 | `flex`, h 27, centre inside | `none`, h 0 |
| `/dashboard` read-only card (`/?embed=1&readonly=1` iframe) | `none`, h 0; `#new-issue-btn` also `none`; `#new-issue-submit` box height 0 | — | — |

"After twice → none" is the assertion that had never passed on any surface.

**Census (every element carrying `hidden` at probe time), all three placements:** `#pull-indicator`, `#engine-stale-banner`, `#theme-settings-menu`, `#theme-settings-row-flyout-{mode,base,theme,chart}`, `#filter-repo`, `#new-issue-form` — computed `display === "none"`, height 0, on every one. Ruling claim (2) holds in the browser, not just statically. (`#expand-all-btn` / `#collapse-all-btn` were not `hidden` at probe time; they render normally.)

**Theme settings menu + row flyout (the one Fix-block box frontend-lead left for a live browser):** menu `none` → trigger → `flex` → Escape → `none`; open → outside click on body → `none`; open → re-trigger → `none`. Flyout (`base` row): row trigger → `flex` → re-trigger → `none` → trigger → `flex` → Escape → flyout `none` while menu stays open (nested close, one level per Escape); second Escape closes the menu. No behaviour change.

Screenshot of the read-only card without the new-issue row delivered to Mosko in-session. All 7 remaining acceptance boxes ticked on this evidence.

### @architect — 2026-09-03

**Architectural review at `06b33e4` — NOT APPROVED. One gating ask, ~5 lines, on the guard
rather than the fix.** Verified from commit objects and a private `git worktree` (removed
after); the shared checkout was never mutated.

## Ruling text landed as specified — confirmed, four for four

- Generic `[hidden] { display: none !important; }` present near the top of `board.css`,
  immediately after `* { box-sizing: border-box; }`.
- Comment carries the **corrected cascade-origin model** — origin compared before specificity
  and source order, author `display` beats UA `[hidden]` unconditionally, hence 100% of the
  time rather than intermittently — plus the explicit note that this overrides PT-69's stated
  `!important`-free preference and why that preference doesn't generalise.
- Both PT-69 per-element rules (`.theme-settings-menu[hidden]`,
  `.theme-settings-row-flyout[hidden]`) and their old comments are gone.
- `#new-issue-form` sits in the `body.readonly` hide list beside `#new-issue-btn`, and the
  PT-72 comment above it now names the form explicitly — closing the enumeration gap in my
  own PT-72 ruling.

## Behaviour-preserving claim — verified from the complete population, not sampled

My ruling asked for this to be verified rather than taken from me. Rather than grepping each
class name, I extracted **every rule in `board.css` that declares `display`** — 30 of them —
and matched the full set against every `hidden`-carrying element in `board.html` plus the
JS-created `.theme-settings-row-flyout`. Only `.theme-settings-menu`,
`.theme-settings-row-flyout` and `.new-issue-form` are reachable by an author `display`
declaration. `#pull-indicator`, `#engine-stale-banner`, `#filter-repo` and the
`#expand-all-btn` / `#collapse-all-btn` pair are matched by **none** — `.filters` and
`.filters label` hit the container and the label wrappers, not the bare `<select>` or the
buttons. The generic rule therefore changes rendering on exactly the three previously-broken
elements and nothing else, as claimed.

## GATING — the guard cannot defend the invariant it exists for

Filter-evasion constructions against `hidden-attribute-reset.test.js`, baseline 11/11:

| Construction | Result | Effect |
|---|---|---|
| Drop `!important` from the generic rule | **fail 1** — caught | — |
| Remove `#new-issue-form` from the readonly list | **fail 1** — caught | — |
| Append `[hidden] { display: flex !important; }` | **11/11 GREEN** | every `hidden` element renders again |
| Append `.new-issue-form { display: flex !important; }` | **11/11 GREEN** | **restores PT-76's exact original bug** |

In both green cases a later author rule of equal origin and importance wins on source order
(specificity ties at 0-1-0). The extractor asserts that *a* conforming bare `[hidden]` rule
exists; it does not assert that **every** bare `[hidden]` rule conforms, and it does not
constrain competing `!important` `display` declarations elsewhere in the file. Same
filter-vs-property shape as PT-75's own defect 2, one layer up.

**Why this is gating and not a note.** The entire reason this ruling chose one generic reset
over three per-element fixes was that it "makes the bug class unrepresentable rather than
fixing instances one at a time." Two one-line appends falsify that with the suite green. That
is this ruling's central justification, so it is held to it.

**The ask — two assertions, on machinery the file already has (`eachRule` +
`stripCssComments`), each with a negative-control fixture matching the eight already written:**

1. **Every** bare `[hidden]` rule declares `display: none !important` — not merely that one
   such rule exists.
2. **No rule other than the generic `[hidden]` reset declares `display` with `!important`.**

Both are satisfiable today with **no change to `board.css`** — measured at `06b33e4`: exactly
one bare `[hidden]` rule, and exactly one `!important` declaration in the whole file
(`[hidden] → display: none !important`). Assertion 2 also pins the real invariant: the file's
`!important` budget is exactly one, deliberately taken.

**I own the risk that makes assertion 2 necessary.** This ruling introduced the file's first
`!important` and did not flag that doing so lowers the barrier to a second. That assertion
guards a cost this ruling created.

## Everything else verified and clear

- **Parse-sanity floor (team-lead's ask 2): clear with wide margin.** Independent scan of
  `board.css` at `06b33e4` — **141 top-level rules against a `min_rules=50` floor**, final
  brace depth 0, minimum depth 0 (never negative), no stray comment-closer.
  `test_css_parse_sanity.py` green, 14 tests. Net rule count moved −1 as predicted.
- **Browser leg (team-lead, `06b33e4`) is the load-bearing acceptance check and is done
  properly** — three placements, the toggle-twice assertion that had never passed on any
  surface, the full `hidden` census, and the theme-menu / row-flyout regression. This is what
  the ruling meant by "not ceremony."
- **`stripCssComments` plus the prose regression guard** correctly defeat the PT-55 trap where
  a comment quoting the ruling's own vocabulary is read as a rule.
- **Full gate at `691e7ae` in my worktree:** 1066 Python (15 skipped) / 395 JS / `cairn check`
  ok — matching frontend-lead's and qa-engineer-76's independent runs at `06b33e4`.

### @architect — 2026-09-03

**APPROVED at `c3c5eab`.** The gating ask from `49c3f18` is closed. Verified from commit
objects and a private `git worktree`, removed after; the shared checkout was never mutated.

## Both evasions closed — and five more probed, all caught

Re-ran the two constructions that were green at `06b33e4`, plus the two that were already
caught (to confirm no regression), plus three further probes of the new assertions. Baseline
at `c3c5eab` is 19/19.

| # | Construction against `board.css` | at `06b33e4` | at `c3c5eab` |
|---|---|---|---|
| 1 | Drop `!important` from the generic rule | fail 1 | **fail 3** |
| 2 | Remove `#new-issue-form` from the `body.readonly` list | fail 1 | **fail 1** |
| 3 | Append `[hidden] { display: flex !important; }` | **GREEN** ❌ | **fail 2** ✅ |
| 4 | Append `.new-issue-form { display: flex !important; }` | **GREEN** ❌ | **fail 1** ✅ |
| 5 | Append `.new-issue-form[hidden] { display: flex !important; }` — compound, 0-2-0, beats the reset | not probed | **fail 2** ✅ |
| 6 | `@media (min-width: 1px) { [hidden] { display: flex !important; } }` | not probed | **fail 2** ✅ |
| 7 | `@media (min-width: 1px) { .new-issue-form { display: flex !important; } }` | not probed | **fail 1** ✅ |

Constructions 5–7 were not part of the ask. 5 is the higher-specificity compound that would
beat the reset on its own terms; 6 and 7 test whether the extractors walk into at-rule blocks,
which is where a rule-by-rule scanner most plausibly goes blind. All three are caught. The
`!important`-on-`display` budget assertion is what generalises here: it does not care where the
competing rule lives or what its selector looks like, only that there is more than one.

**`board.css` is byte-identical to `691e7ae`** — `git diff 691e7ae..c3c5eab -- board.css` is
empty, confirming both assertions were satisfiable without touching the fix, as predicted.

## Team-lead's three asks

**(1) Ruling text landed as specified — confirmed, four for four** (recorded in full at
`49c3f18`): generic reset in place after `* { box-sizing: border-box; }`; corrected
cascade-origin comment including the "100% of the time, not intermittently" consequence and the
explicit PT-69 `!important`-free override; both per-element rules and their old comments gone;
`#new-issue-form` in the `body.readonly` list with the PT-72 comment now naming the form.

**(2) Parse-sanity floor clear with wide margin** — independent scan of `board.css`: **141
top-level rules against `min_rules=50`**, final brace depth 0, minimum depth never negative, no
stray comment-closer. `test_css_parse_sanity.py` green, 14 tests.

**(3) The browser evidence at `06b33e4` satisfies the acceptance leg — yes.** It is the right
*kind* of evidence, which was the whole point of making it load-bearing: computed `display`,
bounding height, and `elementFromPoint` at the element's centre, never an attribute or property
read. `hidden` was `true` on both surfaces the entire time the user was looking at the form, so
a property read would have passed here. It covers all three placements (`/board`,
`/dashboard/issues`, the read-only `/dashboard` card), the **toggle-twice** assertion that had
never passed on any surface, the full census over every `hidden`-carrying element, and the
theme-menu / row-flyout regression that the retired PT-69 rules used to cover.

## Final state at `c3c5eab`

- Full gate in my worktree: **1066 Python (15 skipped) / 403 JS / `cairn check` ok.** JS moved
  395 → 403 with the eight new tests; matches qa-engineer-76's independent run.
- Behaviour-preserving claim verified from the **complete population** of 30 `display`-declaring
  rules in `board.css` matched against every `hidden`-carrying element — only the three
  previously-broken elements are reachable by an author `display` rule.
- The two constructions are now pinned as synthetic regression-guard fixtures in the test file
  itself, so neither can silently return.

### @team-lead — 2026-09-03

PR opened: https://github.com/richmosko/project_template/pull/151. Validate already satisfied on-branch: QA final gate green at c3c5eab (isolated worktree), architect APPROVED (bb594bc). Merge pre-authorized by Mosko for PT-0.10.

### @team-lead — 2026-09-03

Validate passed; merging via PR #151. Closing. Last open issue in PT-0.10 — milestone flips to done in this commit.
